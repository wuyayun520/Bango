
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kCropSucceedTouchName:[UInt8] = [0x77,0x7c,0x77,0x82,0x36,0x71,0x7d,0x72,0x73,0x80,0x48,0x37,0x2e,0x76,0x6f,0x81,0x2e,0x7c,0x7d,0x82,0x2e,0x70,0x73,0x73,0x7c,0x2e,0x77,0x7b,0x7e,0x7a,0x73,0x7b,0x73,0x7c,0x82,0x73,0x72]

fileprivate func attributeEqual(top num: UInt8) -> UInt8 {
    let value = Int(num) - 14
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let k_dismissName:String = "image audience click heightLoad.."
fileprivate let kSharedDeleteValue:[Character] = ["."]

/*: "Skip" :*/
fileprivate let k_addValue:String = "view inside show model colorSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let kActionData:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let k_displayLabName:[UInt8] = [0x6e,0x70,0x77,0x7d,0x76,0x6e,0x37,0x76,0x77,0x49,0x78,0x7e,0x7c,0x4a,0x71,0x76,0x6e,0x3f,0x3f,0x76,0x77,0x49,0x78,0x7e,0x7c,0x4a,0x71,0x76,0x6e,0x31,0x30,0x22]

private func oldBlockCell(from num: UInt8) -> UInt8 {
    return num ^ 25
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let kColorTempName:String = "manager viewjsEvent"
fileprivate let kLineViewText:[Character] = ["(","o","n"]
fileprivate let kEqualModelName:[Character] = ["P","a","g","e","S","h","o","w",")",":"," "]

/*: --- :*/
fileprivate let kLeadingData:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let k_blockData:[UInt8] = [0xea,0xdc,0xe1,0xd7,0xe2,0xea,0xa1,0xe2,0xe1,0xc3,0xd4,0xda,0xd8,0xbb,0xdc,0xd7,0xd8,0x99,0x99,0xe2,0xe1,0xc3,0xd4,0xda,0xd8,0xbb,0xdc,0xd7,0xd8,0x9b,0x9c,0xae]

fileprivate func equalTitle(view num: UInt8) -> UInt8 {
    let value = Int(num) + 141
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let k_toBackContent:String = "jsEventview last color data add"
fileprivate let kMagnitudeName:[Character] = ["(","o","n","P","a","g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let k_modelMessageData:String = "viewiviewle"

/*: "canGoBack" :*/
fileprivate let k_statusData:String = "canGoBackvalue self hidden index"

/*: "PHPSESSID= :*/
fileprivate let k_imageData:[Character] = ["P","H","P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let kRequestLabelValue:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let kUserTitle:String = "Cookienormal message self"

/*: "nav_back_black_nor" :*/
fileprivate let k_dataContent:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c","k","_"]
fileprivate let k_viewColorData:String = "modelr"

/*: "ico_intimate_list_sm" :*/
fileprivate let kCenterErrorData:String = "click giftico_i"
fileprivate let k_atDetailViewTitle:[Character] = ["n","t","i","m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let kAwakeData:[UInt8] = [0xfa,0xf1,0xfd,0xeb,0xf3,0xfb,0xf0,0xea,0xb0,0xf9,0xfb,0xea,0xdb,0xf2,0xfb,0xf3,0xfb,0xf0,0xea,0xed,0xdc,0xe7,0xca,0xff,0xf9,0xd0,0xff,0xf3,0xfb,0xb6,0xb9,0xfc,0xf1,0xfa,0xe7,0xb9,0xb7,0xc5,0xae,0xc3,0xb0,0xed,0xea,0xe7,0xf2,0xfb,0xb0,0xfc,0xff,0xfd,0xf5,0xf9,0xec,0xf1,0xeb,0xf0,0xfa,0xa3,0xb9,0xec,0xf9,0xfc,0xff,0xb6,0xae,0xb2,0xae,0xb2,0xae,0xb2,0xae,0xb7,0xb9]

private func resultData(of num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "Alert" :*/
fileprivate let k_opValue:String = "Alerttype tab request for fill"

/*: "OK" :*/
fileprivate let kMakeName:String = "maleK"

/*: "Cancle" :*/
fileprivate let k_labelMessageText:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let k_textData:String = "Donei value true"

/*: "js call method name =  :*/
fileprivate let k_centerText:[Character] = ["j","s"," ","c","a","l"]
fileprivate let kFormSideData:String = "l metof manager let"
fileprivate let k_receiveValue:String = "ame = hidden voice range color"

/*: , message =  :*/
fileprivate let k_managerName:String = ", messhidden view var live"
fileprivate let k_cancelData:[Character] = ["="," "]

/*: "http://" :*/
fileprivate let kViewObserverValue:String = "item"
fileprivate let kInviteValue:[Character] = ["t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let kLabFrameAddValue:String = "httmakes"
fileprivate let kTextModelTitle:String = ":ageage"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let k_keyValue:[UInt8] = [0x95,0x80,0x91,0x3f,0x82,0x8e,0x8e,0x8a,0x88,0x84,0x6d,0x80,0x8c,0x84,0x92,0x3f,0x5c,0x3f,0x83,0x8e,0x82,0x94,0x8c,0x84,0x8d,0x93,0x4d,0x82,0x8e,0x8e,0x8a,0x88,0x84,0x4d,0x92,0x8f,0x8b,0x88,0x93,0x47,0x46,0x5a,0x3f,0x46,0x48,0x4d,0x8c,0x80,0x8f,0x47,0x85,0x94,0x8d,0x82,0x93,0x88,0x8e,0x8d,0x47,0x82,0x8e,0x8e,0x8a,0x88,0x84,0x48,0x3f,0x9a,0x3f,0x91,0x84,0x93,0x94,0x91,0x8d,0x3f,0x82,0x8e,0x8e,0x8a,0x88,0x84,0x4d,0x92,0x8f,0x8b,0x88,0x93,0x47,0x46,0x5c,0x46,0x48,0x7a,0x4f,0x7c,0x3f,0x9c,0x3f,0x48,0x5a,0x29]

fileprivate func hiddenShared(model num: UInt8) -> UInt8 {
    let value = Int(num) + 225
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let kIndexValue:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let k_centerBehaviorName:String = "view other shared;domain="

/*: ;path=/" :*/
fileprivate let k_errViewText:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let kDismissTitle:[Character] = ["d","o","c","u","m","e","n","t",".","c","o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let k_resultData:String = "\';\n"

/*: ;domin= :*/
fileprivate let kLabelActualName:String = "make tool true equal face;d"
fileprivate let k_modelName:[Character] = ["o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let kModeTitle:[UInt8] = [0xfd,0xf2,0xb4,0xbc,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xda,0xf5,0xf9,0xf1,0xe7,0xba,0xfd,0xfa,0xf0,0xf1,0xec,0xdb,0xf2,0xbc,0xb3,0xc4,0xdc,0xc4,0xc7,0xd1,0xc7,0xc7,0xdd,0xd0,0xb3,0xbd,0xb4,0xa9,0xa9,0xb4,0xb9,0xa5,0xbd,0xb4,0xef,0xb4,0xf0,0xfb,0xf7,0xe1,0xf9,0xf1,0xfa,0xe0,0xba,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xa9,0xb3]

private func outOfSight(gift num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: '; };\n" :*/
fileprivate let k_normalEqualName:[Character] = ["\'"]
fileprivate let kPathName:String = "; };\n"

/*: '" :*/
fileprivate let kNumberData:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let k_selectedName:[Character] = ["h","5","D","o","m","a","i","n"," "]
fileprivate let k_errTitle:[Character] = ["="," "]

/*: "getAllCookies =  :*/
fileprivate let kPositionName:String = "geview"
fileprivate let kLabelValue:String = "player manager to view elseAllCoo"

/*: "PHPSESSID" :*/
fileprivate let kOfName:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let kCountName:String = "ustatus"

/*: "appCookies =  :*/
fileprivate let k_onName:String = "appCooself data"
fileprivate let k_labelTitle:String = "image"

/*: "UID" :*/
fileprivate let k_appTitle:String = "Uprice"

/*: "navigator.userAgent" :*/
fileprivate let k_lengthValue:String = "navimodel"
fileprivate let kAddValue:String = "media equal list value.user"
fileprivate let k_appearanceValue:String = "Agentas model"

/*: "xunwu_iphone" :*/
fileprivate let k_momentModelValue:String = "taskunwu"
fileprivate let kManagerLabelTitle:String = "nfollowing"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let kTapData:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x"]
fileprivate let kButtonSucceedName:[Character] = ["u","n","w","u","_","v"]
fileprivate let kTopTitle:[Character] = ["e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let kLocationValue:String = "blue view xun"
fileprivate let k_equalText:String = "kage/let color import"

/*:  xunwu_bundleId/ :*/
fileprivate let k_pathData:[Character] = [" ","x","u","n","w","u","_","b"]
fileprivate let k_onData:String = "undleId/end price to var me"

/*:   webkit/1" :*/
fileprivate let k_sizeData:String = "string to manager more type  web"

/*: "  :*/
fileprivate let k_makeValue:String = "once"

/*: " xunwu_device_id/ :*/
fileprivate let k_blockUserName:String = " xunwvar start error mode to"
fileprivate let k_cancelModelValue:String = "class res bubbleice_id/"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MakeUpNavigationDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class MakeUpNavigationDelegate: GiftHorseReactiveCompatible {
	var spaceName: String = "content"
	var byArray: [AnyHashable] = []
	var channelDictionary: [AnyHashable: String] = [:]
	var frontLineTitle: String = "at"
	var phoneMessageArray: [AnyHashable] = []
	var cellDictionary: [AnyHashable: String] = [:]

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: GiftAddConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: LastSequence?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: LastSequence?, webConfig: GiftAddConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kCropSucceedTouchName.map{attributeEqual(top: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        mightHaveBeen()
  
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        doAdd()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(k_dismissName.suffix(6)) + String(kSharedDeleteValue))
        //: addNotification()
        motivationDismiss()
        //: func__checkConfig()
        ingotFire()
        //: func__setUpWebView()
        snuggledPlayer()
        //: func__registerObserver()
        funcLayerSoul()
        //: func__beginStartRequest()
        cellOnRequest()
        //: func_checkAppleIAP()
        checkBar()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_restoreLimitVideoTitle, object: nil)
            }
        }
    
		
	}

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            addConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.cellColor(), .font: UIFont.ofAndSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_addValue.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension MakeUpNavigationDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func motivationDismiss() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(incursion),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_timeTitle,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(mightHaveBeen),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func incursion() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kActionData.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func mightHaveBeen() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: k_displayLabName.map{oldBlockCell(from: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(kColorTempName.suffix(7)) + String(kLineViewText) + String(kEqualModelName)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func doAdd() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: k_blockData.map{equalTitle(view: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_toBackContent.prefix(7)) + String(kMagnitudeName)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension MakeUpNavigationDelegate {
    //: func func__registerObserver() {
    func funcLayerSoul() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (k_modelMessageData.replacingOccurrences(of: "view", with: "t")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(k_statusData.prefix(9))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(sendView), name: kLet_errorData, object: nil)
    }

    //: func func__setUpWebView() {
    func snuggledPlayer() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        changeForAdd()
        //: func__addBridgeMethod()
        pontoonBridge()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.textTag()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            ViewBeginReactiveCompatible.backView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_topData)
            }
        }
    }

    //: func func__beginStartRequest() {
    func cellOnRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.labStartPetition()
            }
            //: } else {
        } else {
            //: func__startRequest()
            labStartPetition()
        }
    }

    //: func func__startRequest() {
    func labStartPetition() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = RowReactiveCompatible.share.loginUid
        let uid = RowReactiveCompatible.share.loginUid
        //: let ssid = RowReactiveCompatible.share.loginSessionId
        let ssid = RowReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(k_imageData)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(kRequestLabelValue)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(kUserTitle.prefix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        modeColor()
        //: func__initRightButton()
        mark()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func checkBar() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            DetailDecisionMakerTransactionObserver.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.succeedDoing(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            DetailDecisionMakerTransactionObserver.shared.examineOf()
        }
    }

    //: func func__initLeftButton() {
    func noneUser() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.collectionName(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.collectionName(name: (String(k_dataContent) + k_viewColorData.replacingOccurrences(of: "model", with: "no"))), style: .plain, target: self, action: #selector(enableChannel(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func mark() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(LastSequence.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.collectionName(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.collectionName(name: (String(kCenterErrorData.suffix(5)) + String(k_atDetailViewTitle))), style: .plain, target: self, action: #selector(makeAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func enableChannel(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        confusionCell()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.belowResume() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: MakeUpNavigationDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! MakeUpNavigationDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! MakeUpNavigationDelegate).mightHaveBeen()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func ingotFire() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func liveHide(sucess: Bool) {
        //: ProgressHUD.dismiss()
        ViewBeginReactiveCompatible.capability()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func modeColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: kAwakeData.map{resultData(of: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func targetPrise(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: MiraclePushManager.share.func__pushToWebVC(urlStr: url)
            MiraclePushManager.share.pathTo(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func atUrl(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func sendView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_errorData, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func makeAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = ReloadViewDelegate(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
        //: view.show()
        view.shield()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension MakeUpNavigationDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            ViewBeginReactiveCompatible.backView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            liveHide(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        noneUser()
        //: func__clearJSBgColor()
        modeColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            liveHide(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            liveHide(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(k_opValue.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(k_labelMessageText)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(k_textData.prefix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension MakeUpNavigationDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(k_centerText) + String(kFormSideData.prefix(5)) + "hod n" + String(k_receiveValue.prefix(6))) + "\(message.name)" + (String(k_managerName.prefix(6)) + "age " + String(k_cancelData)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.shootingScript(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func pontoonBridge() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ItemConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func confusionCell() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ItemConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension MakeUpNavigationDelegate {
    //: func func__addUserScript() {
    func twentyFourHours() {
        //: var htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((kViewObserverValue.replacingOccurrences(of: "item", with: "h") + String(kInviteValue))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (kViewObserverValue.replacingOccurrences(of: "item", with: "h") + String(kInviteValue)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((kLabFrameAddValue.replacingOccurrences(of: "make", with: "p") + kTextModelTitle.replacingOccurrences(of: "age", with: "/"))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (kLabFrameAddValue.replacingOccurrences(of: "make", with: "p") + kTextModelTitle.replacingOccurrences(of: "age", with: "/")), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = RowReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = RowReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = RowReactiveCompatible.share.loginSessionId
        let tempSessionID: String = RowReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: k_keyValue.map{hiddenShared(model: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kIndexValue)) + "\(tempUid)" + (String(k_centerBehaviorName.suffix(8))) + "\(htmlDomain)" + (String(k_errViewText))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(kDismissTitle)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(k_imageData)) + "\(tempSessionID)" + (String(kLabelActualName.suffix(2)) + String(k_modelName)) + "\(htmlDomain)" + (String(k_errViewText))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(kDismissTitle)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: kModeTitle.map{outOfSight(gift: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kIndexValue)) + "\(tempUid)" + (String(k_centerBehaviorName.suffix(8))) + "\(htmlDomain)" + (String(k_errViewText))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(kDismissTitle)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(kIndexValue)) + "\(tempSessionID)" + (String(k_centerBehaviorName.suffix(8))) + "\(htmlDomain)" + (String(k_errViewText))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(kDismissTitle)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func addConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(k_selectedName) + String(k_errTitle)) + "\(htmlDomain)")
            //: let userID: String = RowReactiveCompatible.share.loginUserMode.userID
            let userID: String = RowReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (kPositionName.replacingOccurrences(of: "view", with: "t") + String(kLabelValue.suffix(6)) + "kies = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(kOfName)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (kCountName.replacingOccurrences(of: "status", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.cellOnRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.overTap(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func overTap(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(k_selectedName) + String(k_errTitle)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: GiftViewReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(k_onName.prefix(6)) + "kies =" + k_labelTitle.replacingOccurrences(of: "image", with: " ")) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (k_appTitle.replacingOccurrences(of: "price", with: "ID")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (kCountName.replacingOccurrences(of: "status", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(kOfName)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(kOfName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.cellOnRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func changeForAdd() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((k_lengthValue.replacingOccurrences(of: "model", with: "g") + "ator" + String(kAddValue.suffix(5)) + String(k_appearanceValue.prefix(5)))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((k_momentModelValue.replacingOccurrences(of: "task", with: "x") + "_ipho" + kManagerLabelTitle.replacingOccurrences(of: "following", with: "e"))) {
                let str = "xunwu_iphone xunwu_version/\(kLet_tableStatusOutContent) xunwu_package/\(kLet_inviteValue) xunwu_bundleId/\(kLet_tableTitle)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.mugshot { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(k_blockUserName.prefix(5)) + "u_dev" + String(k_cancelModelValue.suffix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
