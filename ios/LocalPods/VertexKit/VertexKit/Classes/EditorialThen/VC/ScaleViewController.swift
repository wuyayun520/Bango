
//: Declare String Begin

/*: "nil" :*/
fileprivate let kValueText:String = "nilocation"

/*: "Edit profiles" :*/
fileprivate let k_modelValue:String = "Edilet return self"
fileprivate let kSizeResignData:String = "public returnt prof"

/*: "Done" :*/
fileprivate let k_labText:String = "white model gift fragmentDone"

/*: "#999999" :*/
fileprivate let k_imageTitle:[Character] = ["#","9","9","9","9","9","9"]

/*: "uid" :*/
fileprivate let k_countTheName:String = "uiclick"

/*: "AtFieldDelegate" :*/
fileprivate let k_closeName:[Character] = ["T","a","l","k","i","n"]
fileprivate let k_qualityValue:String = "gEmodel player size"
fileprivate let k_blockValue:String = "return letditN"

/*: "UserReactiveCompatible" :*/
fileprivate let k_ofValue:String = "side line top kindTalkin"
fileprivate let k_postTitle:String = "ignCellextension let model"

/*: "Save the change?" :*/
fileprivate let k_colorStyleValue:[Character] = ["S","a","v","e"," ","t","h","e"," ","c","h"]
fileprivate let k_logData:String = "ange?path bottom in color"

/*: "Cancel" :*/
fileprivate let k_presentationName:[Character] = ["C","a","n","c","e","l"]

/*: "Save" :*/
fileprivate let k_situationTitle:String = "var guardSave"

/*: "nickname" :*/
fileprivate let k_screenMakeName:[Character] = ["n","i"]
fileprivate let kTopData:[Character] = ["c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let kLikeContent:String = "birvideo"

/*: "desc" :*/
fileprivate let k_mValue:String = "deinfoc"

/*: "Modify the success" :*/
fileprivate let kOriginText:String = "simultaneously last typeModif"
fileprivate let kEqualViewName:String = "SUCCESS"

/*: "pic" :*/
fileprivate let k_observerTitle:String = "sizeic"

/*: "[ :*/
fileprivate let k_topText:[Character] = ["["]

/*: ]" :*/
fileprivate let kViewName:String = "make"

/*: "pids" :*/
fileprivate let kAddData:[Character] = ["p","i","d","s"]

/*: "aboutMe" :*/
fileprivate let k_managerListTitle:String = "abotitlet"
fileprivate let k_toName:[Character] = ["M","e"]

/*: "interest" :*/
fileprivate let kFillData:String = "intereuser"

/*: "tagIds" :*/
fileprivate let kWithName:[Character] = ["t","a","g","I","d","s"]

/*: "category" :*/
fileprivate let kLabData:String = "cateimageory"

/*: "An error occurred, please try again" :*/
fileprivate let kErrorValue:[UInt8] = [0x24,0xb,0x45,0x0,0x17,0x17,0xa,0x17,0x45,0xa,0x6,0x6,0x10,0x17,0x17,0x0,0x1,0x49,0x45,0x15,0x9,0x0,0x4,0x16,0x0,0x45,0x11,0x17,0x1c,0x45,0x4,0x2,0x4,0xc,0xb]

private func liberalDemocratParty(video num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "imgUrl" :*/
fileprivate let k_giftName:String = "path let multiimgUrl"

/*: "videoUrl" :*/
fileprivate let k_endData:String = "user"
fileprivate let k_noMomentValue:String = "ideoUrlself stop"

/*: "Video cover submitted successfully" :*/
fileprivate let kStatusData:[UInt8] = [0xac,0x93,0x9e,0x9f,0x95,0xda,0x99,0x95,0x8c,0x9f,0x88,0xda,0x89,0x8f,0x98,0x97,0x93,0x8e,0x8e,0x9f,0x9e,0xda,0x89,0x8f,0x99,0x99,0x9f,0x89,0x89,0x9c,0x8f,0x96,0x96,0x83]

/*: "TypicalView" :*/
fileprivate let k_viewTitle:[Character] = ["T","a","l","k","i","n","g","E","d","i","t"]
fileprivate let kTextName:[Character] = ["V","i","d","e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let k_rowEqualData:String = "commentd"
fileprivate let k_viewValue:[Character] = ["e","o","_","u","r","l"]

/*: "img_url" :*/
fileprivate let kMoreText:[Character] = ["i","m","g","_","u","r","l"]

/*: "status" :*/
fileprivate let kMainValue:String = "status"
fileprivate let kEnableTitle:[Character] = ["t","a","t","u","s"]

/*: "ColorView" :*/
fileprivate let k_picText:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","P","h","o","t","o","C"]
fileprivate let k_cellName:String = "eadd"

/*: "SearchBirthdayCell" :*/
fileprivate let k_fillName:String = "content make nor interval constraintTalki"
fileprivate let k_collectionName:String = "itBicontainer if text view register"
fileprivate let k_thenValue:String = "voicevoice"

/*: "MediaReactiveCompatible" :*/
fileprivate let k_gestureName:String = "you false backTalki"
fileprivate let k_equalName:String = "itAboat height target equal"
fileprivate let kMakeText:[Character] = ["e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let k_matchInfoContent:[Character] = ["U","I","T","a","b"]
fileprivate let kSucceedTitle:[Character] = ["l","e","V","i","e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ScaleViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class ScaleViewController: RecordReactiveCompatible {
	var targetOn: Bool = true
	var viewBecomeQuantity: Double = -45.1
	var dismissArray: [AnyHashable] = []
	var awakeMaxQuantity: Int = 67

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [HarmonyEquatable] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [HarmonyEquatable] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [SelectedHandyJSON] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (kValueText.replacingOccurrences(of: "location", with: "l"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.textTag()
        //: self.title = "Edit profiles".localized
        self.title = (String(k_modelValue.prefix(3)) + String(kSizeResignData.suffix(6)) + "iles").localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(k_labText.suffix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(k_imageTitle))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(voice), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = RowReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = RowReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters = RowReactiveCompatible.share.loginUserMode.interest!
        seleteInters = RowReactiveCompatible.share.loginUserMode.interest!
        //: designView()
        panoramicView()
        //: GetGallery()
        punish()
        //: getVideoInfo()
        build()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(motile(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        ViewBeginReactiveCompatible.capability()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension ScaleViewController {
    //: func GetGallery() {
    func punish() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = RowReactiveCompatible.share.loginUserMode.userID
        dict[(k_countTheName.replacingOccurrences(of: "click", with: "d"))] = RowReactiveCompatible.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        DeformRequestTool.scope(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<SelectedHandyJSON>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [SelectedHandyJSON])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func statuteText() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(k_closeName) + String(k_qualityValue.prefix(2)) + String(k_blockValue.suffix(4)) + "ameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtFieldDelegate = i as! AtFieldDelegate
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(k_ofValue.suffix(6)) + "gEditS" + String(k_postTitle.prefix(7))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: UserReactiveCompatible = i as! UserReactiveCompatible
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&RowReactiveCompatible.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&RowReactiveCompatible.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && RowReactiveCompatible.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && RowReactiveCompatible.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && RowReactiveCompatible.share.loginUserMode.birthday != self.birthday) || (signStr != (kValueText.replacingOccurrences(of: "location", with: "l")) && RowReactiveCompatible.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = LibraryAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            CurrentThen.leftModel(message: (String(k_colorStyleValue) + String(k_logData.prefix(5))).localized, leftBtnTitle: (String(k_presentationName)).localized, rightBtnTitle: (String(k_situationTitle.suffix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                CurrentThen.meeting()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                CurrentThen.meeting()
                //: self.uploadTool(isBack: true)
                self.targetLoad(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func voice() {
        //: uploadTool(isBack: false)
        targetLoad(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func targetLoad(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(k_closeName) + String(k_qualityValue.prefix(2)) + String(k_blockValue.suffix(4)) + "ameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtFieldDelegate = i as! AtFieldDelegate
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(k_ofValue.suffix(6)) + "gEditS" + String(k_postTitle.prefix(7))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: UserReactiveCompatible = i as! UserReactiveCompatible
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && RowReactiveCompatible.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, RowReactiveCompatible.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(String(k_screenMakeName) + String(kTopData))] = nameStr
        }
        //: if birthday.count > 0 && RowReactiveCompatible.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, RowReactiveCompatible.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(kLikeContent.replacingOccurrences(of: "video", with: "t") + "hday")] = birthday
        }
        //: if signStr != "nil" && RowReactiveCompatible.share.loginUserMode.signature != signStr {
        if signStr != (kValueText.replacingOccurrences(of: "location", with: "l")), RowReactiveCompatible.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(k_mValue.replacingOccurrences(of: "info", with: "s"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        digitizerFrom(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && RowReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.female.rawValue {
            //: uploadVideo()
            executeVideo()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        DeformRequestTool.completionColor(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            ViewBeginReactiveCompatible.capability()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.viewLog(showMsg: (String(kOriginText.suffix(5)) + "y the " + kEqualViewName.lowercased()).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: RowReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                    RowReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: RowReactiveCompatible.share.loginUserMode.birthday = self.birthday
                    RowReactiveCompatible.share.loginUserMode.birthday = self.birthday
                }
                //: RowReactiveCompatible.share.loginUserMode.signature = self.signStr
                RowReactiveCompatible.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.appearShow(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func digitizerFrom(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            photoShow()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                date()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            valueType(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            valueType(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func date() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].compressed()! as NSData
            //: dict["pic"] = resultData
            dict[(k_observerTitle.replacingOccurrences(of: "size", with: "p"))] = resultData
            //: ProgressHUD.show()
            ViewBeginReactiveCompatible.deviseShow()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            DeformRequestTool.bottomTotal(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.viewLog(showMsg: (String(kOriginText.suffix(5)) + "y the " + kEqualViewName.lowercased()).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.punish()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func photoShow() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(String(kAddData))] = str
        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        DeformRequestTool.conversationMessageVelleity(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.date()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.viewLog(showMsg: (String(kOriginText.suffix(5)) + "y the " + kEqualViewName.lowercased()).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.punish()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func valueType(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [HarmonyEquatable] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (k_managerListTitle.replacingOccurrences(of: "title", with: "u") + String(k_toName))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (kFillData.replacingOccurrences(of: "user", with: "st"))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(kWithName))] = str
        //: dict["category"] = category
        dict[(kLabData.replacingOccurrences(of: "image", with: "g"))] = category
        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        DeformRequestTool.local(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            ViewBeginReactiveCompatible.capability()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.viewLog(showMsg: (String(kOriginText.suffix(5)) + "y the " + kEqualViewName.lowercased()).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: RowReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    RowReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: RowReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                    RowReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func executeVideo() {
        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        IndexSignatureProvider.shared.uploadThroughCompletion(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.appearShow(showMsg: String(bytes: kErrorValue.map{liberalDemocratParty(video: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(k_giftName.suffix(6)))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(k_endData.replacingOccurrences(of: "user", with: "v") + String(k_noMomentValue.prefix(7)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            DeformRequestTool.paramsCardUp(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.appearShow(showMsg: String(bytes: kErrorValue.map{liberalDemocratParty(video: $0)}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.viewLog(showMsg: String(bytes: kStatusData.map{$0^250}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(k_viewTitle) + String(kTextName)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: TypicalView = i as! TypicalView
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.nearService(videoPlayPath: dict[(k_endData.replacingOccurrences(of: "user", with: "v") + String(k_noMomentValue.prefix(7)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.setImageSuperiorLandStatus(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func build() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        DeformRequestTool.equalEnable { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(k_rowEqualData.replacingOccurrences(of: "comment", with: "vi") + String(k_viewValue))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(kMoreText))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(kMainValue.replacingOccurrences(of: "status", with: "s") + String(kEnableTitle))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension ScaleViewController {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func motile(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(k_closeName) + String(k_qualityValue.prefix(2)) + String(k_blockValue.suffix(4)) + "ameCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AtFieldDelegate = i as! AtFieldDelegate
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(k_ofValue.suffix(6)) + "gEditS" + String(k_postTitle.prefix(7))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: UserReactiveCompatible = i as! UserReactiveCompatible
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension ScaleViewController: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_errData - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.tapMain(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.tapMain(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(k_viewTitle) + String(kTextName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TypicalView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = TypicalView(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.setImageSuperiorLandStatus(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.colorInstal(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.nearService(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(k_picText) + k_cellName.replacingOccurrences(of: "add", with: "ll"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ColorView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = ColorView(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.mosaic(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.errorPhoto()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(k_closeName) + String(k_qualityValue.prefix(2)) + String(k_blockValue.suffix(4)) + "ameCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AtFieldDelegate
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = AtFieldDelegate(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(k_fillName.suffix(5)) + "ngEd" + String(k_collectionName.prefix(4)) + "rthdayCe" + k_thenValue.replacingOccurrences(of: "voice", with: "l"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? SearchBirthdayCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = SearchBirthdayCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(k_ofValue.suffix(6)) + "gEditS" + String(k_postTitle.prefix(7)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? UserReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = UserReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(k_gestureName.suffix(5)) + "ngEd" + String(k_equalName.prefix(5)) + "utMeC" + String(kMakeText))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? MediaReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = MediaReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.addStatuteName()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.publish(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.publish(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = PlayViewDelegate()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.large(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.large(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.large(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.large(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(k_matchInfoContent) + String(kSucceedTitle))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func tapMain(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [HarmonyEquatable]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.ofAndSize(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_errData - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - DelayEventThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension ScaleViewController: DelayEventThen {
    //: func freshSeleteTag() {
    func digitiser() {
        //: seleteAboutme =  RowReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = RowReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters =  RowReactiveCompatible.share.loginUserMode.interest!
        seleteInters = RowReactiveCompatible.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - ShadowgraphThen

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension ScaleViewController: ShadowgraphThen {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func toPlay(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = SelectedHandyJSON()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func phoneWithoutVignettePhoto(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func mediumBar(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension ScaleViewController {
    //: private func designView() {
    private func panoramicView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(TypicalView.self, forCellReuseIdentifier: (String(k_viewTitle) + String(kTextName)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(ColorView.self, forCellReuseIdentifier: (String(k_picText) + k_cellName.replacingOccurrences(of: "add", with: "ll")))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(AtFieldDelegate.self, forCellReuseIdentifier: (String(k_closeName) + String(k_qualityValue.prefix(2)) + String(k_blockValue.suffix(4)) + "ameCell"))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(SearchBirthdayCell.self, forCellReuseIdentifier: (String(k_fillName.suffix(5)) + "ngEd" + String(k_collectionName.prefix(4)) + "rthdayCe" + k_thenValue.replacingOccurrences(of: "voice", with: "l")))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(UserReactiveCompatible.self, forCellReuseIdentifier: (String(k_ofValue.suffix(6)) + "gEditS" + String(k_postTitle.prefix(7))))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(MediaReactiveCompatible.self, forCellReuseIdentifier: (String(k_gestureName.suffix(5)) + "ngEd" + String(k_equalName.prefix(5)) + "utMeC" + String(kMakeText)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
