
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kColorObserveText:[UInt8] = [0x6,0xb,0x6,0x11,0xc5,0x0,0xc,0x1,0x2,0xf,0xd7,0xc6,0xbd,0x5,0xfe,0x10,0xbd,0xb,0xc,0x11,0xbd,0xff,0x2,0x2,0xb,0xbd,0x6,0xa,0xd,0x9,0x2,0xa,0x2,0xb,0x11,0x2,0x1]

fileprivate func femaleShow(name num: UInt8) -> UInt8 {
    let value = Int(num) - 157
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Text greeting" :*/
fileprivate let kSexText:[Character] = ["T","e","x","t"," ","g"]
fileprivate let kPhotoValue:String = "reetiuser"

/*: "Add" :*/
fileprivate let k_placeLogValue:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let kPlayValue:String = "tymediume"

/*: "addStatus" :*/
fileprivate let kOldName:[Character] = ["a","d","d","S","t","a"]
fileprivate let kSucceedFirstName:[Character] = ["t","u","s"]

/*: "list" :*/
fileprivate let kCurrentName:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let kLayerValue:[UInt8] = [0xfc,0xcf,0xd8,0x9d,0xc4,0xd2,0xc8,0x9d,0xce,0xc8,0xcf,0xd8,0x9d,0xc9,0xd2,0x9d,0xd9,0xd8,0xd1,0xd8,0xc9,0xd8,0x9d,0xc9,0xd5,0xd4,0xce,0x9d,0xd0,0xd8,0xce,0xce,0xdc,0xda,0xd8,0x52,0x1,0x22]

private func bottomResume(content num: UInt8) -> UInt8 {
    return num ^ 189
}

/*: "Cancel" :*/
fileprivate let kPopTitle:String = "text"
fileprivate let kCustomTitle:String = "streamcel"

/*: "Delete" :*/
fileprivate let kArrayValue:String = "Deletelet cell effect"

/*: "id" :*/
fileprivate let k_sureTitle:String = "iview"

/*: "The upper limit has been reached" :*/
fileprivate let k_toolValue:[UInt8] = [0xaf,0x93,0x9e,0xdb,0x8e,0x8b,0x8b,0x9e,0x89,0xdb,0x97,0x92,0x96,0x92,0x8f,0xdb,0x93,0x9a,0x88,0xdb,0x99,0x9e,0x9e,0x95,0xdb,0x89,0x9e,0x9a,0x98,0x93,0x9e,0x9f]

/*: "text" :*/
fileprivate let k_maxTitle:String = "TEXT"

/*: "Successfully added, please wait for approval" :*/
fileprivate let k_localData:[UInt8] = [0x6c,0x61,0x76,0x6f,0x72,0x70,0x70,0x61,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x64,0x64,0x61,0x20,0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53]

/*: "Any-Hex/Java" :*/
fileprivate let k_taskValue:[Character] = ["A","n","y"]
fileprivate let kOfTitle:String = "-Hexsource family"

/*: "CheckThen" :*/
fileprivate let k_textContent:String = "Talkisucceed make select self input"
fileprivate let k_cardTitle:[Character] = ["t","T","e","x"]
fileprivate let kBottomName:[Character] = ["t","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let k_makeContent:String = "cancel"
fileprivate let k_rowData:[Character] = ["F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let kNumberName:[Character] = ["b","t","n","_","m"]
fileprivate let k_userFrameValue:[Character] = ["e","s","s","a","g"]
fileprivate let k_hiddenLeadingValue:String = "e_delenormal live if leading"

/*: "Click the button below to add\ncontent" :*/
fileprivate let kGroupTitle:[UInt8] = [0xe0,0xcf,0xca,0xc0,0xc8,0x83,0xd7,0xcb,0xc6,0x83,0xc1,0xd6,0xd7,0xd7,0xcc,0xcd,0x83,0xc1,0xc6,0xcf,0xcc,0xd4,0x83,0xd7,0xcc,0x83,0xc2,0xc7,0xc7,0xa9,0xc0,0xcc,0xcd,0xd7,0xc6,0xcd,0xd7]

private func actionDrop(from num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let kParaText:[UInt8] = [0xb6,0x89,0x84,0x8f,0xc1,0x80,0x85,0x85,0x88,0x8f,0x86,0xc1,0x8c,0x94,0x8d,0x95,0x88,0x91,0x8d,0x84,0xc1,0x95,0x84,0x99,0x95,0xc1,0x8c,0x84,0x92,0x92,0x80,0x86,0x84,0xcd,0xc1,0x95,0x89,0x84,0xc1,0x92,0x98,0x92,0x95,0xc1,0x84,0x8c,0xc1,0x96,0x88,0x8d,0x8d,0xc1,0x92,0x84,0x8f,0x85,0xc1,0x95,0x89,0x84,0x8c,0xc1,0x83,0x98,0xc1,0x95,0x94,0x93,0x8f,0x92,0xda,0x92,0x8d,0x88,0x85,0x84,0xc1,0x95,0x8e,0xc1,0x95,0x89,0x84,0xc1,0x8c,0x84,0x92,0x92,0x80,0x86,0x84,0xc1,0x95,0x8e,0xc1,0x85,0x84,0x8d,0x84,0x95,0x84]

private func kindAge(after num: UInt8) -> UInt8 {
    return num ^ 225
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WorldwideReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class WorldwideReactiveCompatible: RecordReactiveCompatible {
	var showOn: Bool = false
	var addCount: Double = -89.9
	var detailArray: [AnyHashable] = []
	var putDictionary: [AnyHashable: String] = [:]

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: DetailThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kColorObserveText.map{femaleShow(name: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(kSexText) + kPhotoValue.replacingOccurrences(of: "user", with: "ng")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.textTag()
        //: designView()
        orientation()
        //: getAudioListData()
        life()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [SimulationTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [SimulationTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(k_placeLogValue)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.appSize(colors: UIColor.placeColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(snap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension WorldwideReactiveCompatible {
    //: func getAudioListData() {
    func life() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(kPlayValue.replacingOccurrences(of: "medium", with: "p"))] = 2
        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        DeformRequestTool.holdfast(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            ViewBeginReactiveCompatible.capability()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.viewActivity()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(kOldName) + String(kSucceedFirstName))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(kCurrentName))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [SimulationTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<SimulationTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func fromEqualMarginOfError(index: Int) {
        //: let config = ShowAlertConfig()
        let config = LibraryAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.ofAndSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        CurrentThen.leftModel(message: String(bytes: kLayerValue.map{bottomResume(content: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (kPopTitle.replacingOccurrences(of: "text", with: "C") + kCustomTitle.replacingOccurrences(of: "stream", with: "an")).localized, rightBtnTitle: (String(kArrayValue.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            CurrentThen.meeting()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            CurrentThen.meeting()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: SimulationTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            ViewBeginReactiveCompatible.deviseShow()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            DeformRequestTool.quick(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.life()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension WorldwideReactiveCompatible {
    //: @objc func addButtonClick() {
    @objc func snap() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.appearShow(showMsg: String(bytes: k_toolValue.map{$0^251}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = DetailThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.toRun()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.equalToSnaplineContent(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(kPlayValue.replacingOccurrences(of: "medium", with: "p"))] = 2
            //: dict["text"] = tempStr
            dict[(k_maxTitle.lowercased())] = tempStr
            //: ProgressHUD.show()
            ViewBeginReactiveCompatible.deviseShow()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            DeformRequestTool.pathColor(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.viewLog(showMsg: String(bytes: k_localData.reversed(), encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.life()
                //: self.recordView?.dismiss()
                self.recordView?.system()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func equalToSnaplineContent(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(k_taskValue) + String(kOfTitle.prefix(4)) + "/Java") as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension WorldwideReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kLet_errData - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.ofAndSize(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(k_textContent.prefix(5)) + "ngGree" + String(k_cardTitle) + String(kBottomName))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: CheckThen = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! CheckThen

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.black(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(kArrayValue.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.fromEqualMarginOfError(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (k_makeContent.replacingOccurrences(of: "cancel", with: "#") + String(k_rowData)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.fromEqualMarginOfError(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.collectionName(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.collectionName(name: (String(kNumberName) + String(k_userFrameValue) + String(k_hiddenLeadingValue.prefix(6)) + "te_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = MakeWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension WorldwideReactiveCompatible {
    //: private func setupEmptyDataView() {
    private func viewActivity() {
        //: var style = EmptyStyle()
        var style = SymbolVideoEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: kGroupTitle.map{actionDrop(from: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .textTag()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func orientation() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(CheckThen.self, forCellReuseIdentifier: (String(k_textContent.prefix(5)) + "ngGree" + String(k_cardTitle) + String(kBottomName)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.side()
        //: label.font = UIFont.font(fontSize: 15)
        label.font = UIFont.font(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: kParaText.map{kindAge(after: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
