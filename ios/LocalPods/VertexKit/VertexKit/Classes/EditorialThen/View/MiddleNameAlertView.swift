
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_imageTopValue:[UInt8] = [0xb1,0xb6,0xb1,0xac,0xf0,0xbb,0xb7,0xbc,0xbd,0xaa,0xe2,0xf1,0xf8,0xb0,0xb9,0xab,0xf8,0xb6,0xb7,0xac,0xf8,0xba,0xbd,0xbd,0xb6,0xf8,0xb1,0xb5,0xa8,0xb4,0xbd,0xb5,0xbd,0xb6,0xac,0xbd,0xbc]

private func starUser(index num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "Cancel" :*/
fileprivate let kFeedValue:String = "Cancelresume equal"

/*: "Submit" :*/
fileprivate let kBigToolData:String = "frame infoSubmit"

/*: "Please tell us your problems                   " :*/
fileprivate let k_noTitleValue:[UInt8] = [0xc9,0xf5,0xfc,0xf8,0xea,0xfc,0xb9,0xed,0xfc,0xf5,0xf5,0xb9,0xec,0xea,0xb9,0xe0,0xf6,0xec,0xeb,0xb9,0xe9,0xeb,0xf6,0xfb,0xf5,0xfc,0xf4,0xea,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9,0xb9]

/*: "You need to choose at least one." :*/
fileprivate let k_cellValue:[UInt8] = [0xe9,0xdf,0xc5,0x90,0xde,0xd5,0xd5,0xd4,0x90,0xc4,0xdf,0x90,0xd3,0xd8,0xdf,0xdf,0xc3,0xd5,0x90,0xd1,0xc4,0x90,0xdc,0xd5,0xd1,0xc3,0xc4,0x90,0xdf,0xde,0xd5,0x9e]

private func viewTransform(index num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "Please tell us your problems." :*/
fileprivate let k_meActionData:String = "Pleasetext succeed hidden"
fileprivate let kLoadTransformValue:String = " us yopath meeting model"
fileprivate let kManagerMakeData:[Character] = ["l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let kStatusContent:String = "app/image container"
fileprivate let kPushName:String = "Reportcut view"

/*: "id" :*/
fileprivate let kUserData:[UInt8] = [0x38,0x35]

private func statisticalTable(line num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "reason" :*/
fileprivate let kLeadingText:[Character] = ["r","e","a","s","o","n"]

/*: "desc" :*/
fileprivate let kViewMiniValue:[UInt8] = [0x70,0x71,0x7f,0x6f]

fileprivate func backgroundGift(intimate num: UInt8) -> UInt8 {
    let value = Int(num) - 12
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let kPathText:[Character] = ["m","f","/"]
fileprivate let k_sourceData:[Character] = ["m","o","m","e","n","t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let kBottomTitle:[UInt8] = [0xf1,0xf3,0xf1,0xe9,0xf2,0xf8,0xcd,0xe8]

fileprivate func layerBubble(current num: UInt8) -> UInt8 {
    let value = Int(num) + 124
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/momentReply/report" :*/
fileprivate let k_lineName:String = "mf/moself view let count"
fileprivate let kTextName:String = "view list model videoply/"
fileprivate let k_makeText:[Character] = ["r","t"]

/*: "app/videoReport" :*/
fileprivate let k_keyText:[Character] = ["a","p","p","/","v","i","d"]
fileprivate let kTheCookieName:String = "video on leteoReport"

/*: "logId" :*/
fileprivate let k_showValue:[UInt8] = [0xe9,0xea,0xe2,0xcc,0xe1]

private func pageCenter(color num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "Submitted successfully" :*/
fileprivate let k_pathValue:String = "title maskSubm"
fileprivate let kRequestName:String = "udataes"

/*: "Nudity" :*/
fileprivate let k_recordingData:String = "Nuditysince to"

/*: "Violence" :*/
fileprivate let k_sizeData:String = "layer err bottom tempViolenc"
fileprivate let k_colorValue:[Character] = ["e"]

/*: "Harassment" :*/
fileprivate let kFatalTitle:[Character] = ["H","a","r","a","s","s","m"]
fileprivate let k_halfData:[Character] = ["e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let k_managerModeName:[Character] = ["S","u","i","c","i","d","e"]
fileprivate let kToolValue:String = " or Selftype log match"
fileprivate let kInputOpViewValue:[Character] = ["-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let kRandomData:[Character] = ["F","a","l","s","e"," ","I","n","f","o","r"]
fileprivate let k_withValue:[Character] = ["m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let k_textValue:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let k_lastName:[Character] = ["H","a","t","e"]
fileprivate let k_imageData:String = " Speechcase corner index"

/*: "Terrorism" :*/
fileprivate let kConversationName:String = "line selfTe"
fileprivate let k_enableName:[Character] = ["r","r","o","r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let k_bottomValue:[UInt8] = [0x1a,0x28,0x33,0x2c,0xe7,0x36,0x2d,0xe7,0x10,0x33,0x33,0x2c,0x2e,0x28,0x33,0xe7,0x36,0x39,0xe7,0x19,0x2c,0x2e,0x3c,0x33,0x28,0x3b,0x2c,0x2b,0xe7,0xe,0x36,0x36,0x2b,0x3a]

fileprivate func nameWith(add num: UInt8) -> UInt8 {
    let value = Int(num) + 57
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let kImageYearName:String = "Sendindata self type item"
fileprivate let kMoreValue:String = "GUST"
fileprivate let k_infoData:[Character] = ["i","n","g"," ","m"]
fileprivate let kFrameTitle:String = "eviewages"

/*: "Using photos of nudity or drugs" :*/
fileprivate let k_itemData:[UInt8] = [0x4e,0x6c,0x62,0x67,0x60,0x19,0x69,0x61,0x68,0x6d,0x68,0x6c,0x19,0x68,0x5f,0x19,0x67,0x6e,0x5d,0x62,0x6d,0x72,0x19,0x68,0x6b,0x19,0x5d,0x6b,0x6e,0x60,0x6c]

fileprivate func iconSucceed(text num: UInt8) -> UInt8 {
    let value = Int(num) + 7
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Harassing or sending ads" :*/
fileprivate let kTopName:String = "identity var shadow attribute typeHara"
fileprivate let k_angleTitle:String = "written view user or "
fileprivate let kLiveValue:String = "wills"

/*: "Defraud or using fake info" :*/
fileprivate let kModeValue:[Character] = ["D","e","f","r","a","u","d"]
fileprivate let k_controlSucceedTitle:[Character] = [" ","o","r"]
fileprivate let kShareRefreshTitle:[Character] = [" ","u","s","i","n","g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let kEqualTitle:String = "top"
fileprivate let kFileMValue:String = "THERS"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiddleNameAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum BagCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class MiddleNameAlertView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: BagCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [VideoReportModel] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = VideoReportModel()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: BagCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        strip()
        //: setupSubViewsConstraint()
        startConstraint()
        //: setupReasons()
        observerIn()
        //: bindInteraction()
        pull()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_imageTopValue.map{starUser(index: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(kFeedValue.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.cellColor(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.cellColor().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(sharedTime), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(kBigToolData.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.appSize(colors: UIColor.placeColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(scaleValue), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kLet_errData - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.viewSearch(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = BeginReactiveCompatible(placeholder: String(bytes: k_noTitleValue.map{$0^153}, encoding: .utf8)!.localized, placeholderColor: UIColor.side(), frame: CGRect(x: 10, y: 10, width: kLet_errData - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .ofAndSize(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.dropMultiView()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .ofAndSize(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.viewSearch(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(VideoViewCell.self, forCellReuseIdentifier: VideoViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension MiddleNameAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func sizeBar() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func bridge(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = NameMacroDefine.getWindow()
            let window = NameMacroDefine.playWindow()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func succeed(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func sharedTime() {
        //: self.dismissReportView(succeed: false)
        self.succeed(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func scaleValue() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.appearShow(showMsg: String(bytes: k_cellValue.map{viewTransform(index: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.appearShow(showMsg: (String(k_meActionData.prefix(6)) + " tell" + String(kLoadTransformValue.prefix(6)) + "ur prob" + String(kManagerMakeData)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(kStatusContent.prefix(4)) + "user" + String(kPushName.prefix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (String(kLeadingText)): selectedModel.type, String(bytes: kViewMiniValue.map{backgroundGift(intimate: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(kPathText) + String(k_sourceData))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: kBottomTitle.map{layerBubble(current: $0)}, encoding: .utf8)!: tempId, (String(kLeadingText)): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(k_lineName.prefix(5)) + "mentRe" + String(kTextName.suffix(4)) + "repo" + String(k_makeText))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (String(kLeadingText)): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(k_keyText) + String(kTheCookieName.suffix(8)))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: k_showValue.map{pageCenter(color: $0)}, encoding: .utf8)!: tempId, (String(kLeadingText)): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        BagThen.statuteTitle(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.viewLog(showMsg: (String(k_pathValue.suffix(4)) + "itted s" + kRequestName.replacingOccurrences(of: "data", with: "cc") + "sfully").localized)
                //: self.dismissReportView(succeed: true)
                self.succeed(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension MiddleNameAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: VideoViewCell.className()) as! VideoViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.modelSearch(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension MiddleNameAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func strip() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func startConstraint() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func observerIn() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(k_recordingData.prefix(6))).localized,
                           //: "Violence".localized,
                           (String(k_sizeData.suffix(7)) + String(k_colorValue)).localized,
                           //: "Harassment".localized,
                           (String(kFatalTitle) + String(k_halfData)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(k_managerModeName) + String(kToolValue.prefix(8)) + String(kInputOpViewValue)).localized,
                           //: "False Information".localized,
                           (String(kRandomData) + String(k_withValue)).localized,
                           //: "Spam".localized,
                           (String(k_textValue)).localized,
                           //: "Hate Speech".localized,
                           (String(k_lastName) + String(k_imageData.prefix(7))).localized,
                           //: "Terrorism".localized,
                           (String(kConversationName.suffix(2)) + String(k_enableName)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: k_bottomValue.map{nameWith(add: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(kImageYearName.prefix(6)) + "g dis" + kMoreValue.lowercased() + String(k_infoData) + kFrameTitle.replacingOccurrences(of: "view", with: "ss")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: k_itemData.map{iconSucceed(text: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(kTopName.suffix(4)) + "ssing" + String(k_angleTitle.suffix(4)) + "sending a" + kLiveValue.replacingOccurrences(of: "will", with: "d")).localized,
                           //: "Defraud or using fake info".localized,
                           (String(kModeValue) + String(k_controlSucceedTitle) + String(kShareRefreshTitle)).localized,
                           //: "Others".localized]
                           (kEqualTitle.replacingOccurrences(of: "top", with: "O") + kFileMValue.lowercased()).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = RowReactiveCompatible.share.appConfigMode.videoReport["reason"] {
            if let reason = RowReactiveCompatible.share.appConfigMode.videoReport[(String(kLeadingText))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = VideoReportModel()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func pull() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
