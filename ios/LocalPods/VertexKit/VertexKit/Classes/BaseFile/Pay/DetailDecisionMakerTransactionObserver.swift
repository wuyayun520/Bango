
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let k_femaleData:[UInt8] = [0x7b,0x6a,0x6a,0x76,0x7f,0x65,0x73,0x7b,0x6a,0x65,0x6a,0x7b,0x63,0x65,0x69,0x6f,0x79,0x79,0x7f,0x7f,0x7e,0x65,0x74,0x75,0x6e,0x73,0x7c,0x73,0x79,0x7b,0x6e,0x73,0x75,0x74]

private func userMin(model num: UInt8) -> UInt8 {
    return num ^ 58
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let k_levelName:[UInt8] = [0xc1,0xd0,0xd0,0xcc,0xc5,0xdf,0xc9,0xc1,0xd0,0xdf,0xd3,0xd5,0xc2,0xd3,0xc3,0xd2,0xc9,0xc2,0xc5,0xdf,0xd3,0xd5,0xc3,0xc3,0xc5,0xc5,0xc4,0xdf,0xce,0xcf,0xd4,0xc9,0xc6,0xc9,0xc3,0xc1,0xd4,0xc9,0xcf,0xce]

private func numbereractionManagerRange(bottom num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let kEndAspectData:String = "mf/remy progress guard share"
fileprivate let k_modelValue:String = "ge/createsize view path video send"
fileprivate let kGroupOfTitle:String = "Appcurrent make selected"
fileprivate let k_addManagerValue:String = "lePayreturn will in actual"

/*: "productId" :*/
fileprivate let kImageName:String = "ppending"
fileprivate let kServerValue:String = "oductIdcollection push progress false true"

/*: "source" :*/
fileprivate let k_dataValue:String = "sconcernrce"

/*: "IAP充值创建订单失败: :*/
fileprivate let k_countData:[Character] = ["I","A","P","充","\u{503c}","创","建","订","单","失"]
fileprivate let kRawInsideData:String = "label:"

/*: , 参数: :*/
fileprivate let k_nameLiveValue:[Character] = [","," ","参","数",":"]

/*: ." :*/
fileprivate let kGiftName:[Character] = ["."]

/*: "orderNum" :*/
fileprivate let kUnknownText:String = "if mode table manager giftorderNum"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let k_whiteText:String = "jump to height on resultmf/r"
fileprivate let kAddTitle:String = "path hidden list var colorrge/ap"
fileprivate let k_centerText:String = "self value left block pointNoti"
fileprivate let k_containerWindowLayerTitle:[Character] = ["f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let kStatusIndexValue:[Character] = ["I","A","P","充","值","校"]
fileprivate let k_sizeName:String = "验\u{5931}败:"

/*: "transactionId" :*/
fileprivate let kGiftValue:String = "trlistn"
fileprivate let kFrameText:String = "onIdself if class button"

/*: "reportMoney" :*/
fileprivate let kCountTitle:[Character] = ["r","e","p","o","r","t","M","o","n"]
fileprivate let k_viewListenTitle:String = "errory"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let k_useValue:[Character] = ["m","f","/","A","u","t","o","S"]
fileprivate let kStyleAppearName:String = "ub/Apreturn manager make"
fileprivate let k_makeValue:String = "view managerateO"

/*: "IAP订阅创建订单失败: :*/
fileprivate let k_showTitle:String = "IAP订阅创app height transform layer"
fileprivate let kLeadingValue:String = "建订单失\u{8d25}:"

/*: "orderId" :*/
fileprivate let k_onData:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x72,0x6f]

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let kToContent:String = "mf/Auother text"
fileprivate let kRangeData:String = "name count limitb/Ap"
fileprivate let kReportName:String = "Successview define"

/*: "IAP订阅校验失败: :*/
fileprivate let kModeName:String = "wrap succeed count layerIAP订阅校验"
fileprivate let k_unknownText:[Character] = ["失","败",":"]

/*: "App" :*/
fileprivate let k_actData:[Character] = ["A","p","p"]

/*: "OrderTransactionInfo_ :*/
fileprivate let k_eventData:String = "OrderTif label let break return"
fileprivate let kPathCellData:String = "nInfo_event view let"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let kColorData:[UInt8] = [0xff,0xc2,0xd4,0xd5,0xc2,0xe4,0xc2,0xd1,0xde,0xc3,0xd1,0xd3,0xc4,0xd9,0xdf,0xde,0xf9,0xde,0xd6,0xdf,0xef,0xe3,0xc5,0xd2,0xc3,0xd3,0xc2,0xd9,0xd2,0xd5,0xef]

/*: "verifyData" :*/
fileprivate let k_appName:[UInt8] = [0xd6,0xc5,0xd2,0xc9,0xc6,0xd9,0xe4,0xc1,0xd4,0xc1]

private func lineImage(view num: UInt8) -> UInt8 {
    return num ^ 160
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let kInfoText:[Character] = ["I","A","P","查","\u{8be2}","内","\u{8d2d}"]
fileprivate let kWindowValue:String = "\u{5546}\u{54c1}失败,"
fileprivate let k_withIconName:String = "list attention bottomductId:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let kAssetValue:String = "IAP\u{67e5}询"
fileprivate let k_followTitle:String = "失败, "
fileprivate let k_atTitle:String = "prmodeuc"
fileprivate let k_networkText:String = "unt\u{4e3a}0."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let kLabTitle:[Character] = ["I","A","P","\u{67e5}","\u{8be2}","\u{5185}","购","\u{5546}","品","失","败",","," ","E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let kContentValue:[Character] = ["I","A","P","-","p","u","r","c"]
fileprivate let kEffectName:String = "hased:info live self task send"

/*: , 订单号: :*/
fileprivate let kRegularData:String = ", 订单号:"

/*: , 商品Id: :*/
fileprivate let kCollectionValue:[Character] = [","," ","商","品","I","d",":"]

/*: , 苹果transId: :*/
fileprivate let k_viewName:[Character] = [","," ","苹","\u{679c}","t","r","a","n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let k_toText:[Character] = ["I","A","P","-","f"]
fileprivate let k_valueTitle:String = "ailed:user color"

/*: "IAP-deferred: :*/
fileprivate let k_noContent:[Character] = ["I","A","P","-","d","e","f","e"]
fileprivate let kCellArrayName:String = "as let price grouprred:"

/*: "IAP未知交易类型: :*/
fileprivate let k_pathToData:String = "IAP\u{672a}"

/*: " 未知的交易类型" :*/
fileprivate let k_statusTitle:[Character] = [""," ","\u{672a}","知","\u{7684}","交","易","类","型"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let k_modeSucceedContent:[Character] = ["I","A","P","\u{83b7}","取","\u{672c}","地","收","据","数","据","\u{5931}","\u{8d25}",","," ","t","r","a","n","s","a","c","t","i","o","n","I","d"]
fileprivate let kMakeBetweenContent:[Character] = [":"]

/*: , orderId: :*/
fileprivate let k_workContent:String = ", orframe current name live network"
fileprivate let k_userData:String = "collection bottom burnd:"

/*: , payType: :*/
fileprivate let kTotalimateData:[Character] = [","," ","p"]
fileprivate let k_saveSecondValue:[Character] = ["a","y","T","y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DetailDecisionMakerTransactionObserver.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kLet_buttonTitle = NSNotification.Name(rawValue: String(bytes: k_femaleData.map{userMin(model: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let kLet_domainValue = NSNotification.Name(rawValue: String(bytes: k_levelName.map{numbereractionManagerRange(bottom: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kLet_askName = 9

/// 支付类型
//: enum ApplePayType {
enum ServiceType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum GraduatedTableKeyRepresentable: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double) -> Void
typealias IAPcompletionHandle = (GraduatedTableKeyRepresentable, Double) -> Void

//: class AppleIAPManager: NSObject {
class DetailDecisionMakerTransactionObserver: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: ServiceType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = DetailDecisionMakerTransactionObserver()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(instrumentation),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func instrumentation() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension DetailDecisionMakerTransactionObserver {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func sendForPlace(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(kEndAspectData.prefix(5)) + "char" + String(k_modelValue.prefix(9)) + String(kGroupOfTitle.prefix(3)) + String(k_addManagerValue.prefix(5)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(kImageName.replacingOccurrences(of: "pending", with: "r") + String(kServerValue.prefix(7)))] = productId
        //: dict["source"] = source
        dict[(k_dataValue.replacingOccurrences(of: "concern", with: "ou"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: JumpLogTool.lineLoad(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                JumpLogTool.lineLoad(msg: (String(k_countData) + kRawInsideData.replacingOccurrences(of: "label", with: "败")) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(kUnknownText.suffix(8)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func drogue(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(k_whiteText.suffix(4)) + "echa" + String(kAddTitle.suffix(6)) + "plePay" + String(k_centerText.suffix(4)) + String(k_containerWindowLayerTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: JumpLogTool.lineLoad(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    JumpLogTool.lineLoad(msg: (String(kStatusIndexValue) + k_sizeName) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.writeBy(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.patronymic()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_buttonTitle, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(kCountTitle) + k_viewListenTitle.replacingOccurrences(of: "error", with: "e"))].doubleValue)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension DetailDecisionMakerTransactionObserver {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func tradeNameHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(k_useValue) + String(kStyleAppearName.prefix(5)) + "pleCre" + String(k_makeValue.suffix(4)) + "rder")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(kImageName.replacingOccurrences(of: "pending", with: "r") + String(kServerValue.prefix(7)))] = productId
        //: dict["source"] = source
        dict[(k_dataValue.replacingOccurrences(of: "concern", with: "ou"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: JumpLogTool.lineLoad(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                JumpLogTool.lineLoad(msg: (String(k_showTitle.prefix(6)) + kLeadingValue) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: k_onData.reversed(), encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func equalListenerParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(kToContent.prefix(5)) + "toSu" + String(kRangeData.suffix(4)) + "plePay" + String(kReportName.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: JumpLogTool.lineLoad(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    JumpLogTool.lineLoad(msg: (String(kModeName.suffix(7)) + String(k_unknownText)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.writeBy(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.roundModel()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_domainValue, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(kCountTitle) + k_viewListenTitle.replacingOccurrences(of: "error", with: "e"))].doubleValue)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension DetailDecisionMakerTransactionObserver {
    /// 初始化数据
    //: private func iap_initData() {
    private func corium() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = objectPost(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = objectPost(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func objectPost(payType: ServiceType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = patronymic()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = roundModel()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func patronymic() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(k_actData)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_tableRouteValue.string(forKey: kLet_sessionData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(k_eventData.prefix(6)) + "ransactio" + String(kPathCellData.prefix(6))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func roundModel() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(k_actData)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_tableRouteValue.string(forKey: kLet_sessionData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: kColorData.map{$0^176}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func sendState(_ transactionId: String, _ payType: ServiceType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: k_appName.map{lineImage(view: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: k_appName.map{lineImage(view: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension DetailDecisionMakerTransactionObserver {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func examineOf() {
        //: iap_initData()
        corium()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            willImageRandom(dict[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            willImageRandom(dict[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func willImageRandom(_ transactionId: String?, _ payType: ServiceType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        writeBy(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension DetailDecisionMakerTransactionObserver {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func softwareProductStartSourcePickPurchaseTypeHandle(productId: String, payType: ServiceType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        corium()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            sendForPlace(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.actionToInfo(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            tradeNameHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.actionToInfo(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func actionToInfo(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: JumpLogTool.lineLoad(msg: "IAP查询内购商品失败, productId:\(productId).")
            JumpLogTool.lineLoad(msg: (String(kInfoText) + kWindowValue + " pro" + String(k_withIconName.suffix(7))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0)
            self.completionHandle?(.notArrow, 0)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.yearRequest()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func yearRequest() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension DetailDecisionMakerTransactionObserver: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: JumpLogTool.lineLoad(msg: "IAP查询内购商品失败, products.count为0.")
            JumpLogTool.lineLoad(msg: (kAssetValue + "内购商\u{54c1}" + k_followTitle.capitalized + k_atTitle.replacingOccurrences(of: "mode", with: "od") + "ts.co" + k_networkText))
            //: self.completionHandle?( .noProductId, 0)
            self.completionHandle?(.noProductId, 0)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: JumpLogTool.lineLoad(msg: "IAP查询内购商品失败, Error:\(error).")
        JumpLogTool.lineLoad(msg: (String(kLabTitle)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0)
        self.completionHandle?(.noProductId, 0)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension DetailDecisionMakerTransactionObserver: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0)
                    self.completionHandle?(.renewSucceed, 0)
                    //: } else {
                } else { // 普通购买和订阅
                    //: JumpLogTool.lineLoad(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    JumpLogTool.lineLoad(msg: (String(kContentValue) + String(kEffectName.prefix(6))) + "\(currentPayType)" + (kRegularData.capitalized) + "\(String(describing: createOrderId))" + (String(kCollectionValue)) + "\(transaction.payment.productIdentifier)" + (String(k_viewName)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    writeBy(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: JumpLogTool.lineLoad(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                JumpLogTool.lineLoad(msg: (String(k_toText) + String(k_valueTitle.prefix(6))) + "\(currentPayType)" + (kRegularData.capitalized) + "\(String(describing: createOrderId))" + (String(kCollectionValue)) + "\(transaction.payment.productIdentifier)" + (String(k_viewName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0)
                self.completionHandle?(.failed, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0)
                self.completionHandle?(.restored, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: JumpLogTool.lineLoad(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                JumpLogTool.lineLoad(msg: (String(k_noContent) + String(kCellArrayName.suffix(5))) + "\(currentPayType)" + (kRegularData.capitalized) + "\(String(describing: createOrderId))" + (String(kCollectionValue)) + "\(transaction.payment.productIdentifier)" + (String(k_viewName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0)
                self.completionHandle?(.deferred, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0)
                self.completionHandle?(.unknow, 0)
                //: createOrderId = nil
                createOrderId = nil
                //: JumpLogTool.lineLoad(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                JumpLogTool.lineLoad(msg: (k_pathToData + "知交易\u{7c7b}型:") + "\(currentPayType)" + (kRegularData.capitalized) + "\(String(describing: createOrderId))" + (String(kCollectionValue)) + "\(transaction.payment.productIdentifier)" + (String(k_viewName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((String(k_statusTitle)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func writeBy(_ transactionId: String, _ payType: ServiceType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = sendState(transactionId, payType) else {
            //: JumpLogTool.lineLoad(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            JumpLogTool.lineLoad(msg: (String(k_modeSucceedContent) + String(kMakeBetweenContent)) + "\(transactionId)" + (String(k_workContent.prefix(4)) + "derI" + String(k_userData.suffix(2))) + "\(String(describing: createOrderId))" + (String(kTotalimateData) + String(k_saveSecondValue)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据

                if self.payCacheList.filter({ $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId || $0[String(bytes: k_onData.reversed(), encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: k_onData.reversed(), encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: k_appName.map{lineImage(view: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.patronymic()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据

                if self.subscribeCacheList.filter({ $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId || $0[String(bytes: k_onData.reversed(), encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: k_onData.reversed(), encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: k_appName.map{lineImage(view: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.roundModel()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kLet_askName {
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            drogue(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(kGiftValue.replacingOccurrences(of: "list", with: "a") + "sacti" + String(kFrameText.prefix(4)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            equalListenerParams(transactionId, params: paramsArr.first!)
        }
    }
}
