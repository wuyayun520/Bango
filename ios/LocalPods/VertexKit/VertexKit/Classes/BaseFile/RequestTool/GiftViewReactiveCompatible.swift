
//: Declare String Begin

/*: "http://" :*/
fileprivate let k_giftTitle:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let k_tapTitle:String = "false in accept hiddenhttps://"

/*: "http:// :*/
fileprivate let k_iconName:String = "http://manager lab"

/*: "serverSelctIndex =  :*/
fileprivate let k_verticalValue:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n"]
fileprivate let k_tableData:[Character] = ["d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let kLabelTitleName:String = "sappve"
fileprivate let k_dataName:String = "rUrlSmodel perform new change self"

/*: "app. :*/
fileprivate let kIconValue:String = "app.show and"

/*: .com" :*/
fileprivate let k_toTitle:String = ".commake tap"

/*: "IM/getMyBlackList" :*/
fileprivate let k_rowSearchLabValue:String = "IM/gefalse return path in"
fileprivate let k_pathName:[Character] = ["t","M","y","B","l","a"]
fileprivate let kAppContent:String = "user array table modelckList"

/*: "mf/business/sendMsg" :*/
fileprivate let kHostContent:String = "mf/busivideo let i tar path"
fileprivate let k_sourceValue:String = "var situation viewness/s"

/*: "mf/business/sendGift" :*/
fileprivate let kModelData:String = "mf/bbottom title"
fileprivate let k_indexData:String = "identity pics/s"
fileprivate let k_pathData:String = "endGiftvar user frame"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let k_labTitle:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e"]
fileprivate let k_customViewName:String = "let with edge if sharendMsg"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let kLabelData:String = "image type catchmf/cha"
fileprivate let k_indexText:String = "m/senduser data account left"
fileprivate let kTargetValue:String = "view error gift title userGift"

/*: "recharge/applePayNotify" :*/
fileprivate let kManagerName:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N"]
fileprivate let kTableText:String = "olabify"

/*: "user/appleLogin" :*/
fileprivate let kEqualValue:String = "user/apresult to to other max"
fileprivate let k_addValue:[Character] = ["p","l","e","L","o","g","i","n"]

/*: "en" :*/
fileprivate let kLabelName:String = "eview"

/*: "ar" :*/
fileprivate let kAppSTitle:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let kOffTitle:String = "ES"

/*: "pt" :*/
fileprivate let k_viewTitle:String = "femalet"

/*: "GJ21FB" :*/
fileprivate let k_colorContent:String = "GJ21show"

/*: "完成了检测,请求状态正常" :*/
fileprivate let kIndexTitle:String = "完\u{6210}了检测,"

/*: "接口请求失败" :*/
fileprivate let kInfoName:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let k_instanceName:String = "完\u{6210}了检\u{6d4b},"
fileprivate let kCenterVideoData:[Character] = ["e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let k_areaTitle:[Character] = ["H","T","T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let k_dataViewName:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let kGiftName:String = "textap"

/*: "tun" :*/
fileprivate let k_imageValue:String = "itemun"

/*: "ipsec" :*/
fileprivate let kViewValue:String = "ipsevideo"

/*: "ppp" :*/
fileprivate let kResultUserTitle:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let kEndValue:String = "view tofunc__upd"
fileprivate let kUpTitle:[Character] = ["a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let kManagerValue:[Character] = ["h","t","t","p","s",":"]
fileprivate let kOffText:String = "iconicon"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let k_earnModelName:[UInt8] = [0x7,0x14,0xf,0x2,0x3e,0x3e,0x5,0x4,0xd,0x4,0x15,0x4,0x32,0x0,0xc,0x4,0x22,0xe,0xe,0xa,0x8,0x4,0x12,0x36,0x8,0x15,0x9,0x34,0x13,0xd,0x41,0x5c,0x41]

private func equalPhoto(to num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "UID" :*/
fileprivate let k_multiName:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let k_smallData:[Character] = ["P","H","P","S","E","S","S"]
fileprivate let kStorageTitle:String = "Ititle"

/*: "func__saveAllCookies =  :*/
fileprivate let kImageViewName:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i"]
fileprivate let k_indexContent:String = "view var to loges = "

/*: "func__saveAllCookies  error" :*/
fileprivate let kGiftTitle:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"," "," "]
fileprivate let k_tabName:String = "edirectionor"

/*: "🍪 删除 :*/
fileprivate let k_blockName:[Character] = ["🍪"," ","删","\u{9664}"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GiftViewReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum LengthTransformable: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class GiftViewReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = GiftViewReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        timeUser()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(k_giftTitle))) || newValue.hasPrefix((String(k_tapTitle.suffix(8)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_listValue && !kLet_fileSenseData {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_iconName.prefix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_iconName.prefix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    pushPlay(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func timeUser() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_tableRouteValue.integer(forKey: kLet_senseName)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(k_verticalValue) + String(k_tableData)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = sexStart()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_giftTitle))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_listValue && !kLet_fileSenseData {
            //: serverPrefix = "http://"
            serverPrefix = (String(k_giftTitle))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (kLabelTitleName.replacingOccurrences(of: "app", with: "er") + String(k_dataName.prefix(5)) + "tr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func sexStart() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func yellow(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(kIconValue.prefix(4))) + "\(kLet_intimateData)" + (String(k_toTitle.prefix(4))), (String(kIconValue.prefix(4))) + "\(kLet_intimateData)" + (String(k_toTitle.prefix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = NameMacroDefine.getSystemLangCode()
        let lang = NameMacroDefine.hostile()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .titleNameLength(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (k_colorContent.replacingOccurrences(of: "show", with: "FB")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension GiftViewReactiveCompatible {
    //: func checkServerUrl() {
    func aboveEnd() {
        //: if RowReactiveCompatible.share.networkStatus != .Unavailable {
        if RowReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            makeHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            belowModel()
        }
    }

    //: func func_starCodeTime() {
    func belowModel() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.makeHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func funcAddClip() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func makeHost() {
        //: if RowReactiveCompatible.share.networkStatus == .Unavailable {
        if RowReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        funcAddClip()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PaperReactiveCompatible.noneHostCompletion { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (kIndexTitle + "请\u{6c42}状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (kInfoName.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.belowModel()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (k_instanceName + "\u{9700}要更换serv" + String(kCenterVideoData)))
                    //: self.func__changeHostUrl()
                    self.hostSize()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func hostSize() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        funcAddClip()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_tableRouteValue.set(serverSelctIndex, forKey: kLet_senseName)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_giftTitle))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_listValue, !kLet_fileSenseData {
            //: serverPrefix = "http://"
            serverPrefix = (String(k_giftTitle))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension GiftViewReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func column() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(k_areaTitle))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func linguisticContext() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(k_dataViewName))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((kGiftName.replacingOccurrences(of: "text", with: "t"))) || keyStr.contains((k_imageValue.replacingOccurrences(of: "item", with: "t"))) || keyStr.contains((kViewValue.replacingOccurrences(of: "video", with: "c"))) || keyStr.contains((String(kResultUserTitle))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension GiftViewReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func pushPlay(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(kEndValue.suffix(9)) + String(kUpTitle)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = setOut()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(k_giftTitle)), with: (String(k_tapTitle.suffix(8))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(k_tapTitle.suffix(8)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(kManagerValue) + kOffText.replacingOccurrences(of: "icon", with: "/")) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            fillAppear(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        musculoskeletalSystem()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func fillAppear(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: k_earnModelName.map{equalPhoto(to: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func setOut() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_giftTitle))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_listValue, !kLet_fileSenseData {
            //: serverPrefix = "https://"
            serverPrefix = (String(k_tapTitle.suffix(8)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(kIconValue.prefix(4))) + "\(kLet_intimateData)" + (String(k_toTitle.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func backgroundName(note _: Notification) {
        //: if RowReactiveCompatible.share.loginUid.isEmptyString {
        if RowReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            equalVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            emptyHandedLocation()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func equalVersion() {
        //: var htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_multiName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_multiName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_multiName)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(k_multiName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(k_multiName))), h5Cookie.keys.contains((String(k_multiName))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(k_multiName))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(k_multiName))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))), h5Cookie.keys.contains((String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func emptyHandedLocation() {
        //: let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = RowReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == RowReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (String(k_multiName)), cookie.value == RowReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_multiName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_multiName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(k_multiName))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(k_multiName))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(k_smallData) + kStorageTitle.replacingOccurrences(of: "title", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func musculoskeletalSystem() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(kImageViewName) + String(k_indexContent.suffix(5))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_tableRouteValue.set(cookieData, forKey: kLet_successData)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(kGiftTitle) + k_tabName.replacingOccurrences(of: "direction", with: "rr")))
        }
    }

    //: func func__setAppCookies() {
    func viewBlock() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_tableRouteValue.object(forKey: kLet_successData)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func redCookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_tableRouteValue.removeObject(forKey: kLet_successData)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
