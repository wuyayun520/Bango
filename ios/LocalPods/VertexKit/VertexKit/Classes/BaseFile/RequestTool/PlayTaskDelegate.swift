
//: Declare String Begin

/*: / :*/
fileprivate let k_behindMinTitle:String = "actual"

/*:  ( :*/
fileprivate let k_colorValue:String = " male"

/*: ; build: :*/
fileprivate let kMakeValue:[Character] = [";"," ","b","u","i","l"]
fileprivate let kViewText:String = "d:finish for"

/*: ; iOS  :*/
fileprivate let kSucceedData:[Character] = [";"," ","i","O","S"]
fileprivate let kClearTitle:String = "index"

/*: ;  :*/
fileprivate let k_buttonFromName:String = "; "

/*: )" :*/
fileprivate let k_itemValue:[Character] = [")"]

/*: "audio" :*/
fileprivate let kKitValue:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let k_indicatorData:String = "result"

/*: .wav" :*/
fileprivate let kFillDataValue:String = ".wavsection data size"

/*: "audio/wav" :*/
fileprivate let kMultiTotalRandomName:[Character] = ["a","u","d","i","o","/","w"]
fileprivate let kContainerValue:[Character] = ["a","v"]

/*: .jpg" :*/
fileprivate let k_originData:String = "show var me of voice.jpg"

/*: "image/jpeg" :*/
fileprivate let kTableMessageData:[Character] = ["i","m","a","g","e","/"]
fileprivate let k_managerValue:[Character] = ["j","p","e","g"]

/*: "://app." :*/
fileprivate let kControlTitle:String = "://appmake self"
fileprivate let k_tingValue:[Character] = ["."]

/*: "data" :*/
fileprivate let k_rankName:String = "arrayata"

/*: ":null" :*/
fileprivate let k_columnChoiceTitleValue:String = ":nullif button let block"

/*: ":{}" :*/
fileprivate let kDataStripValue:[Character] = [":","{","}"]

/*: "Request failed, Try again later" :*/
fileprivate let kImageValue:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x54,0x20,0x2c,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x74,0x73,0x65,0x75,0x71,0x65,0x52]

/*: "json error" :*/
fileprivate let kStatusData:String = "jcellon"

/*: "platform=iphone&version= :*/
fileprivate let kMultiContent:String = "pathlatf"
fileprivate let kViewValue:String = "array view new as spacehone&v"
fileprivate let k_imageTitle:String = "="

/*: &packageId= :*/
fileprivate let kManagerTitle:String = "&pacto image false scale"
fileprivate let kIconContent:String = "kageId=make status"

/*: &bundleId= :*/
fileprivate let kLabelTitle:[Character] = ["&","b","u","n","d"]
fileprivate let kMessageName:String = "leId=true block model"

/*: &lang= :*/
fileprivate let k_sizeData:[Character] = ["&","l","a","n","g","="]

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let kNameData:String = "?!@#$"
fileprivate let kPostText:String = "main,:;="
fileprivate let k_tapLabName:String = "\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let k_normalInfoIfValue:String = "`<>than)"
fileprivate let k_effectModelEqualData:String = "| "

/*: "/route/slowindex" :*/
fileprivate let k_viewValue:String = "pin touch case/rout"
fileprivate let k_playerName:String = "oplayindex"

/*: "/route/index" :*/
fileprivate let k_topLeadingValue:String = "observer frame true/rou"
fileprivate let kAutomaticValue:String = "te/indexcolor with"

/*: "?path= :*/
fileprivate let k_actualSourceAddName:String = "return background?path="

/*: "&GJNonceStr= :*/
fileprivate let kDayValue:[Character] = ["&","G","J","N","o","n"]
fileprivate let kVideoValue:String = "type view user styleceStr="

/*: "& :*/
fileprivate let kWeightData:[Character] = ["&"]

/*: = :*/
fileprivate let kModeData:String = "="

/*: "&params= :*/
fileprivate let kMaxSourceValue:String = "&parammemory push if"
fileprivate let kTransformData:String = "gold elses="

/*: "/ :*/
fileprivate let k_listName:[Character] = ["/"]

/*: "? :*/
fileprivate let kTextName:[Character] = ["?"]

/*: "token" :*/
fileprivate let k_toName:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let kDataName:String = "请\u{6c42}成功"

/*: "AAAAAAAA" :*/
fileprivate let k_labRegularEqualText:String = "equalequalequalA"
fileprivate let kTouchTitle:String = "container"

/*: "AA" :*/
fileprivate let k_beginValue:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let kTextModelData:String = "tabletable"

/*: "下载完成 -  :*/
fileprivate let k_fileDataLiveName:String = "\u{4e0b}"
fileprivate let kCardSharedName:String = "载完成 - "

/*: "/Documents/" :*/
fileprivate let kLeadingValue:String = "data make model to/Doc"
fileprivate let kEqualValue:String = "uments/table application normal return location"

/*: ".mp4" :*/
fileprivate let k_birthTitle:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let kContentValue:[Character] = ["移","\u{52a8}","\u{5730}","\u{5740}",":"]

/*: "后台任务下载回来" :*/
fileprivate let kTableValue:String = "后台\u{4efb}务下载回\u{6765}"

/*: "下载成功 : :*/
fileprivate let kClearMultiInText:[Character] = ["\u{4e0b}"]
fileprivate let k_addTitle:[Character] = ["载","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let kGiftLeadingValue:String = "Neequal text of re"
fileprivate let kLogValue:[Character] = ["t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let k_beanMediumData:String = "下载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PlayTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_guideData = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_agentDeviceData = "\(kLet_buttonName)/\(kLet_onValue) (\(kLet_tableTitle)" + (String(kMakeValue) + String(kViewText.prefix(2))) + "\(kLet_onNoValue)" + (String(kSucceedData) + kClearTitle.replacingOccurrences(of: "index", with: " ")) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: CurrentErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_gameName = PlayTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class PlayTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
    public func onView(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.beatificationTo() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.magnitudeeractionPath(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.diligent(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
    public func magnitudeeractionPath(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.offPath(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.bottomWith(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subIn(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: kLet_messageScreenTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        appearShow(showMsg: kLet_messageScreenTitle)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(kKitValue)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.clipButton())+\(kLet_inviteValue)" + (String(kFillDataValue.prefix(4))), mimeType: (String(kMultiTotalRandomName) + String(kContainerValue)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(k_originData.suffix(4))), mimeType: (String(kTableMessageData) + String(k_managerValue)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subIn(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: kLet_messageScreenTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        appearShow(showMsg: kLet_messageScreenTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
    public func diligent(model: TalkingMakePtolemaicSystemThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.offPath(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.bottomWith(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_fileSenseData ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_guideData }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subIn(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: kLet_messageScreenTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        appearShow(showMsg: kLet_messageScreenTitle)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_guideData }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subIn(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: kLet_messageScreenTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        appearShow(showMsg: kLet_messageScreenTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingMakePtolemaicSystemThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func subIn(model: TalkingMakePtolemaicSystemThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            GiftViewReactiveCompatible.share.pushPlay(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_fileSenseData, model.requestServer.contains((String(kControlTitle.prefix(6)) + String(k_tingValue))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.addObject(key: GiftViewReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (k_rankName.replacingOccurrences(of: "array", with: "d")) + "\"" + (String(k_columnChoiceTitleValue.prefix(5))), with: "" + "\"" + (k_rankName.replacingOccurrences(of: "array", with: "d")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !kLet_fileSenseData {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(k_columnChoiceTitleValue.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<UpMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == GiftExpressionConvertible.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, CurrentErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    appearShow(showMsg: responseModel.msg ?? String(bytes: kImageValue.reversed(), encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case GiftExpressionConvertible.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_ageValue, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: (kStatusData.replacingOccurrences(of: "cell", with: "s") + " error").localized))
        }
    }

    //: func buildServerUrl(model: TalkingMakePtolemaicSystemThen) -> String {
    func offPath(model: TalkingMakePtolemaicSystemThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (kMultiContent.replacingOccurrences(of: "path", with: "p") + "orm=ip" + String(kViewValue.suffix(6)) + "ersion" + k_imageTitle.capitalized) + "\(kLet_tableStatusOutContent)" + (String(kManagerTitle.prefix(4)) + String(kIconContent.prefix(7))) + "\(kLet_inviteValue)" + (String(kLabelTitle) + String(kMessageName.prefix(5))) + "\(kLet_tableTitle)" + (String(k_sizeData)) + "\(GiftViewReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_fileSenseData, serverUrl.contains((String(kControlTitle.prefix(6)) + String(k_tingValue))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if GiftViewReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(k_viewValue.suffix(5)) + "e/sl" + k_playerName.replacingOccurrences(of: "play", with: "w")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(k_topLeadingValue.suffix(4)) + String(kAutomaticValue.prefix(8)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(k_actualSourceAddName.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(kDayValue) + String(kVideoValue.suffix(6))) + "\(GiftViewReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.performKey(key: GiftViewReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(kMaxSourceValue.prefix(6)) + String(kTransformData.suffix(2))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingMakePtolemaicSystemThen) -> HTTPHeaders {
    func bottomWith(model: TalkingMakePtolemaicSystemThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_agentDeviceData)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(k_toName)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingMakePtolemaicSystemThen) {
    func restaurantAttendant(model: TalkingMakePtolemaicSystemThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.offPath(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (kDataName) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (k_labRegularEqualText.replacingOccurrences(of: "equal", with: "AA") + kTouchTitle.replacingOccurrences(of: "container", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (k_fileDataLiveName + kCardSharedName.capitalized) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(kLeadingValue.suffix(4)) + String(kEqualValue.prefix(7))) + NSDate.clipButton() + (String(k_birthTitle))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(kContentValue)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (kTableValue))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ProvenanceDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func appBorder(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: kLet_domainData.constraintView(), with: kLet_intimateData)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(kClearMultiInText) + String(k_addTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.Normal.rawValue, errorMsg: (String(kGiftLeadingValue.prefix(2)) + String(kLogValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: (String(kGiftLeadingValue.prefix(2)) + String(kLogValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (k_beanMediumData.capitalized) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func atWithPost(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: kLet_domainData.constraintView(), with: kLet_intimateData)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(kClearMultiInText) + String(k_addTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.Normal.rawValue, errorMsg: (String(kGiftLeadingValue.prefix(2)) + String(kLogValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, CurrentErrorResponse(errorCode: GiftExpressionConvertible.NetError.rawValue, errorMsg: (String(kGiftLeadingValue.prefix(2)) + String(kLogValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (k_beanMediumData.capitalized) + "\(errMsg)")
            }
        }
    }
}
