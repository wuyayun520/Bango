
//: Declare String Begin

/*: "fee" :*/
fileprivate let k_voiceText:String = "fshowshow"

/*: "VIPFee" :*/
fileprivate let kGroupAppName:String = "to"
fileprivate let k_listTitle:String = "IPFeeself in content"

/*: "chattingNum" :*/
fileprivate let k_titleContent:String = "chaneedin"
fileprivate let kSpecialText:String = "self color let model datagNum"

/*: "You're already in her live room" :*/
fileprivate let k_withTitle:[UInt8] = [0x8c,0xa2,0xa8,0x5a,0xa5,0x98,0x53,0x94,0x9f,0xa5,0x98,0x94,0x97,0xac,0x53,0x9c,0xa1,0x53,0x9b,0x98,0xa5,0x53,0x9f,0x9c,0xa9,0x98,0x53,0xa5,0xa2,0xa2,0xa0]

fileprivate func compartmentTouch(interaction num: UInt8) -> UInt8 {
    let value = Int(num) - 51
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "toUid" :*/
fileprivate let k_valueCornerTitle:[UInt8] = [0xdd,0xc6,0xfc,0xc0,0xcd]

/*: "momentId" :*/
fileprivate let k_videoData:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "extra" :*/
fileprivate let k_modelContent:[UInt8] = [0x1e,0x3,0xf,0x9,0x1a]

private func cuttingEdge(line num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "uid" :*/
fileprivate let k_frameData:[UInt8] = [0xd8,0xcc,0xc7]

fileprivate func infoColor(server num: UInt8) -> UInt8 {
    let value = Int(num) - 99
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let k_launchTitle:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let k_coverValue:[UInt8] = [0x6e,0x78,0x53,0x6a,0x7c,0x5a,0x78,0x6a,0x77]

fileprivate func statusUser(at num: UInt8) -> UInt8 {
    let value = Int(num) - 5
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "userStatus" :*/
fileprivate let kExitTitle:[UInt8] = [0x20,0x26,0x30,0x27,0x6,0x21,0x34,0x21,0x20,0x26]

private func rawAdd(end num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "Account is restricted！" :*/
fileprivate let kCenterName:String = "Accoreturn if"
fileprivate let kWithValue:String = "line status lab make res"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let k_actualData:[UInt8] = [0xf,0x28,0x20,0x25,0x2c,0x2d,0x69,0x3d,0x26,0x69,0x2c,0x27,0x3d,0x2c,0x3b,0x69,0x3d,0x21,0x2c,0x69,0x2a,0x21,0x28,0x3d,0x69,0x3b,0x26,0x26,0x24,0x67,0x69,0x19,0x25,0x2c,0x28,0x3a,0x2c,0x69,0x3d,0x3b,0x30,0x69,0x28,0x2e,0x28,0x20,0x27,0x69,0x25,0x28,0x3d,0x2c,0x3b]

/*: "url" :*/
fileprivate let k_targetViewToName:[Character] = ["u","r","l"]

/*: "userDetail" :*/
fileprivate let kPathName:String = "userDetrequest title"
fileprivate let kCancelSendLetName:String = "taskil"

/*: "&type=7" :*/
fileprivate let k_locationText:String = "hidden"
fileprivate let k_errName:String = "self as gift pathtype=7"

/*: "amount" :*/
fileprivate let kWillName:[UInt8] = [0xeb,0xe7,0xe5,0xff,0xe4,0xfe]

/*: "transparency" :*/
fileprivate let kResultMessageValue:String = "searchransp"
fileprivate let k_meName:String = "share"
fileprivate let k_valueTitle:String = "RENCY"

/*: "`%^{}" :*/
fileprivate let k_topTitle:String = "`%^status}"

/*: "[]|\\<>" :*/
fileprivate let k_equalModelData:[Character] = ["[","]","|","\\","<",">"]

/*: "version= :*/
fileprivate let k_infoText:[Character] = ["v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let k_noteName:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let k_itemName:String = "&bunsize snap value image"

/*: & :*/
fileprivate let k_plusName:[Character] = ["&"]

/*: ? :*/
fileprivate let k_sexValue:[Character] = ["?"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiraclePushManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class MiraclePushManager: NSObject {
public class MiraclePushManager: NSObject {
    //: @objc static public let share = MiraclePushManager()
    @objc public static let share = MiraclePushManager()
    //: private override init() {}
    override private init() {}
}

//: extension MiraclePushManager {
extension MiraclePushManager {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func viaList(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        kLet_maxValue.detailTing(eventID: kLet_clickName)
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SendSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.appearShow(showMsg: kLet_diskLineData)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        AuthorizationReactiveCompatible.cellMicrophone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            QuickVideoManager.doHiddenCompletion { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = StanzaThen()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(k_voiceText.replacingOccurrences(of: "show", with: "e"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(kGroupAppName.replacingOccurrences(of: "to", with: "V") + String(k_listTitle.prefix(5)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(k_titleContent.replacingOccurrences(of: "need", with: "tt") + String(kSpecialText.suffix(4)))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.belowResume()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func lengthAddInfoVc() {
        //: RowReactiveCompatible.share.userFillInfoMode = UserFillInfoModel.init()
        RowReactiveCompatible.share.userFillInfoMode = ViewInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = ChromaticColorGenderVc()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = sectionVc()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func devise(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = sectionVc()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: TitleRecognizerDelegate.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: PublishViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = TitleRecognizerDelegate()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func toImage(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.belowResume() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? RecordViewController, WithNeighbourReactiveCompatible.clickEqual().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.frontEndIndex()

            //: } else if currentVC.isKind(of: TalkingVoiceRoomViewController.self), TalkingVoiceRoomManager.shared().isParty {
        } else if currentVC.isKind(of: DetailViewController.self), EventManager.bracketOut().isParty {
            //: TalkingVoiceRoomManager.shared().voiceRoom_mini()
            EventManager.bracketOut().magnitudeervalPause()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? UserViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.assemblage()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = UserViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        lineController()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func equalButtonKindTo(uid: String, enterType: TimeEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard AuthorizationReactiveCompatible.neighbourActiveOrParty() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(WithNeighbourReactiveCompatible.clickEqual().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.appearShow(showMsg: String(bytes: k_withTitle.map{compartmentTouch(interaction: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        VehicleAudienceManager().writeCamera(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = WorldHandyJSON.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.sectionVc()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: AddMusicViewDelegate.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! AddMusicViewDelegate).pluralForm()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            WithNeighbourReactiveCompatible.clickEqual().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = AddMusicViewDelegate()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                kLet_maxValue.detailTing(eventID: kLet_qualityText, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                kLet_maxValue.detailTing(eventID: kLet_useTitle, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    /// 进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func func_pushToVoiceRoomVC(roomId: String? = nil) {
    func deleteFinishDeep(roomId: String? = nil) {
        //: if TalkingLiveManager.shared().isLive == true {
        if WithNeighbourReactiveCompatible.clickEqual().isLive == true { // 直播中
            //: func__showStatusBarErrorMsg(showMsg: kMessage_live_limit)
            appearShow(showMsg: kLet_systemOpenText)
            //: return
            return
        }
        //: if roomId == TalkingVoiceRoomManager.shared().partyModel.roomId {
        if roomId == EventManager.bracketOut().partyModel.roomId { // 返回语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_goback()
            EventManager.bracketOut().viewAdd()
            //: return
            return
        }
        //: if TalkingVoiceRoomManager.shared().isParty == true {
        if EventManager.bracketOut().isParty == true { // 切换语聊房
            //: if roomId != nil {
            if roomId != nil {
                //: TalkingVoiceRoomManager.shared().voiceRoom_switch(roomId: roomId!, beforeRoomId: TalkingVoiceRoomManager.shared().partyModel.roomId)
                EventManager.bracketOut().sprechstimmeModel(roomId: roomId!, beforeRoomId: EventManager.bracketOut().partyModel.roomId)
            }
            //: return
            return
        }
        //: TalkingVoiceRoomManager.shared().voiceRoom_checkAndTurnOn(roomId: roomId)
        EventManager.bracketOut().appAlive(roomId: roomId)
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func showDigitizer(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = ForenameViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = sectionVc()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func tapWith(chatID: String, isFrom: BagFromEnum = .Normal, completion: ((_ vc: DoingdDoorBeginViewController) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != NameMacroDefine.getXiaoMiID() {
        if chatID != NameMacroDefine.have() { // 系统消息
            //: ProgressHUD.show()
            ViewBeginReactiveCompatible.deviseShow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: k_valueCornerTitle.map{$0^169}, encoding: .utf8)!: chatID, String(bytes: k_videoData.reversed(), encoding: .utf8)!: "0", String(bytes: k_modelContent.map{cuttingEdge(line: $0)}, encoding: .utf8)!: "1"]
            //: SourceRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            SourceRequestTool.lockTotalCompletion(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = FrontRecordCacheModel.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = FrontRecordCacheModel.dropIn(userDic: result as! [String: Any])
                //: MusicInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                MusicInfoManager.enable(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: k_frameData.map{infoColor(server: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: k_launchTitle.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: k_coverValue.map{statusUser(at: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: kExitTitle.map{rawAdd(end: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_sessionValue,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.appearShow(showMsg: (String(kCenterName.prefix(4)) + "unt is" + String(kWithValue.suffix(4)) + "tricted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.shroudTo(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: FrontRecordCacheModel())
                let chatVC = self.shroudTo(chatID: chatID, isFrom: isFrom, userWrap: FrontRecordCacheModel())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: FrontRecordCacheModel ) -> TalkingPrivateChatController {
    func shroudTo(chatID: String, isFrom: BagFromEnum = .Normal, userWrap: FrontRecordCacheModel) -> DoingdDoorBeginViewController {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = sectionVc()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: DoingdDoorBeginViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! DoingdDoorBeginViewController
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = DoingdDoorBeginViewController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func appDoing(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        ViewBeginReactiveCompatible.deviseShow()
        //: SourceRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        SourceRequestTool.gestureAction(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            ViewBeginReactiveCompatible.capability()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.appearShow(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if NameMacroDefine.isGroupChat(groupId) {
            if NameMacroDefine.betweenIndexView(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.rowAppear(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = ClickThen(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.sectionVc()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.rowAppear(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = ClickThen(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.sectionVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.appearShow(showMsg: String(bytes: k_actualData.map{$0^73}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func rowAppear(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.sectionVc()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: ClickThen.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! ClickThen
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func indexToast(toast: String?) {
        //: let tpAuth = TPUserAuth(rawValue: RowReactiveCompatible.share.loginUserMode.isTPAuth)
        let tpAuth = GiftBasicType(rawValue: RowReactiveCompatible.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingFaceVerificationVC()
            let vc = WithRecognizerDelegate()
            //: vc.toastStr = toast ?? ""
            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = sectionVc()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingFinalVerificationVC()
            let vc = FundamentalMeasureViewController()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = sectionVc()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func boardUp(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func errorVc() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = sectionVc()
        //: let vc = TalkingFeedbackVC.init()
        let vc = MakeRecognizerDelegate()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func packet(jumpModel: AddModelType) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (String(k_targetViewToName)) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            pathTo(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(kPathName.prefix(7)) + kCancelSendLetName.replacingOccurrences(of: "task", with: "a")) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func dataConverterVc() {
    @objc public func dataConverterVc() {
        //: MiraclePushManager.share.func__pushToWebVC(webViewType: .SubscribePage)
        MiraclePushManager.share.dataType(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func funcName(type: LengthOutputStream, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = RoundViewController()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        sectionVc()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension MiraclePushManager {
extension MiraclePushManager {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func dataType(webViewType: LastSequence) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        digitiserEqual(webViewType: webViewType, webConfig: GiftAddConfig())
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func noBuild(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (k_locationText.replacingOccurrences(of: "hidden", with: "&") + String(k_errName.suffix(6)))) {
        //: let payWinType = RowReactiveCompatible.share.appUserConfigMode.payWinType
        let payWinType = RowReactiveCompatible.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)
        //: if payWinType == 1 || (RowReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) {
        if payWinType == 1 || (RowReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
            //: func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
            adjustEvent(webViewType: .RechargeHalfPage, clickEvent: clickEvent)

            //: } else if payWinType == 2 {
        } else if payWinType == 2 { // 会员订阅弹窗
            //: func__pushToSubscribeAlert(appendParams: appendParams)
            alert(appendParams: appendParams)
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func alert(appendParams: String = (k_locationText.replacingOccurrences(of: "hidden", with: "&") + String(k_errName.suffix(6)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = AstatineReactiveCompatible.typeOn(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: MiraclePushManager.share.func__pushToWebVC(urlStr: urlStr)
        MiraclePushManager.share.pathTo(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        kLet_maxValue.listStr(eventID: kLet_voiceText, parameterStr: [String(bytes: kWillName.map{$0^138}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func adjustEvent(webViewType: LastSequence, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = GiftAddConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        digitiserEqual(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            kLet_dismissValue = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                kLet_maxValue.listStr(eventID: clickEvent, parameterStr: [String(bytes: kWillName.map{$0^138}, encoding: .utf8)!: 0])
            }
        }
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func digitiserEqual(webViewType: LastSequence, webConfig: GiftAddConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = AstatineReactiveCompatible.typeOn(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        imageBy(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func pathTo(urlStr: String?, webConfig: GiftAddConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? GiftAddConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        imageBy(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func imageBy(urlStr: String?, webViewType: LastSequence?, webConfig: GiftAddConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(kResultMessageValue.replacingOccurrences(of: "search", with: "t") + k_meName.replacingOccurrences(of: "share", with: "a") + k_valueTitle.lowercased())].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(k_infoText)) + "\(kLet_tableStatusOutContent)" + (String(k_noteName)) + "\(kLet_inviteValue)" + (String(k_itemName.prefix(4)) + "dleId=") + "\(kLet_tableTitle)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = MakeUpNavigationDelegate(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = sectionVc() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: MakeUpNavigationDelegate.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! MakeUpNavigationDelegate).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.neglect(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_errData / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension MiraclePushManager {
public extension MiraclePushManager {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func sectionVc() -> UIViewController? {
        //: return currentViewController()
        return belowResume()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct AddModelType: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
