
//: Declare String Begin

/*: "cmd" :*/
fileprivate let k_textValue:[UInt8] = [0xa4,0xaa,0xa3]

private func transformVoice(path num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "cancelCall" :*/
fileprivate let k_removeText:String = "bar in index not equalcancelCa"
fileprivate let k_viewData:String = "interactioninteraction"

/*: "data" :*/
fileprivate let k_labelName:[UInt8] = [0x65,0x62,0x75,0x62]

fileprivate func userAsset(frame num: UInt8) -> UInt8 {
    let value = Int(num) + 255
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let k_emptyData:String = "logIdload data video button"

/*: "#322870" :*/
fileprivate let k_tingTitle:[Character] = ["#","3","2","2","8","7"]
fileprivate let k_tapTitle:[Character] = ["0"]

/*: "Tap to start video chatting" :*/
fileprivate let kConstraintValue:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d","e","o"]
fileprivate let kErrorName:String = " chadate data color"

/*: "icon_close_nor" :*/
fileprivate let kAddContent:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let kTopTitle:[Character] = ["o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let kDenyValue:[Character] = ["i","c","o","n","_","v","i","e"]
fileprivate let kMediumFromTitle:String = "WS"
fileprivate let k_userValue:[Character] = ["_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let k_loadData:[Character] = ["%","@"," "]
fileprivate let kAppearanceData:[Character] = ["c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let kMakeTitle:String = "modelon"
fileprivate let kPriceData:String = "gmanager"

/*: "icon_rank_coin" :*/
fileprivate let k_textTitle:[Character] = ["i","c","o","n","_"]
fileprivate let kVideoText:String = "main"
fileprivate let k_bottomValue:String = "nk_coincontent view wait user permission"

/*: "vip_btn_top" :*/
fileprivate let k_playLineGiftData:String = "title view effectvip_b"
fileprivate let k_videoName:String = "tn_topif image title constraint"

/*: "btn_nearby_settings_nor" :*/
fileprivate let kCurrentMakeLeadingName:String = "btn_to import make"
fileprivate let kToolValue:String = "totoings"

/*: "%@ Coins/min" :*/
fileprivate let k_elementTitle:String = "%@ Cview data color else let"
fileprivate let k_timeValue:[Character] = ["/","m","i","n"]

/*: "get json error" :*/
fileprivate let kDismissName:[Character] = ["g","e","t"," ","j","s"]
fileprivate let kAboutEndName:[Character] = ["o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let k_boldData:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let kFromTitle:[UInt8] = [0x84,0x91,0x91,0x8d,0x8e]

fileprivate func editTableModel(time num: UInt8) -> UInt8 {
    let value = Int(num) + 225
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let k_sizeSharedWhiteName:String = "VIPFeeself normal me view aspect"

/*: "Matching now" :*/
fileprivate let k_errorNameValue:String = "cookie any view addMatchi"
fileprivate let k_shareContent:[Character] = ["o","w"]

/*: "userTouch" :*/
fileprivate let kGreenValue:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let k_bottomName:String = "color textmf/fre"
fileprivate let k_backValue:String = "level video/getVi"
fileprivate let kViewName:String = "nfmanager"

/*: "fromType" :*/
fileprivate let k_modeName:[UInt8] = [0x80,0x94,0x89,0x8b,0xb2,0x9f,0x96,0x83]

private func graduatedTable(more num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "You've been barred from receiving calls" :*/
fileprivate let k_colorData:[UInt8] = [0x88,0xbe,0xa4,0xf6,0xa7,0xb4,0xf1,0xb3,0xb4,0xb4,0xbf,0xf1,0xb3,0xb0,0xa3,0xa3,0xb4,0xb5,0xf1,0xb7,0xa3,0xbe,0xbc,0xf1,0xa3,0xb4,0xb2,0xb4,0xb8,0xa7,0xb8,0xbf,0xb6,0xf1,0xb2,0xb0,0xbd,0xbd,0xa2]

private func economicTheory(dismiss num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "source" :*/
fileprivate let k_amTipName:[UInt8] = [0xbe,0xa2,0xb8,0xbf,0xae,0xa8]

private func computerSimulation(no num: UInt8) -> UInt8 {
    return num ^ 205
}

/*: "type" :*/
fileprivate let k_makerCornerData:[UInt8] = [0x55,0x58,0x51,0x44]

private func toData(back num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "uid" :*/
fileprivate let k_labelText:[UInt8] = [0xe9,0xf5,0xf8]

/*: "fromFreeCall" :*/
fileprivate let k_intimateName:String = "succeed else app image titlefromFre"
fileprivate let k_picName:String = "eCallrequest gift height view"

/*: "requestCall" :*/
fileprivate let k_beginValue:[Character] = ["r","e","q","u","e","s","t","C","a"]
fileprivate let kGestureValue:String = "count"

/*: "onVideoMatch" :*/
fileprivate let kCollectionText:String = "gift name inonVi"
fileprivate let kDataName:String = "atcthe"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let kTextData:[UInt8] = [0x59,0x43,0x77,0x76,0x6d,0x6f,0x63,0x76,0x6b,0x61,0x22,0x74,0x6b,0x66,0x67,0x6d,0x5f,0x22,0x50,0x67,0x61,0x67,0x6b,0x74,0x67,0x22,0x74,0x6b,0x66,0x67,0x6d,0x22,0x61,0x63,0x6e,0x6e,0x22,0x6b,0x6c,0x61,0x6d,0x6f,0x6b,0x6c,0x65]

/*: "refuseCall" :*/
fileprivate let k_requestData:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let kHolderValue:String = "onCasend self"
fileprivate let k_momentName:String = "direction"

/*: "retryAfter" :*/
fileprivate let k_priceTitle:[Character] = ["r","e","t","r","y","A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let kPhotoText:String = "onRequesself float image"
fileprivate let k_lengthUserData:[Character] = ["t","C","a","l","l"]

/*: "failType" :*/
fileprivate let k_kindValue:String = "leading model self centerfailType"

/*: "callWaitingTime" :*/
fileprivate let kTalkTitle:String = "CALL"
fileprivate let kAddTitle:String = "self kindgTime"

/*: "toUid" :*/
fileprivate let k_menuData:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let kModelErrorName:String = "return string fileFree"

/*: "%@/min" :*/
fileprivate let k_labContent:String = "actual net and%@/mi"
fileprivate let kBottomNameContent:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StanzaThen.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class StanzaThen: RecordReactiveCompatible {
	var toTotal: Double = -7.3

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.inexpensivenessPrice()
            }
        }
        //: self.resetCamera()
        self.movieCameraGesture()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        upSubviews()
        //: setupSubViewsConstraint()
        dowry()
        //: bindInteraction()
        actionFollow()
        //: videoManager.startPreview(isMatch: true)
        videoManager.toClick(isMatch: true)
        //: setPlayer(isRandon: false)
        concern(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            startTitle()
        }
    
            

	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_textValue.map{transformVoice(path: $0)}, encoding: .utf8)!: (String(k_removeText.suffix(8)) + k_viewData.replacingOccurrences(of: "interaction", with: "l")), String(bytes: k_labelName.map{userAsset(frame: $0)}, encoding: .utf8)!: [(String(k_emptyData.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        collectionSection()
        //: if TalkingSocketManager.shared.isTalking == false {
        if SendSocketDelegate.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SendSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SendSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SendSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SendSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RefreshThen = {
        //: let m = TalkingVideoChatManager()
        let m = RefreshThen()
        //: let info = TalkingVideoChatModel()
        let info = MakeChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .dropMultiView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(startTitle), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_errData + 1, height: kLet_nameValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(k_tingTitle) + String(k_tapTitle)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(kConstraintValue) + String(kErrorName.prefix(4)) + "tting").localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.collectionName(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.collectionName(name: (String(kAddContent) + String(kTopTitle))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(golfer), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: AddReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = AddReactiveCompatible()
        //: btn.setImage(UIImage.collectionName(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.collectionName(name: (String(kDenyValue) + kMediumFromTitle.lowercased() + String(k_userValue))), for: .normal)
        //: btn.setImage(UIImage.collectionName(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.collectionName(name: (String(kDenyValue) + kMediumFromTitle.lowercased() + String(k_userValue))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.ofAndSize(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(k_loadData) + String(kAppearanceData)).roundName(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.collectionName(name: "icon_lounge")
        img.image = UIImage.collectionName(name: (kMakeTitle.replacingOccurrences(of: "model", with: "ic") + "_loun" + kPriceData.replacingOccurrences(of: "manager", with: "e")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.collectionName(name: "icon_rank_coin")
        img.image = UIImage.collectionName(name: (String(k_textTitle) + kVideoText.replacingOccurrences(of: "main", with: "ra") + String(k_bottomValue.prefix(7))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.ofAndSize(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.collectionName(name: "vip_btn_top")
        imgV.image = UIImage.collectionName(name: (String(k_playLineGiftData.suffix(5)) + String(k_videoName.prefix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: AddReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = AddReactiveCompatible()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sexualityClickSnap), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.collectionName(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.collectionName(name: (String(kCurrentMakeLeadingName.prefix(4)) + "nearby_se" + kToolValue.replacingOccurrences(of: "to", with: "t") + "_nor")), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.collectionName(name: "icon_rank_coin")
        imgV.image = UIImage.collectionName(name: (String(k_textTitle) + kVideoText.replacingOccurrences(of: "main", with: "ra") + String(k_bottomValue.prefix(7))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .coloured()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(k_elementTitle.prefix(4)) + "oins" + String(k_timeValue)).roundName(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.ofAndSize(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: BeanReactiveCompatible = //: return BeanReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension StanzaThen {
    //: private func setPlayer(isRandon: Bool) {
    private func concern(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = DataEnvironmentReactiveCompatible.default.atType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(kDismissName) + String(kAboutEndName)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func removeErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(k_boldData)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: k_textValue.map{transformVoice(path: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: kFromTitle.map{editTableModel(time: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        QuickVideoManager.labelCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.firstVideo()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_boldData))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func tapUp() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        QuickVideoManager.doHiddenCompletion { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(k_sizeSharedWhiteName.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.inexpensivenessPrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension StanzaThen {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func sexualityClickSnap() {
        //: MiraclePushManager.share.func__pushToSubscribeAlert()
        MiraclePushManager.share.alert()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func golfer() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        restrictionList()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_maxValue.detailTing(eventID: kLet_useData)

        //: if RowReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if RowReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.countShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        belowResume()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func startTitle() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SendSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.appearShow(showMsg: kLet_diskLineData)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        dealCell()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(k_errorNameValue.suffix(6)) + "ng n" + String(k_shareContent)).localized
        //: setPlayer(isRandon: true)
        concern(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if RowReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if RowReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_maxValue.detailTing(eventID: kLet_dismissTitle)
            //: req_getVideoInfo()
            tvJump()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_maxValue.detailTing(eventID: kLet_barName)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            SendSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            SendSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            SendSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            SendSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            viewCell()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func viewCell(cmd: String = (String(kGreenValue)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        collectionSection()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        removeErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func firstVideo() {
        // 停止定时器
        //: destroyWaitingTimer()
        collectionSection()
        //: showCloseBtn()
        restrictionList()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(kConstraintValue) + String(kErrorName.prefix(4)) + "tting").localized
        //: setPlayer(isRandon: false)
        concern(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if SendSocketDelegate.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SendSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SendSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SendSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SendSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        inexpensivenessPrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func collectionSection() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func tvJump() {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(k_bottomName.suffix(6)) + "eCall" + String(k_backValue.suffix(6)) + "deoI" + kViewName.replacingOccurrences(of: "manager", with: "o"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: k_modeName.map{graduatedTable(more: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.firstVideo()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: RowReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    RowReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.startTitle()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.appearShow(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = VideoModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = PanelPlayerManager()
                //: player.setMute(bEnable: true)
                player.moment(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.pushName(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                SelectGlobalManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                AuthorizationReactiveCompatible.cellMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.gameWithThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func gameWithThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_maxValue.detailTing(eventID: kLet_blockData)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard SelectGlobalManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.appearShow(showMsg: String(bytes: k_colorData.map{economicTheory(dismiss: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            SelectGlobalManager.shared.loadClick(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.firstVideo()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = VideoPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.belowResume()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.firstVideo()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func dealCell() {
        //: if RowReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if RowReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(RowReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(RowReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func restrictionList() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func movieCameraGesture() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.toClick()
        }
    }
}

// MARK: - DataObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension StanzaThen: DataObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func underMatch(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = SearchedHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(k_boldData)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: k_amTipName.map{computerSimulation(no: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: k_makerCornerData.map{toData(back: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: k_labelText.map{$0^156}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = RoundNameConvertible.randow
        //: if RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RowReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RowReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(k_intimateName.suffix(7)) + String(k_picName.prefix(5))))
            //: TalkingSocketManager.shared.isFreeCall = true
            SendSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: k_textValue.map{transformVoice(path: $0)}, encoding: .utf8)!: (String(k_beginValue) + kGestureValue.replacingOccurrences(of: "count", with: "ll")), String(bytes: k_labelName.map{userAsset(frame: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        SendSocketDelegate.shared.castToDataFormattingSub(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_textValue.map{transformVoice(path: $0)}, encoding: .utf8)!: (String(k_removeText.suffix(8)) + k_viewData.replacingOccurrences(of: "interaction", with: "l")), String(bytes: k_labelName.map{userAsset(frame: $0)}, encoding: .utf8)!: [(String(k_emptyData.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.viewCell(cmd: (String(kCollectionText.suffix(4)) + "deoM" + kDataName.replacingOccurrences(of: "the", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func toTime(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(k_emptyData.prefix(5)))].intValue
    }
}

// MARK: - UserObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension StanzaThen: UserObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func vacuumBagData(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func outsideEqual(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func statUp(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func aggregationButton(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ProvenanceDelegateHelper.checked(body: String(bytes: kTextData.map{$0^2}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = SearchedHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            directorate(toUid: String(uid))
        }
        //: stop_randomVideo()
        firstVideo()
        //: RowReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        RowReactiveCompatible.share.situation(info: data, situation: .Calling)
    }
}

// MARK: - RecordObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StanzaThen: RecordObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func levelBar(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        viewCell(cmd: (String(k_requestData)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func tableData(data _: [String: Any]) {}
}

// MARK: - DirectorObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension StanzaThen: DirectorObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func anonymous(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kHolderValue.prefix(4)) + "ncelCa" + k_momentName.replacingOccurrences(of: "direction", with: "ll")) { // 取消通话异常
            //: stop_randomVideo()
            firstVideo()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(kCollectionText.suffix(4)) + "deoM" + kDataName.replacingOccurrences(of: "the", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            firstVideo()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_boldData))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.appearShow(showMsg: msg)
            //: uploadFailTime(type: 2)
            tillTag(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(kCollectionText.suffix(4)) + "deoM" + kDataName.replacingOccurrences(of: "the", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(k_priceTitle))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(k_boldData))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.viewCell(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(kPhotoText.prefix(8)) + String(k_lengthUserData)), errorNo == TvEnablesetTarget.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            firstVideo()
            //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else { return }
            //: MiraclePushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            MiraclePushManager.share.noBuild(clickEvent: kLet_screenName, sufficient: false)
            //: uploadFailTime(type: 1)
            tillTag(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        viewCell(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension StanzaThen {
    //: func uploadFailTime(type: Int) {
    func tillTag(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(k_kindValue.suffix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(kTalkTitle.lowercased() + "Waitin" + String(kAddTitle.suffix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_maxValue.listStr(eventID: kLet_successName, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func directorate(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(kTalkTitle.lowercased() + "Waitin" + String(kAddTitle.suffix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(k_menuData))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_maxValue.listStr(eventID: kLet_cleanValue, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension StanzaThen {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func inexpensivenessPrice() {
        //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !RowReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !RowReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = RoundNameConvertible.randow
        //: if RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RowReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if RowReactiveCompatible.share.loginUserMode.freeCallTimes > 0, RowReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if RowReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if RowReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(kModelErrorName.suffix(4)) + " 1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = RowReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = RowReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(k_labContent.suffix(5)) + String(kBottomNameContent)).roundName(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.moleculeClick(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(k_labContent.suffix(5)) + String(kBottomNameContent)).roundName(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.moleculeClick(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = RowReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = RowReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = RowReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = RowReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !RowReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !RowReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func upSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func dowry() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_topData + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func actionFollow() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(inexpensivenessPrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_frameClickValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(tapUp),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_pingName,
                                               //: object: nil)
                                               object: nil)
    }
}
