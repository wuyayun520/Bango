
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let k_titleData:String = "setPasmeeting normal add layer"
fileprivate let k_insideName:String = "swmodeld"

/*: "user/appleLogin" :*/
fileprivate let kMakeErrorContent:[Character] = ["u","s","e","r","/","a","p","p","l","e","L","o","g","i"]
fileprivate let kCenterData:String = "to"

/*: "user/smsLogin" :*/
fileprivate let kFormatData:String = "if view shareuser/s"

/*: "user/emailVcodeLogin" :*/
fileprivate let k_makeFieldName:[Character] = ["u","s","e","r","/","e","m"]
fileprivate let kLoadText:String = "let collection aspect icon notailVc"

/*: "user/pwdLogin" :*/
fileprivate let kPositionData:[Character] = ["u","s","e","r","/","p","w","d","L","o"]
fileprivate let k_rowData:String = "topin"

/*: "user/emailPwdLogin" :*/
fileprivate let kManagerValue:[Character] = ["u","s","e","r","/","e","m","a","i","l","P","w","d","L","o","g","i","n"]

/*: "+ :*/
fileprivate let kInsideValue:String = "data"

/*: "mobile" :*/
fileprivate let k_mediaIndexJumpValue:[UInt8] = [0xa3,0xa1,0xac,0xa7,0xa2,0xab]

private func errorVar(model num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "areaCode" :*/
fileprivate let kStatusYearData:[UInt8] = [0x65,0x76,0x69,0x65,0x47,0x73,0x68,0x69]

fileprivate func dateFrom(title num: UInt8) -> UInt8 {
    let value = Int(num) - 4
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/sendLoginSms" :*/
fileprivate let kUntilData:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g","i","n","S","m"]
fileprivate let kDeadlineAddValue:String = "mode"

/*: "RSA加密失败" :*/
fileprivate let kTitleRequestName:String = "RSA\u{52a0}密"
fileprivate let kTargetContent:String = "失image"

/*: "password" :*/
fileprivate let k_colorValue:[UInt8] = [0xb,0xfc,0xe,0xe,0x12,0xa,0xd,0xff]

fileprivate func viewMessage(make num: UInt8) -> UInt8 {
    let value = Int(num) - 155
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "vcode" :*/
fileprivate let kModelSourceValue:[UInt8] = [0xa7,0x94,0xa0,0x95,0x96]

fileprivate func layerBottom(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 49
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/modifyPassword" :*/
fileprivate let kModelName:[Character] = ["u","s"]
fileprivate let k_backClearData:[Character] = ["e","r","/","m","o","d","i","f","y","P","a","s","s","w","o","r","d"]

/*: "user/sendModifyPwdSms" :*/
fileprivate let k_withTitle:String = "color progress delay else buttonuser/"
fileprivate let kRawData:String = "to in fill forModi"
fileprivate let k_makeContent:[Character] = ["S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let k_statusData:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f","y","P"]
fileprivate let kSearchText:String = "wdEmailtop to user string"

/*: "mf/user/getMyInfo" :*/
fileprivate let kModelOfBlockData:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","M"]
fileprivate let kPathInfoText:String = "yInfoself title index if"

/*: "invite/bindInviter" :*/
fileprivate let k_frameValue:String = "iblockvite"
fileprivate let kDateText:[Character] = ["/","b","i","n"]
fileprivate let k_atName:String = "dInvitermake style if"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let kIndexData:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let kGiftName:[Character] = ["C","h","e","c","k","I","n","v","i","t","e","C","o","d","e"]

/*: "mf/user/updateInfo" :*/
fileprivate let k_partyText:String = "true countmf/us"
fileprivate let kShowValue:[Character] = ["e"]
fileprivate let kModelAppValue:String = "head resume message color ifr/up"

/*: "p0" :*/
fileprivate let kErrorTitle:String = "pdata"

/*: "email" :*/
fileprivate let kTableTitle:[UInt8] = [0x51,0x59,0x4d,0x55,0x58]

fileprivate func researchLab(top num: UInt8) -> UInt8 {
    let value = Int(num) - 236
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/getEmailBindVCode" :*/
fileprivate let k_micData:String = "item false touser/g"
fileprivate let kLiveData:String = "ailBiwith progress height false"

/*: "user/sendLoginEmail" :*/
fileprivate let kBlockListenName:String = "usbuttonr"
fileprivate let kToolName:String = "LoginElab table"

/*: "user/emailBind" :*/
fileprivate let kAddName:String = "user/eelse title"
fileprivate let k_valueTopData:String = "mailBindof count conversation self make"

/*: "user/logout" :*/
fileprivate let kClearTitle:String = "usdropr"
fileprivate let kEqualTitle:String = "T"

/*: "app/countryList" :*/
fileprivate let k_enableValue:[Character] = ["a","p","p","/","c","o","u","n","t","r","y","L","i","s","t"]

/*: "user/getMobileBindVCode" :*/
fileprivate let kIconValue:String = "buttoner"
fileprivate let kTaskValue:String = "save fail self sizebileB"
fileprivate let k_headData:String = "indVCodeframe event user"

/*: "user/mobileBind" :*/
fileprivate let kButtonValue:String = "user/mto user"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LastLengthRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class LastLengthRequestTool: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func up(type: LengthOutputStream, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.method(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(k_titleData.prefix(6)) + k_insideName.replacingOccurrences(of: "model", with: "or"))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: MiraclePushManager.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                MiraclePushManager.share.funcName(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            LastLengthRequestTool.head(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingMakePtolemaicSystemThen {
    private class func method(type: LengthOutputStream, params: [String: String]) -> TalkingMakePtolemaicSystemThen {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (String(kMakeErrorContent) + kCenterData.replacingOccurrences(of: "to", with: "n"))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(kFormatData.suffix(6)) + "msLogin")
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(k_makeFieldName) + String(kLoadText.suffix(5)) + "odeLogin")
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(kPositionData) + k_rowData.replacingOccurrences(of: "top", with: "g"))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(kManagerValue))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func start(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: k_mediaIndexJumpValue.map{errorVar(model: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kStatusYearData.map{dateFrom(title: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingMakePtolemaicSystemThen.init()
            let reqModel = TalkingMakePtolemaicSystemThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(kUntilData) + kDeadlineAddValue.replacingOccurrences(of: "mode", with: "s"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func name(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: k_mediaIndexJumpValue.map{errorVar(model: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: kStatusYearData.map{dateFrom(title: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: k_colorValue.map{viewMessage(make: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            LastLengthRequestTool.up(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func small(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: k_colorValue.map{viewMessage(make: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: kModelSourceValue.map{layerBottom(gift: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingMakePtolemaicSystemThen()
            let reqModel = TalkingMakePtolemaicSystemThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(kModelName) + String(k_backClearData))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func add(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(k_withTitle.suffix(5)) + "send" + String(kRawData.suffix(4)) + "fyPwd" + String(k_makeContent))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func view(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(k_statusData) + String(kSearchText.prefix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func head(_ type: LengthOutputStream, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kModelOfBlockData) + String(kPathInfoText.prefix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            kLet_tableRouteValue.set(result, forKey: kLet_lineName)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<VanguardUserModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: RowReactiveCompatible.share.loginUserMode = userModel
                RowReactiveCompatible.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                kLet_tableRouteValue.set(type.rawValue, forKey: kLet_giftValue)
                //: if RowReactiveCompatible.share.loginUserMode.updateInfo == false {
                if RowReactiveCompatible.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: MiraclePushManager.share.func__pushToFillUserInfoVC()
                    MiraclePushManager.share.lengthAddInfoVc()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: kLet_itemText, object: nil, userInfo: result as! [String: Any])

                //: if RowReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                if RowReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: MiraclePushManager.share.func__pushToLockUserEmailVC(isShowBack: false)
                        MiraclePushManager.share.devise(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func holdFastKeyCompletion(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (k_frameValue.replacingOccurrences(of: "block", with: "n") + String(kDateText) + String(k_atName.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func completionTheme(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(kIndexData) + String(kGiftName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func dataRequest(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(k_partyText.suffix(5)) + String(kShowValue) + String(kModelAppValue.suffix(4)) + "dateInfo")
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.clipButton()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(kLet_screenData)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.pathLength(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func labelAction(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: kTableTitle.map{researchLab(top: $0)}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingMakePtolemaicSystemThen.init()
            let reqModel = TalkingMakePtolemaicSystemThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(k_micData.suffix(6)) + "etEm" + String(kLiveData.prefix(5)) + "ndVCode")
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (kBlockListenName.replacingOccurrences(of: "button", with: "e") + "/send" + String(kToolName.prefix(6)) + "mail")
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func of(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(kAddName.prefix(6)) + String(k_valueTopData.prefix(8)))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: kModelSourceValue.map{layerBottom(gift: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func login(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: kTableTitle.map{researchLab(top: $0)}, encoding: .utf8)!: resultEmailStr, String(bytes: k_colorValue.map{viewMessage(make: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            LastLengthRequestTool.up(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func picture(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (kClearTitle.replacingOccurrences(of: "drop", with: "e") + "/logou" + kEqualTitle.lowercased())
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func nameView(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kModelOfBlockData) + String(kPathInfoText.prefix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                kLet_tableRouteValue.set(result, forKey: kLet_lineName)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<VanguardUserModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: RowReactiveCompatible.share.loginUserMode = userModel
                    RowReactiveCompatible.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func countryCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(k_enableValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func request(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: k_mediaIndexJumpValue.map{errorVar(model: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kStatusYearData.map{dateFrom(title: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingMakePtolemaicSystemThen.init()
            let reqModel = TalkingMakePtolemaicSystemThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (kIconValue.replacingOccurrences(of: "button", with: "us") + "/getMo" + String(kTaskValue.suffix(5)) + String(k_headData.prefix(8)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func frenchTelephoneMedium(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kLet_buttonTabTitle)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: k_mediaIndexJumpValue.map{errorVar(model: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kStatusYearData.map{dateFrom(title: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: kModelSourceValue.map{layerBottom(gift: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingMakePtolemaicSystemThen.init()
            let reqModel = TalkingMakePtolemaicSystemThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(kButtonValue.prefix(6)) + "obileBind")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (kTitleRequestName + kTargetContent.replacingOccurrences(of: "image", with: "败")))
        }
    }
}
