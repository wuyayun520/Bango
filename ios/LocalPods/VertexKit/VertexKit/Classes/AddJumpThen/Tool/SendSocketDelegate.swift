
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kEffectValue:String = "SOCcellE"
fileprivate let kGiftData:String = "female share let tap upR_KE"
fileprivate let k_toName:[Character] = ["Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let k_whiteName:[UInt8] = [0x83,0x9f,0x93,0x9b,0x95,0x84,0x8f,0x86,0x99,0x94,0x95,0x9f,0x93,0x98,0x91,0x84,0x8f,0x82,0x95,0x81,0x85,0x95,0x83,0x84,0x93,0x91,0x9c,0x9c,0x8f,0x9e,0x9f,0x84,0x99,0x96,0x99,0x93,0x91,0x84,0x99,0x9f,0x9e]

private func observerBottom(list num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let kEqualMediumValue:[UInt8] = [0x9d,0x8a,0x8c,0x86,0x99,0x8a,0x90,0x89,0x83,0x80,0x8e,0x9b,0x9c,0x8c,0x9d,0x8a,0x8a,0x81,0x90,0x81,0x80,0x9b,0x86,0x89,0x86,0x8c,0x8e,0x9b,0x86,0x80,0x81]

/*: "isCalling" :*/
fileprivate let k_labData:String = "size view self lock customisCal"

/*: "isTalking" :*/
fileprivate let kMakeData:String = "enable object button sizeisTal"

/*: "SOCKET 连接成功." :*/
fileprivate let kLayerSizeData:String = "SOtime"
fileprivate let k_areaValue:[Character] = ["K","E","T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let k_imageValue:String = "contentOCK"
fileprivate let k_userViewData:String = "false cell false败, error"
fileprivate let kFailName:String = "below"

/*: ." :*/
fileprivate let kTextValue:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let kItemAreaText:String = "SOCphoto"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let kAppData:String = "次, 切换"
fileprivate let kPopTitle:String = "et域名重新\u{5c1d}\u{8bd5}"
fileprivate let k_hiddenName:[Character] = ["\u{ff0c}","\u{5730}","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let kLabData:String = "needOCKET"
fileprivate let k_locationText:String = " 已关\u{95ed}, "
fileprivate let kWhiteName:[Character] = ["c","o","d","e",":"]

/*: , reason: :*/
fileprivate let k_valueName:String = ", reimport class"

/*: "ws:// :*/
fileprivate let k_robotText:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let k_normalName:String = "/?sid=user never background cell"

/*: &uid= :*/
fileprivate let kModelName:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let kHideValueName:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e"]
fileprivate let k_nameEndText:String = "collection center cell&v"
fileprivate let k_screenText:String = "ersion=script video red"

/*: &packageId= :*/
fileprivate let k_borderContent:String = "index complete app if view&pack"

/*: "1v1server. :*/
fileprivate let k_giftTextProductTitle:String = "label insert path var import1v1ser"

/*: .com:7766" :*/
fileprivate let k_dataInsideName:[Character] = [".","c","o","m",":","7","7","6"]
fileprivate let kPlayerName:String = "size"

/*: "43.129.185.194:7767" :*/
fileprivate let k_resultValue:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9","4",":"]
fileprivate let k_upTitle:String = "7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let k_meetingValue:[UInt8] = [0x21,0x3d,0x31,0x39,0x37,0x26,0x52,0x95,0xcf,0xe3,0x95,0xc9,0xee,0x0,0x17,0x13,0x11,0x1a,0x13,0x10,0x1b,0x1e,0x1b,0x6,0xb,0x31,0x1a,0x13,0x1c,0x15,0x17,0x16,0x95,0xe9,0xe3,0x97,0xe2,0xde,0x9d,0xce,0xfe,0x94,0xe4,0xdf,0x95,0xcf,0xe3,0x97,0xf7,0xc1,0x9b,0xe5,0xdf,0x5c]

private func sizeLet(data num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "cmd" :*/
fileprivate let k_errorTableName:String = "cmadd"

/*: "ping" :*/
fileprivate let kPageName:String = "maxng"

/*: "callHeartbeat" :*/
fileprivate let kBackgroundName:String = "callHfinish self size return count"
fileprivate let k_parentData:String = "amessage"

/*: "recievedDate" :*/
fileprivate let kTopData:[Character] = ["r","e","c","i","e","v","e"]
fileprivate let k_styleName:String = "if pic equal vardDate"

/*: "liveHeartBeat" :*/
fileprivate let kRemarkVideoContent:String = "picture manager gameliveHeart"
fileprivate let kCellName:[Character] = ["B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let k_requestName:[Character] = ["\u{0001f517}"," ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let kFatalTitle:[Character] = ["T","h","e"," ","u","s","e","r"," ","l","o","g","s"," ","o"]
fileprivate let kEndPathPriceValue:String = "viewt"

/*: "SOCKET  :*/
fileprivate let k_theValue:String = "SOCKET image end voice float"

/*: 重连. 重试次数： :*/
fileprivate let k_imageText:String = "重\u{8fde}. 重试"
fileprivate let kMeContent:[Character] = ["\u{6b21}","数","\u{ff1a}"]

/*: , 地址： :*/
fileprivate let kSharedValue:String = ", 地址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let k_scaleData:String = "SphotoK"
fileprivate let k_pathValue:String = "接\u{6b63}常 不"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let k_resultData:[Character] = ["S","O","C","K","E","T"," ","正","\u{5728}","连","\u{63a5}"," "]
fileprivate let k_toValue:String = "不需要重新keep."

/*: "&resume=1" :*/
fileprivate let kSharedName:String = "greet height&resu"
fileprivate let k_succeedName:String = "me=1inside phone true pop"

/*: "&lang= :*/
fileprivate let k_topValue:[Character] = ["&"]
fileprivate let k_progressData:String = "price time view returnlang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let k_withContent:[Character] = ["S","O","C","K","E","T"," ","\u{5f00}","始","\u{8fde}","接"]
fileprivate let k_visibleContent:[Character] = [","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let kRemoveData:[Character] = ["o","n","N","e","t","D","i","s","a"]
fileprivate let kCardTitle:String = "bvideod"

/*: "errno" :*/
fileprivate let k_infoContent:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let k_viewData:String = "mainta"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let k_lengthName:String = "SOlimit"
fileprivate let k_collectionName:String = "view path background app let保活cmd"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let k_actionText:[UInt8] = [0x6c,0x70,0x7c,0x74,0x7a,0x6b,0x1f,0x4b,0x4d,0x4b,0x5c,0xdb,0x80,0xa2,0xd9,0x8b,0x84,0x5c,0x52,0x5b,0x5,0x1f,0x5c,0x5e,0x53,0x53,0x77,0x5a,0x5e,0x4d,0x4b,0x5d,0x5a,0x5e,0x4b,0x11]

private func videoStyleBy(now num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let k_succeedLeadingValue:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let k_playerName:String = "onPtext import identity return color"

/*: "msg" :*/
fileprivate let k_viewModelTitle:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let kModePremiumTitle:String = "🔗 处理"
fileprivate let kThemeValue:String = "Name:return view year request level"

/*: , errNo: :*/
fileprivate let k_listText:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let kShowData:String = "as target, msg:"

/*: , data: :*/
fileprivate let k_requestBorderData:[Character] = [","]
fileprivate let k_rawText:[Character] = [" ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let k_modelName:String = "cropO"
fileprivate let k_cancelData:String = "tag item image areaCKET 接"
fileprivate let kTimeValue:String = "cmdName:gift remove equal cell"

/*: "onReceivedGift" :*/
fileprivate let kCalledName:String = "onReceiuser equal title bring"
fileprivate let kConversationValue:[Character] = ["v","e"]
fileprivate let k_awakeMediumMakeValue:[Character] = ["d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let k_rowTitle:String = "else text action labonVi"
fileprivate let k_appText:String = "user"

/*: "onRequestCall" :*/
fileprivate let k_videoValue:[Character] = ["o","n","R","e","q"]
fileprivate let kNumberValue:[Character] = ["u"]
fileprivate let kNameText:String = "estCalledge image frame"

/*: "onCancelCall" :*/
fileprivate let k_equalMagnitudeText:String = "onCatab line"
fileprivate let k_imageViewName:[Character] = ["a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let k_nameValue:[Character] = ["o","n"]
fileprivate let kMainTitle:[Character] = ["R","e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let kTargetData:String = "onAcceframe at let source"

/*: "onStopCall" :*/
fileprivate let k_onCellContent:String = "image"
fileprivate let k_theText:String = "method self mainnSto"

/*: "freeCallTimes" :*/
fileprivate let k_belowValue:[Character] = ["f","r","e","e","C","a","l","l","T","i","m","e"]
fileprivate let k_filterData:String = "frame"

/*: "mfCoin" :*/
fileprivate let k_byValue:[Character] = ["m"]
fileprivate let kPathName:String = "super view err heightfCoin"

/*: "onMissCoin" :*/
fileprivate let kLayerText:[Character] = ["o","n","M","i","s","s","C","o"]
fileprivate let k_labelData:String = "istyle"

/*: "acceptCall" :*/
fileprivate let k_cellData:String = "acclevelp"
fileprivate let kTableTitle:[Character] = ["t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let k_titleData:[Character] = ["s","t","o"]
fileprivate let kAddToGiftTitle:String = "block path readpCall"

/*: "requestCall" :*/
fileprivate let k_touchTitle:[Character] = ["r","e","q","u","e","s","t"]
fileprivate let k_transformData:[Character] = ["C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let kPathCustomValue:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let k_topPlayValue:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let kSizeName:[UInt8] = [0xec,0xda,0xff,0x7,0xfa,0x5,0xf6,0xb1,0xa,0x0,0x6,0xb1,0x5,0x0,0xb1,0xf2,0xb1,0x7,0x0,0xfa,0xf4,0xf6,0xb1,0xf4,0xf2,0xfd,0xfd,0xbf,0xbf,0xbf,0xee]

fileprivate func liveError(err num: UInt8) -> UInt8 {
    let value = Int(num) - 145
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let k_stopData:String = "uimedia"

/*: "nickname" :*/
fileprivate let k_contentData:String = "niccollectionna"
fileprivate let kPlanName:[Character] = ["m","e"]

/*: "headPic" :*/
fileprivate let kArrayText:String = "type collection tool if phoneheadPic"

/*: "onCallResume" :*/
fileprivate let k_requestFromValue:[Character] = ["o","n","C","a"]
fileprivate let kBottomData:String = "llResumethat self color table view"

/*: "cancelCall" :*/
fileprivate let kOccurValue:[Character] = ["c","a","n","c","e","l","C"]
fileprivate let kTitleFrameValue:String = "ALL"

/*: "refuseCall" :*/
fileprivate let k_objectTitle:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let k_rangeData:String = "onCallMoerror bit"
fileprivate let kActualValue:String = "tivequal"

/*: "onLiveWarning" :*/
fileprivate let kSumName:String = "onLibar view right"

/*: "reason" :*/
fileprivate let kCellStyleData:String = "reasto"
fileprivate let k_layerData:[Character] = ["n"]

/*: "time" :*/
fileprivate let k_timeCountValue:String = "tgifte"

/*: "onStopLive" :*/
fileprivate let k_beanName:String = "manager data make top addonSt"
fileprivate let k_endAppValue:String = "page contentopLive"

/*: "onSendProgramGift" :*/
fileprivate let kMakeText:String = "name model as grouponSend"
fileprivate let kSizeTheContent:String = "Progrerror gift frame"

/*: "onGameFs" :*/
fileprivate let k_removeValue:String = "top time self make dataonGameF"
fileprivate let k_bottomValue:String = "location"

/*: "broadStatus" :*/
fileprivate let kWhiteText:String = "lock back return if elsebroadS"
fileprivate let kStartTitle:String = "buttonabuttonus"

/*: "onCallChatMsg" :*/
fileprivate let k_indexNameValue:String = "onCallCas make self"
fileprivate let kLeadingTermsName:String = "name"
fileprivate let k_textPostData:[Character] = ["a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let kViewBlockErrName:String = "onNotimodel var if"
fileprivate let kValueData:String = "if let to hidden useronPopu"

/*: "onUserBlock" :*/
fileprivate let k_playerValue:String = "onUserlet false true to model"
fileprivate let k_existMText:String = "Blockin model"

/*: "freeCallWin" :*/
fileprivate let k_detailData:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let kActiveName:[Character] = ["l","W","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SendSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_licenseVersionData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_barData = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_maxData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_domainTitle = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_finishData = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_onCountimateTitle = (kEffectValue.replacingOccurrences(of: "cell", with: "K") + "T_ADD" + String(kGiftData.suffix(4)) + String(k_toName))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_bottomValue = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_keyContent = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_giftTitle = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_clickValue = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_viewContent = NSNotification.Name(rawValue: String(bytes: k_whiteName.map{observerBottom(list: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_userValue = NSNotification.Name(rawValue: String(bytes: kEqualMediumValue.map{$0^207}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol DirectorObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func anonymous(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol DataObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func underMatch(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func toTime(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol UserObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func aggregationButton(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func outsideEqual(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func vacuumBagData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func statUp(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol RecordObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func levelBar(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func tableData(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func modelMessage(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class SendSocketDelegate: NSObject {
	var markContent: String = "live"
	var constraintArray: [AnyHashable] = []
	var startShouldTitle: String = "location"
	var nameArray: [AnyHashable] = []

    //: enum SocketRetryType: String {
    enum SocketEventVariationGift: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TextLanguageManager.shared.direction == .rightToLeft ? 0 : kLet_errData,
        //: y: StatusBarHeight+190
        y: kLet_topData + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TextLanguageManager.shared.direction == .rightToLeft ? 0 : kLet_errData,
        //: y: StatusBarHeight+190
        y: kLet_topData + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TextLanguageManager.shared.direction == .rightToLeft ? 0 : kLet_errData,
        //: y: StatusBarHeight+500
        y: kLet_topData + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: DirectorObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: DataObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: UserObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: RecordObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = SendSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
 
		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { 
		
		return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(objectReachability(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(magnitudeimate),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(k_labData.suffix(5)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AppTalkingApplication.shared as! AppTalkingApplication).blockTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(kMakeData.suffix(5)) + "king"))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AppTalkingApplication.shared as! AppTalkingApplication).blockTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension SendSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: JumpLogTool.lineLoad(msg: "SOCKET 连接成功.")
        JumpLogTool.lineLoad(msg: (kLayerSizeData.replacingOccurrences(of: "time", with: "C") + String(k_areaValue)))
        //: sendPing()
        buttonPing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_giftContent,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_onCountimateTitle)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        stopView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: JumpLogTool.lineLoad(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        JumpLogTool.lineLoad(msg: (k_imageValue.replacingOccurrences(of: "content", with: "S") + "ET 连接\u{5931}" + String(k_userViewData.suffix(8)) + kFailName.replacingOccurrences(of: "below", with: ":")) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = RowReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = RowReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_domainTitle {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = sumeraction(isRetry: true)
            //: JumpLogTool.lineLoad(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            JumpLogTool.lineLoad(msg: (kItemAreaText.replacingOccurrences(of: "photo", with: "KE") + "T \u{5931}败") + "\(kLet_domainTitle)" + (kAppData.capitalized + "\u{8fc7}Sock" + kPopTitle + String(k_hiddenName)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.imageDownSort(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.barrelhouse(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: JumpLogTool.lineLoad(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        JumpLogTool.lineLoad(msg: (kLabData.replacingOccurrences(of: "need", with: "S") + k_locationText + String(kWhiteName)) + "\(code)" + (String(k_valueName.prefix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.fileOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_clickValue {
            //: return
            return
        }
        //: if let status = RowReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = RowReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.imageDownSort(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func sumeraction(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = RowReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = RowReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = wharf()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_onCountimateTitle)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = RowReactiveCompatible.share.loginUid
        let uid = RowReactiveCompatible.share.loginUid
        //: let sid = RowReactiveCompatible.share.loginSessionId
        let sid = RowReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(k_normalName.prefix(6))) + "\(sid)" + (String(kModelName)) + "\(uid)" + (String(kHideValueName) + String(k_nameEndText.suffix(2)) + String(k_screenText.prefix(7))) + "\(kLet_tableStatusOutContent)" + (String(k_borderContent.suffix(5)) + "ageId=") + "\(kLet_inviteValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func wharf() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_listValue ? [(String(k_giftTextProductTitle.suffix(6)) + "ver.") + "\(kLet_intimateData)" + (String(k_dataInsideName) + kPlayerName.replacingOccurrences(of: "size", with: "6"))] : [(String(k_resultValue) + k_upTitle.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func objectReachability(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: JumpLogTool.lineLoad(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            JumpLogTool.lineLoad(msg: String(bytes: k_meetingValue.map{sizeLet(data: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.fileOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.imageDownSort(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func magnitudeimate() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.imageDownSort(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.getOutOf()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func buttonPing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_licenseVersionData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.castToDataFormattingSub(info: [(k_errorTableName.replacingOccurrences(of: "add", with: "d")): (kPageName.replacingOccurrences(of: "max", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func gesturePing() {
        //: endTalkingPing()
        startEventPing()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_barData, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.belowResume()?.isKind(of: LastViewController.self) == false && self.belowResume()?.isKind(of: JumpErrorDelegate.self) == false) && (self.currTalkingVC?.isKind(of: LastViewController.self) == false && self.currTalkingVC?.isKind(of: JumpErrorDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.castToDataFormattingSub(info: [(k_errorTableName.replacingOccurrences(of: "add", with: "d")): (String(kBackgroundName.prefix(5)) + "eartbe" + k_parentData.replacingOccurrences(of: "message", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func startEventPing() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func getOutOf() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(kTopData) + String(k_styleName.suffix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_replaceCellData {
                //: RowReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                RowReactiveCompatible.share.programmeUser(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func indicatorPing() {
        //: endLivePing()
        eigenvalueOfAMatrix()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_maxData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.castToDataFormattingSub(info: [(k_errorTableName.replacingOccurrences(of: "add", with: "d")): (String(kRemarkVideoContent.suffix(9)) + String(kCellName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func eigenvalueOfAMatrix() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func stopView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = PatronymicThen()
        //: let keyWindow = NameMacroDefine.getWindow()
        let keyWindow = NameMacroDefine.playWindow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func levelView() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard WithNeighbourReactiveCompatible.clickEqual().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard EventManager.bracketOut().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func fileOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(k_requestName)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_clickValue, reason: (String(kFatalTitle) + kEndPathPriceValue.replacingOccurrences(of: "view", with: "u")))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        startEventPing()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            levelView()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func imageTa() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = SendSocketDelegate.shared.sumeraction(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.imageDownSort(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func imageDownSort(timeInterval: TimeInterval, type: SocketEventVariationGift) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: JumpLogTool.lineLoad(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            JumpLogTool.lineLoad(msg: (String(k_theValue.prefix(7))) + "\(type.rawValue)" + (k_imageText + String(kMeContent)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.bridgeLineSocketHandle(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func bridgeLineSocketHandle(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: JumpLogTool.lineLoad(msg: "SOCKET 连接正常 不需要重新连接.")
                JumpLogTool.lineLoad(msg: (k_scaleData.replacingOccurrences(of: "photo", with: "OC") + "ET 连" + k_pathValue + "需\u{8981}重新连接."))
                //: } else {
            } else {
                //: JumpLogTool.lineLoad(msg: "SOCKET 正在连接 不需要重新连接.")
                JumpLogTool.lineLoad(msg: (String(k_resultData) + k_toValue.replacingOccurrences(of: "keep", with: "连接")))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kSharedName.suffix(5)) + String(k_succeedName.prefix(4))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(k_topValue) + String(k_progressData.suffix(5))) + "\(GiftViewReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        fileOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_finishData
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: JumpLogTool.lineLoad(msg: "SOCKET 开始连接, 地址：\(newStr).")
        JumpLogTool.lineLoad(msg: (String(k_withContent) + String(k_visibleContent)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func castToDataFormattingSub(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if RowReactiveCompatible.share.reachability?.connection == nil || RowReactiveCompatible.share.reachability?.connection == .unavailable {
        if RowReactiveCompatible.share.reachability?.connection == nil || RowReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(k_errorTableName.replacingOccurrences(of: "add", with: "d")): (String(kRemoveData) + kCardTitle.replacingOccurrences(of: "video", with: "le")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(k_infoContent)): "\(kLet_giftTitle)",
                                       //: "data": [:]]
                                       (k_viewData.replacingOccurrences(of: "main", with: "da")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            barrelhouse(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(k_errorTableName.replacingOccurrences(of: "add", with: "d"))] as? String == (kPageName.replacingOccurrences(of: "max", with: "pi")) {
                //: JumpLogTool.lineLoad(msg: "SOCKET 连接保活cmd: ping.")
                JumpLogTool.lineLoad(msg: (k_lengthName.replacingOccurrences(of: "limit", with: "CK") + "ET \u{8fde}接" + String(k_collectionName.suffix(5)) + ": ping."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(k_errorTableName.replacingOccurrences(of: "add", with: "d"))] as? String == (String(kBackgroundName.prefix(5)) + "eartbe" + k_parentData.replacingOccurrences(of: "message", with: "t")) {
                //: JumpLogTool.lineLoad(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                JumpLogTool.lineLoad(msg: String(bytes: k_actionText.map{videoStyleBy(now: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(k_errorTableName.replacingOccurrences(of: "add", with: "d"))] as? String == (String(kRemarkVideoContent.suffix(9)) + String(kCellName)) {
                //: JumpLogTool.lineLoad(msg: "SOCKET live保活cmd: liveHeartBeat.")
                JumpLogTool.lineLoad(msg: String(bytes: k_succeedLeadingValue.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(k_errorTableName.replacingOccurrences(of: "add", with: "d"))] as? String == (kPageName.replacingOccurrences(of: "max", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(k_errorTableName.replacingOccurrences(of: "add", with: "d")): (String(k_playerName.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(k_infoContent)): "\(kLet_bottomValue)",
                                           //: "data": [:]]
                                           (k_viewData.replacingOccurrences(of: "main", with: "da")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                barrelhouse(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.imageDownSort(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func barrelhouse(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(k_errorTableName.replacingOccurrences(of: "add", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(k_infoContent))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(k_viewModelTitle))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(k_viewData.replacingOccurrences(of: "main", with: "da"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (kModePremiumTitle.capitalized + "消\u{606f}\u{ff1a}cmd" + String(kThemeValue.prefix(5))) + "\(cmdName)" + (String(k_listText)) + "\(errorNo)" + (String(kShowData.suffix(6))) + "\(msg)" + (String(k_requestBorderData) + String(k_rawText)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        valueWith(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: JumpLogTool.lineLoad(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            JumpLogTool.lineLoad(msg: (k_modelName.replacingOccurrences(of: "crop", with: "S") + String(k_cancelData.suffix(6)) + "收\u{5f02}常消息\u{ff1a}" + String(kTimeValue.prefix(8))) + "\(cmdName)" + (String(k_listText)) + "\(errorNo)" + (String(kShowData.suffix(6))) + "\(msg)" + (String(k_requestBorderData) + String(k_rawText)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: RowReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                RowReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !RowReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !RowReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                BeanTimeThen.shared.beardEqual()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.anonymous(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kCalledName.prefix(7)) + String(kConversationValue) + String(k_awakeMediumMakeValue)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_barTitle, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(k_rowTitle.suffix(4)) + "deoMatc" + k_appText.replacingOccurrences(of: "user", with: "h")) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.underMatch(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(k_videoValue) + String(kNumberValue) + String(kNameText.prefix(7))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.toTime(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(k_equalMagnitudeText.prefix(4)) + "ncelC" + String(k_imageViewName)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(k_nameValue) + String(kMainTitle)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(kTargetData.prefix(6)) + "ptCall") { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            gesturePing()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.tableData(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (k_onCellContent.replacingOccurrences(of: "image", with: "o") + String(k_theText.suffix(4)) + "pCall") { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            startEventPing()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(k_belowValue) + k_filterData.replacingOccurrences(of: "frame", with: "s"))] as? Int {
                //: RowReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                RowReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: RowReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RowReactiveCompatible.share.loginUserMode.mf_coin = json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(String(k_byValue) + String(kPathName.suffix(5)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_errorData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AppTalkingApplication.shared as! AppTalkingApplication).animateBeing()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(kLayerText) + k_labelData.replacingOccurrences(of: "style", with: "n")) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.statUp(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (k_cellData.replacingOccurrences(of: "level", with: "e") + String(kTableTitle)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            gesturePing()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.aggregationButton(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(k_titleData) + String(kAddToGiftTitle.suffix(5))) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            startEventPing()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.outsideEqual(data: data)
            // 更新余额
            //: RowReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RowReactiveCompatible.share.loginUserMode.mf_coin = json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(String(k_byValue) + String(kPathName.suffix(5)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            beforeTo(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_errorData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AppTalkingApplication.shared as! AppTalkingApplication).animateBeing()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(k_touchTitle) + String(k_transformData)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_viewContent, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: RowReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                RowReactiveCompatible.share.programmeUser(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(kTopData) + String(k_styleName.suffix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: kPathCustomValue.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(String(k_topPlayValue))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kSizeName.map{liveError(err: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ProvenanceDelegateHelper.checked(uid: String(json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(k_stopData.replacingOccurrences(of: "media", with: "d"))].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(k_contentData.replacingOccurrences(of: "collection", with: "k") + String(kPlanName))] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data[(String(kArrayText.suffix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ProvenanceDelegateHelper.shared.breakApartAfter()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(k_requestFromValue) + String(kBottomData.prefix(8))) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            gesturePing()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(k_requestFromValue) + String(kBottomData.prefix(8)))] = "1"
            //: RowReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            RowReactiveCompatible.share.transitionShow(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (AppTalkingApplication.shared as! AppTalkingApplication).blockTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(kOccurValue) + kTitleFrameValue.lowercased()) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            startEventPing()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.vacuumBagData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ProvenanceDelegateHelper.strike(identifier: String(json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(k_stopData.replacingOccurrences(of: "media", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ProvenanceDelegateHelper.shared.encounterBgm()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(k_objectTitle)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            startEventPing()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.levelBar(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(k_rangeData.prefix(8)) + kActualValue.replacingOccurrences(of: "equal", with: "a") + "tion") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.modelMessage?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(kSumName.prefix(4)) + "veWarning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            WithNeighbourReactiveCompatible.tipEqual(content: msg, reason: json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(kCellStyleData.replacingOccurrences(of: "to", with: "o") + String(k_layerData))].stringValue, time: json[(k_viewData.replacingOccurrences(of: "main", with: "da"))][(k_timeCountValue.replacingOccurrences(of: "gift", with: "im"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(k_beanName.suffix(4)) + String(k_endAppValue.suffix(6))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            WithNeighbourReactiveCompatible.clickEqual().nameObjectNeed()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(kMakeText.suffix(6)) + String(kSizeTheContent.prefix(5)) + "amGift") { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.viewLog(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(k_removeValue.suffix(7)) + k_bottomValue.replacingOccurrences(of: "location", with: "s")) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(kWhiteText.suffix(6)) + kStartTitle.replacingOccurrences(of: "button", with: "t"))] as? Int ?? -1
            //: if RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_userValue, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_barTitle, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_userValue, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(k_indexNameValue.prefix(7)) + kLeadingTermsName.replacingOccurrences(of: "name", with: "h") + String(k_textPostData)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TalkingDetailDanmuManager.observerShared().rite(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(kViewBlockErrName.prefix(6)) + "ficati" + String(kValueData.suffix(6)) + "pWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            BeanTimeThen.shared.comeInDownBackground(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_playerValue.prefix(6)) + String(k_existMText.prefix(5))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            WithNeighbourReactiveCompatible.clickEqual().nameObjectNeed()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            EventManager.bracketOut().wire()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_ageValue, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension SendSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func valueWith(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(k_touchTitle) + String(k_transformData)) || cmdName == (String(k_videoValue) + String(kNumberValue) + String(kNameText.prefix(7))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if WithNeighbourReactiveCompatible.clickEqual().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_systemValue = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                WithNeighbourReactiveCompatible.labCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                WithNeighbourReactiveCompatible.clickEqual().nameObjectNeed()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_systemValue == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(kOccurValue) + kTitleFrameValue.lowercased()) || cmdName == (String(k_equalMagnitudeText.prefix(4)) + "ncelC" + String(k_imageViewName)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(k_objectTitle)) || cmdName == (String(k_nameValue) + String(kMainTitle)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(k_titleData) + String(kAddToGiftTitle.suffix(5))) || cmdName == (k_onCellContent.replacingOccurrences(of: "image", with: "o") + String(k_theText.suffix(4)) + "pCall")
        {
            //: NEED_RESTORE_LIVE = false
            kLet_systemValue = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = BeanReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                WithNeighbourReactiveCompatible.clickEqual().consumerDetail()
            }
            //: alert.show()
            alert.countShow()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func beforeTo(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(k_belowValue) + k_filterData.replacingOccurrences(of: "frame", with: "s"))] as? Int {
            //: RowReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            RowReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(k_detailData) + String(kActiveName))] as? Bool
        //: if freeCallWin == true && RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = VideoAppObjectProtocol()
            //: view.show()
            view.viewColor()
            //: view.setShowData(data: data)
            view.tableLine(data: data)
        }
    }
}
