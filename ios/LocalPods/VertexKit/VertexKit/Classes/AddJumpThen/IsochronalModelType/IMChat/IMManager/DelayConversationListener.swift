
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let k_ofText:[Character] = ["I","M","退","出","登","录","\u{5931}","\u{8d25}","\u{ff0c}","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let kImageName:String = ", desc:if home my for view"

/*: ." :*/
fileprivate let k_userTitle:String = "."

/*: "IM登录失败，code: :*/
fileprivate let k_menuValue:[Character] = ["I","M","登","\u{5f55}","失","\u{8d25}"]
fileprivate let k_withTitle:[Character] = ["\u{ff0c}"]
fileprivate let k_countName:String = "let view classcode:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let k_pathTitle:String = "IM获取\u{672a}读消"
fileprivate let k_modelData:[Character] = ["息","总","数","失","败","\u{ff0c}","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let kPopPushContent:String = "ccolors"

/*: "badNumber" :*/
fileprivate let k_clickNameData:[UInt8] = [0xb2,0xb1,0xb4,0x9e,0xa5,0xbd,0xb2,0xb5,0xa2]

private func toCan(video num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "plaza" :*/
fileprivate let k_heTitle:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let kFromClickData:String = "roomNugift to"
fileprivate let kAddTitle:[Character] = ["m"]

/*: "num" :*/
fileprivate let kUpMethodTitle:String = "NUM"

/*: "onlyOneRoomId" :*/
fileprivate let kViewSizeData:String = "onstreamy"
fileprivate let k_pushValue:String = "OneRopush layer print"

/*: "LIVE-CHAT" :*/
fileprivate let kViewData:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let k_matterVideoName:String = "PequalTY"

/*: "extra" :*/
fileprivate let kMakeValue:String = "extrpair"

/*: "msgType" :*/
fileprivate let k_modelName:String = "msgTypfile about block"
fileprivate let k_upName:String = "E"

/*: "newPkgGift" :*/
fileprivate let kAllJumpDataName:String = "result of type tonewPkg"
fileprivate let k_colorTitle:String = "Gifttitle manager"

/*: "MF:ChatMentionMsg" :*/
fileprivate let kThumbValue:String = "link selected statusMF:Cha"
fileprivate let kValueTitle:String = "main icon selected hiddenionMsg"

/*: "uid" :*/
fileprivate let k_modelAddTitle:[UInt8] = [0x4a,0x56,0x5b]

private func selfIndexBottom(count num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "onlineStatus" :*/
fileprivate let kSucceedValue:[UInt8] = [0x6,0x5,0x3,0x0,0x5,0xfc,0xea,0xb,0xf8,0xb,0xc,0xa]

fileprivate func cellCollection(index num: UInt8) -> UInt8 {
    let value = Int(num) + 105
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let kFlushValue:[UInt8] = [0xa8,0xb2,0x8f,0xa4,0xb6,0x94,0xb2,0xa4,0xb3]

private func imageRequest(manager num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "c2c_ :*/
fileprivate let kBeautyText:String = "target if rightc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let kScaleText:String = "IM\u{83b7}取会"
fileprivate let kObjectName:String = "search data month imageconvId"
fileprivate let kByValue:String = "bar"

/*: , code: :*/
fileprivate let kPlaceText:String = ", "
fileprivate let kLabelTitle:[Character] = ["c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let k_valueShakeTitle:String = "IM插入else name"
fileprivate let kTotalValue:String = "rlevel"
fileprivate let k_momentTitle:String = "etId:add for"

/*: "changeDirection" :*/
fileprivate let kBlockLayerDeleteValue:String = "chaequal"
fileprivate let kTableTitle:[Character] = ["g","e","D","i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let k_mediumTaskPathValue:String = "rYpath view"
fileprivate let k_nameData:String = "self bottom hiddenMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let kFrameData:String = "size page share ifGJ:C"
fileprivate let kChangeValue:String = "stosize"

/*: "IM获取会话列表失败，code: :*/
fileprivate let k_keyTitle:String = "IM获\u{53d6}\u{4f1a}"
fileprivate let k_nameValue:String = "败\u{ff0c}code:"

/*: "msgInfo" :*/
fileprivate let k_colorName:[Character] = ["m","s","g","I","n","f"]
fileprivate let kRequestValue:String = "data"

/*: "needFold" :*/
fileprivate let kEqualName:String = "main selfneedFo"
fileprivate let kModelTitle:[Character] = ["l","d"]

/*: "group_ :*/
fileprivate let kMaxContent:[Character] = ["g","r","o","u","p"]
fileprivate let k_barName:[Character] = ["_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let kStatusName:[Character] = ["I","M","\u{5220}","除","会","话","失","\u{8d25}"]
fileprivate let k_modelValue:String = "make event，co"
fileprivate let kModeValue:[Character] = ["n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let k_interestValue:String = "IM\u{7f6e}顶"
fileprivate let k_finishData:String = "language video data colornvId:"

/*: "SDK 正在连接到服务器" :*/
fileprivate let kExtraValue:String = "view model selfSDK "
fileprivate let kViewValue:[Character] = ["到","服","务","\u{5668}"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let k_maybeValue:[Character] = ["S","D","K"," ","已","经","成","功","连"]
fileprivate let k_stateData:String = "接\u{5230}服\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let kUseContent:String = "IM连接\u{670d}"
fileprivate let k_executeName:[Character] = ["务","器","\u{5931}","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let k_barText:String = "model model new, error"
fileprivate let k_borderName:[Character] = [":"]

/*: "IM账号被挤下线。" :*/
fileprivate let k_transactionValue:String = "size collection topIM账号被挤下线"
fileprivate let k_upValue:[Character] = ["。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let k_toData:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let k_whiteStatusName:String = "Offlifor effect video"
fileprivate let k_logContent:String = "allificaall"
fileprivate let k_atName:[Character] = ["i","o","n"]

/*: "Cancel" :*/
fileprivate let kToAddContent:String = "model on item gift dataCancel"

/*: "Re Login" :*/
fileprivate let kPushReadyValue:String = "Reself data center"
fileprivate let k_shareNowModelTitle:String = " Loginview priority user style"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DelayConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol PanelObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func areaReading(data: [PhaticCommunicationThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func dataQuantity(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func communication(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum KeyDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class DelayConversationListener: NSObject {
@objcMembers public class DelayConversationListener: NSObject {
    //: public static let shared = DelayConversationListener()
    public static let shared = DelayConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [PhaticCommunicationThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [PhaticCommunicationThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [PhaticCommunicationThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [PhaticCommunicationThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [PhaticCommunicationThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: PhaticCommunicationThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.toInitialization()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func audience() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kLet_blockAssetValue, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func toInitialization() {
        //: func__removeAllDelegate()
        clickDisembowel()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.array()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func array() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = PhaticCommunicationThen.labType(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = PhaticCommunicationThen.labType(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func titleAdd() -> [PhaticCommunicationThen] {
        // 审核模式，在消息列表移除官方客服
        //: if RowReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if RowReactiveCompatible.share.appStatus == SkinSubscriptType.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == NameMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == NameMacroDefine.finishShared() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension DelayConversationListener {
extension DelayConversationListener {
    //: func func__removeAllDelegate() {
    func clickDisembowel() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func store(_ delegate: PanelObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func rootImageDelegate(_ delegate: PanelObjectProtocol) {
        //: self.func__invokeDelegate {
        self.titleClickDelegate {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func titleClickDelegate(_ invocation: (PanelObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! PanelObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension DelayConversationListener {
extension DelayConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func down() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: JumpLogTool.lineLoad(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            JumpLogTool.lineLoad(msg: (String(k_ofText)) + "\(code)" + (String(kImageName.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.toInitialization()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func equalCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = RowReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = RowReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = RowReactiveCompatible.share.loginUserMode.userID
        let uidStr = RowReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.titleBy()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: JumpLogTool.lineLoad(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            JumpLogTool.lineLoad(msg: (String(k_menuValue) + String(k_withTitle) + String(k_countName.suffix(5))) + "\(code)" + (String(kImageName.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func makeAdd() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || RowReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || RowReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.appearShow(showMsg: kLet_messageScreenTitle)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension DelayConversationListener {
extension DelayConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func titleBy() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: JumpLogTool.lineLoad(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            JumpLogTool.lineLoad(msg: (k_pathTitle + String(k_modelData)) + "\(code)" + (String(kImageName.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func feedback() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: SourceRequestTool.req_sysMessageList { _, result in
        SourceRequestTool.outsideOf { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(kPopPushContent.replacingOccurrences(of: "color", with: "ru") + "hBadge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kLet_changeData, object: self, userInfo: [String(bytes: k_clickNameData.map{toCan(video: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func custom(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: SourceRequestTool.req_getChatRoomInfo { succeed, result, _ in
        SourceRequestTool.addCoverCompletion { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(k_heTitle))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(kFromClickData.prefix(6)) + String(kAddTitle))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = PhaticCommunicationThen.labType(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(kUpMethodTitle.lowercased())] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(kViewSizeData.replacingOccurrences(of: "stream", with: "l") + String(k_pushValue.prefix(5)) + "omId")] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension DelayConversationListener: V2TIMAdvancedMsgListener {
extension DelayConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(kViewData))) || msg.groupID.contains((k_matterVideoName.replacingOccurrences(of: "equal", with: "AR") + "-CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            TalkingReactiveCompatible.removeShared().showImage(msg: msg)
            //: return
            return
        }
        //: if msg.sender == RowReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == RowReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(kMakeValue.replacingOccurrences(of: "pair", with: "a"))] as! [AnyHashable: Any]
        //: let msgModel = PanAwakeModel(dic: extraDic)
        let msgModel = PanAwakeModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kLet_matchData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(k_modelName.prefix(6)) + k_upName.lowercased())] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kAllJumpDataName.suffix(6)) + String(k_colorTitle.prefix(4))) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_streamName, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: SmallnessMsgTable.db_inserVoiceMsgToDB(dic)
                SmallnessMsgTable.nameDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.welt(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            AddJumpThen.shared.voiceSignature(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            AddJumpThen.shared.atDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(kThumbValue.suffix(6)) + "tMent" + String(kValueTitle.suffix(6))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ExaggerateChatManager.share.userStart(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: IndexViewThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: IndexViewThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: IndexViewThen.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: IndexViewThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != NameMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != NameMacroDefine.have() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.titleClickDelegate { $0.communication?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            MusicThen.shared.appAdd(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: k_modelAddTitle.map{selfIndexBottom(count: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: kSucceedValue.map{cellCollection(index: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: kFlushValue.map{imageRequest(manager: $0)}, encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_sessionValue,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: MusicInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            MusicInfoManager.priceInfo(with: receipt.userID, time: receipt.timestamp)
            //: IndexViewThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            IndexViewThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kLet_partyValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func likeContent(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.equalWith(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(kBeautyText.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.fault(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.titleList(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                JumpLogTool.lineLoad(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func equalWith(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = RowReactiveCompatible.share.loginUserMode.userID
        let senderStr = RowReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                JumpLogTool.lineLoad(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func welt(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(kBlockLayerDeleteValue.replacingOccurrences(of: "equal", with: "n") + String(kTableTitle))].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && MiraclePushManager.share.sectionVc()!.isKind(of: DoingdDoorBeginViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kLet_infoContent) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kLet_infoContent)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension DelayConversationListener: V2TIMConversationListener {
extension DelayConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.fault(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.titleList(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.fault(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.titleList(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: RowReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        RowReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.titleClickDelegate {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.dataQuantity?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func titleList(conVersationList: [PhaticCommunicationThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if MusicInfoManager.shared().needReqUidsSet != nil {
            if MusicInfoManager.nation().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: MusicInfoManager.shared().needReqUidsSet!.formUnion(set)
                MusicInfoManager.nation().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.titleClickDelegate {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.areaReading?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func tip(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [PhaticCommunicationThen] = []

        // V2TIMConversation->PhaticCommunicationThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = PhaticCommunicationThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.upward(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [PhaticCommunicationThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = DeviceInfoTable.startWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.convCrush(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension DelayConversationListener {
extension DelayConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func deadlineFilter(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.tip(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.titleList(conVersationList: arr as? [PhaticCommunicationThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: JumpLogTool.lineLoad(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            JumpLogTool.lineLoad(msg: (k_keyTitle + "话列表失" + k_nameValue) + "\(code)" + (String(kImageName.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func titleAcross(targetId: String) -> PhaticCommunicationThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func fault(convList: [V2TIMConversation]) -> [PhaticCommunicationThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [PhaticCommunicationThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = PhaticCommunicationThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.upward(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.convCrush(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func convCrush(convModel: PhaticCommunicationThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.array()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: KeyDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - RowReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - RowReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = MusicInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = MusicInfoManager.totalerval(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(kMakeValue.replacingOccurrences(of: "pair", with: "a"))][(String(k_colorName) + kRequestValue.replacingOccurrences(of: "data", with: "o"))][(String(kEqualName.suffix(6)) + String(kModelTitle))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.textTopType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.textTopType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func textTopType(convList: [PhaticCommunicationThen], dataType: KeyDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func submit(targetId: String, dataType: KeyDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(kBeautyText.suffix(4))) + "\(targetId)" : (String(kMaxContent) + String(k_barName)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: MusicInfoManager.db_removeRecord(with: targetId)
            MusicInfoManager.greetErase(with: targetId)
            //: SmallnessMsgTable.db_deleteVoiceMsg(RowReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            SmallnessMsgTable.toCell(RowReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.titleBy()

            //: } fail: { code, desc in
        } fail: { code, desc in
            JumpLogTool.lineLoad(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func stochasticProcess(convModel: PhaticCommunicationThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            JumpLogTool.lineLoad(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.textTopType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.textTopType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension DelayConversationListener: V2TIMSDKListener {
extension DelayConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(kExtraValue.suffix(4)) + "正在连接" + String(kViewValue)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(k_maybeValue) + k_stateData))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: JumpLogTool.lineLoad(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        JumpLogTool.lineLoad(msg: (kUseContent + String(k_executeName)) + "\(code)" + (String(k_barText.suffix(7)) + String(k_borderName)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: JumpLogTool.lineLoad(msg: "IM账号被挤下线。")
        JumpLogTool.lineLoad(msg: (String(k_transactionValue.suffix(8)) + String(k_upValue)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        SendSocketDelegate.shared.fileOut(userLoginOut: true)
        //: DelayConversationListener.shared.isConnection = false
        DelayConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (AppTalkingApplication.shared as! AppTalkingApplication).makeIndex()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: k_toData.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = LibraryAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.ar.rawValue || GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            GiftViewReactiveCompatible.share.interfaceLang == LengthTransformable.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.moleculeClick(fontSize: 13)
            config.rightFont = UIFont.moleculeClick(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        CurrentThen.magnitudeimateCan()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        CurrentThen.magnitudeimateCan(title: (String(k_whiteStatusName.prefix(5)) + "ne No" + k_logContent.replacingOccurrences(of: "all", with: "t") + String(k_atName)).localized, message: msg, leftBtnTitle: (String(kToAddContent.suffix(6))).localized, rightBtnTitle: (String(kPushReadyValue.prefix(2)) + String(k_shareNowModelTitle.prefix(6))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            WithNeighbourReactiveCompatible.clickEqual().nameObjectNeed()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            EventManager.bracketOut().wire()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            LastLengthRequestTool.picture { t in
                //: ProgressHUD.dismiss()
                ViewBeginReactiveCompatible.capability()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_ageValue, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            LastLengthRequestTool.nameView { t in
                //: if t {
                if t {
                    //: DelayConversationListener.shared.func__LogingIn { _ in
                    DelayConversationListener.shared.equalCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if WithNeighbourReactiveCompatible.clickEqual().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(WithNeighbourReactiveCompatible.clickEqual().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    SendSocketDelegate.shared.imageTa()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
