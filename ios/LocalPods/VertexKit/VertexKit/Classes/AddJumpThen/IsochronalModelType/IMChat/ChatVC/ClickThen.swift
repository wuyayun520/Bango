
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_productName:[UInt8] = [0xa5,0xa2,0xa5,0xb8,0xe4,0xaf,0xa3,0xa8,0xa9,0xbe,0xf6,0xe5,0xec,0xa4,0xad,0xbf,0xec,0xa2,0xa3,0xb8,0xec,0xae,0xa9,0xa9,0xa2,0xec,0xa5,0xa1,0xbc,0xa0,0xa9,0xa1,0xa9,0xa2,0xb8,0xa9,0xa8]

private func matchCenter(status num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let kFrameData:String = "player to lab varTalki"
fileprivate let k_ofContent:String = "oupCguard for content"
fileprivate let kAppData:String = "xtMsgCelet interval to equal lock"
fileprivate let kDismissName:String = "llIDwhite super make view"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let k_taskData:String = "let page view voice elseTalki"
fileprivate let k_mainText:String = "ngChatIelse result page"
fileprivate let k_pointSendData:String = "mageMin self app title pop"

/*: "EventView" :*/
fileprivate let k_miniUserValue:[Character] = ["T","a","l","k","i","n","g","G","r","o","u"]
fileprivate let kMagnitudeDataText:String = "pChatask with video return let"
fileprivate let k_playerName:String = "corner language if letMsgCell"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let k_hiddenValue:String = "Talkingtop pop"
fileprivate let k_modelValue:String = "self icon if shareChatTi"
fileprivate let k_viewContent:String = "mode self equal view selfCellID"

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let k_circleText:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C","e","l"]
fileprivate let kValueData:[Character] = ["l","_","R","e","u","s","e","I","d"]

/*: "roomId" :*/
fileprivate let kDeleteData:String = "crop content equalroomId"

/*: "type" :*/
fileprivate let k_insertTitle:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let k_modeValue:String = "EXTRA"

/*: "jumpKey" :*/
fileprivate let kViewTitle:[UInt8] = [0x71,0x6e,0x76,0x6b,0x50,0x7e,0x62]

private func sendMake(a num: UInt8) -> UInt8 {
    return num ^ 27
}

/*: "Send Gift" :*/
fileprivate let k_targetData:[Character] = ["S","e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let k_titleName:String = "v"
fileprivate let kImageName:String = "iew infoguard item view make let"

/*: "Report" :*/
fileprivate let k_enterValue:String = "let current levelRepor"
fileprivate let k_finishMakeName:String = "info"

/*: "uid" :*/
fileprivate let k_fromAddValue:[UInt8] = [0x1c,0x10,0xb]

fileprivate func cornerSearch(content num: UInt8) -> UInt8 {
    let value = Int(num) + 89
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "name" :*/
fileprivate let kModelMakeValue:[UInt8] = [0x65,0x6d,0x61,0x6e]

/*: "Can't send blank message" :*/
fileprivate let k_actionTimeValue:String = "Can\'t "
fileprivate let kGiftText:String = "equal true card model blank "
fileprivate let kInsideValue:String = "messnoge"

/*: "user" :*/
fileprivate let k_makeValue:String = "uscount"

/*: "content" :*/
fileprivate let k_indexValue:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let kMatchData:[Character] = ["m","s"]
fileprivate let k_likeData:[Character] = ["g","T","y","p","e"]

/*: "txt" :*/
fileprivate let k_tableData:[Character] = ["t","x","t"]

/*: "isGroup" :*/
fileprivate let k_equalData:[Character] = ["i","s","G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let k_examplePlayerValue:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let k_thenName:[UInt8] = [0x63,0x5b,0x69,0x69,0x57,0x5d,0x5b]

fileprivate func sizeInstance(user num: UInt8) -> UInt8 {
    let value = Int(num) + 10
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let k_imageContent:String = "TxtMsggift self"

/*: "toUid" :*/
fileprivate let k_canValue:String = "self and to app mytoUid"

/*: "msgInfo" :*/
fileprivate let k_postManagerTitle:String = "path point raw to sizemsgIn"
fileprivate let kEqualName:[Character] = ["f","o"]

/*: "params" :*/
fileprivate let kHeadValueData:[UInt8] = [0xc,0x1d,0xe,0x1d,0x11,0xf]

private func viewToday(price num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "contentType" :*/
fileprivate let kModelName:[UInt8] = [0x5b,0x57,0x56,0x4c,0x5d,0x56,0x4c,0x6c,0x41,0x48,0x5d]

private func meSource(make num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "ImgMsg" :*/
fileprivate let kTopValue:String = "ImgMsgdata table video pic"

/*: "img" :*/
fileprivate let k_exitViewContainerName:String = "imimage"

/*: "imageUri" :*/
fileprivate let k_collectionText:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let k_touchText:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let k_intimateValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let k_bridgeLayerName:[Character] = ["t","o","t","a","l","M","f"]
fileprivate let k_errorName:String = "Coinlet self index"

/*: "giftId" :*/
fileprivate let kUserTitle:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let kResultData:[Character] = ["g","i"]
fileprivate let kAfterContentData:String = "shadow left modelftNum"

/*: "pkgItemsetId" :*/
fileprivate let k_colorValue:String = "make equal bar finalpkgI"
fileprivate let k_layerText:[Character] = ["I","d"]

/*: "msg" :*/
fileprivate let kViewData:String = "observerg"

/*: "tips" :*/
fileprivate let kAddModelName:String = "TIPS"

/*: "gift" :*/
fileprivate let k_guideCenterName:String = "gnormalt"

/*: "nickname" :*/
fileprivate let kMakePhotoImageContent:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let kLastTitle:[UInt8] = [0xc,0x1,0x5,0x0,0x34,0xd,0x7]

private func viewType(user num: UInt8) -> UInt8 {
    return num ^ 100
}

/*: "All Numbers" :*/
fileprivate let k_styleData:String = "name gift in dataAll Nu"
fileprivate let kSharedContent:String = "mmomentrs"

/*: "sendNum" :*/
fileprivate let k_controlAlwaysData:String = "sendNuintimate frame"
fileprivate let kMakeTargetTitle:[Character] = ["m"]

/*: "toUser" :*/
fileprivate let kSumTitle:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let kAspectLabGestureText:[Character] = ["m","s","g","I","n","c"]
fileprivate let kStateName:[Character] = ["o","m","e"]

/*: "isConnection" :*/
fileprivate let kModelLiveValue:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ClickThen.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ClickThen: RecordReactiveCompatible {
	var rangeSum: Double = -55.4
	var createCount: Double = -27.9
	var labSelectName: String = "progress"
	var priceArray: [AnyHashable] = []

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = AtTransformable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = PhaticCommunicationThen() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = AtTransformable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_productName.map{matchCenter(status: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ScreenDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    
            

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        topUi()
        //: createUIConstraints()
        groupConstraints()
        //: bindInteraction()
        occurrence()
        //: addTapGestureRecognizer()
        hold()
        //: func__chatRoom_insertTipMsg()
        will()
        //: chatSettings()
        onSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            shirtButton()
        }
    
		
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.spot(join: false)
        //: self.destroyTipsTimer()
        self.tight()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.register(UserMsgCell.self, forCellReuseIdentifier: "UserMsgCellID")
        msgVC.tableView.register(SupplementViewCell.self, forCellReuseIdentifier: "TSupplementViewCellID")
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(EventView.self, forCellReuseIdentifier: (String(k_miniUserValue) + String(kMagnitudeDataText.prefix(4)) + "tGift" + String(k_playerName.suffix(7))))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(InstanceMsgCell.self, forCellReuseIdentifier: (String(k_hiddenValue.prefix(7)) + String(k_modelValue.suffix(6)) + "psMsg" + String(k_viewContent.suffix(6))))
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(k_circleText) + String(kValueData)))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: RecordInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = RecordInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: LengthView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = LengthView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: UpView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = UpView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: EqualThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = EqualThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: WithReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = WithReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 群聊设置
    //: private func chatSettings() {
    private func onSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ExaggerateChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        spot(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func spot(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(kDeleteData.suffix(6))): self.conversationData.groupID, String(bytes: k_insertTitle.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: SourceRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        SourceRequestTool.upwards(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TMessageControllerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ClickThen: TMessageControllerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        //: if data.isKind(of: AddCellData.self) {
        if data.isKind(of: AddCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = UserMsgCell(style: .default, reuseIdentifier: "UserMsgCellID")
            //: let cellData = data as! AddCellData
            let cellData = data as! AddCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.afoot(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ClickCellData.self) {
        if data.isKind(of: ClickCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = SupplementViewCell(style: .default, reuseIdentifier: "TSupplementViewCellID")
            //: let cellData = data as! ClickCellData
            let cellData = data as! ClickCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: MusicCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = EventView(style: .default, reuseIdentifier: (String(k_miniUserValue) + String(kMagnitudeDataText.prefix(4)) + "tGift" + String(k_playerName.suffix(7))))
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! MusicCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: UserCellData.self) {
        if data.isKind(of: UserCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = InstanceMsgCell(style: .default, reuseIdentifier: (String(k_hiddenValue.prefix(7)) + String(k_modelValue.suffix(6)) + "psMsg" + String(k_viewContent.suffix(6))))
            //: let cellData = data as! UserCellData
            let cellData = data as! UserCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(RowReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(RowReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                afoot(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                take(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SupplementViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ClickCellData
            let imageCellData = cell.messageData as! ClickCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(kLet_maleData)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = UserTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = SurroundingsModelType()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [UserTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = RowFlowLayout(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.belowResume()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: UserMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! UserMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_modeValue.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: kViewTitle.map{sendMake(a: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AddModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: MiraclePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    MiraclePushManager.share.packet(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: InstanceMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! InstanceMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_modeValue.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: kViewTitle.map{sendMake(a: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AddModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: MiraclePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    MiraclePushManager.share.packet(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        makeFullView(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func tight() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func take(user: HighProfileModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ViewItemReactiveCompatible(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.alongAm(cellTitleList: [(String(k_targetData)).localized, "@", (k_titleName.uppercased() + String(kImageName.prefix(8))).localized, (String(k_enterValue.suffix(5)) + k_finishMakeName.replacingOccurrences(of: "info", with: "t")).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.cellFrom(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.transactionUser(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.afoot(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.titleUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func cellFrom(user: HighProfileModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.theAdd()
        //: let model = TalkingChatRoomMemberModel()
        let model = DetailReactiveCompatible()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.largeModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func transactionUser(user: HighProfileModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: k_fromAddValue.map{cornerSearch(content: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: kModelMakeValue.reversed(), encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.calendar(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func afoot(uid: Int) {
        //: MiraclePushManager.share.func__pushToUserDetailVC(uid: String(uid))
        MiraclePushManager.share.toImage(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func titleUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = MiddleNameAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.bridge(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func funcToModel(msgStr: String, quoteModel _: StatusModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            appearShow(showMsg: (k_actionTimeValue + "send" + String(kGiftText.suffix(7)) + kInsideValue.replacingOccurrences(of: "no", with: "a")).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: kViewTitle.map{sendMake(a: $0)}, encoding: .utf8)!: (k_makeValue.replacingOccurrences(of: "count", with: "er")), String(bytes: k_fromAddValue.map{cornerSearch(content: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: k_indexValue.reversed(), encoding: .utf8)!: temStr, (String(kMatchData) + String(k_likeData)): (String(k_tableData)), (String(k_equalData)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(k_examplePlayerValue))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: k_thenName.map{sizeInstance(user: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: k_insertTitle.reversed(), encoding: .utf8)!: (String(k_imageContent.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kDeleteData.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(k_canValue.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(k_postManagerTitle.suffix(5)) + String(kEqualName)): msgInfo, String(bytes: kHeadValueData.map{viewToday(price: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        makeFullView(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func draft(img: UIImage) {
        //: let imagePath = ClickCellData.getChatImagePath(image: img)
        let imagePath = ClickCellData.reportSession(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: kModelName.map{meSource(make: $0)}, encoding: .utf8)!: (String(kTopValue.prefix(6))), (String(kMatchData) + String(k_likeData)): (k_exitViewContainerName.replacingOccurrences(of: "image", with: "g")), String(bytes: k_collectionText.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(k_touchText)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: k_insertTitle.reversed(), encoding: .utf8)!: (String(kTopValue.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kDeleteData.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(k_postManagerTitle.suffix(5)) + String(kEqualName)): msgInfo, String(bytes: kHeadValueData.map{viewToday(price: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        makeFullView(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func makeFullView(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            appearShow(showMsg: kLet_messageScreenTitle)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(k_touchText))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(k_touchText))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(k_touchText))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: SourceRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.renderNo(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.phone(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(k_postManagerTitle.suffix(5)) + String(kEqualName))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(k_intimateValue))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(k_bridgeLayerName) + String(k_errorName.prefix(4)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(k_bridgeLayerName) + String(k_errorName.prefix(4)))] as! NSNumber
                //: RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func phone(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard RowReactiveCompatible.share.loginUserMode.status != 1 else {
            guard RowReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    appearShow(showMsg: errorStr)
                }
                //: return
                return
            }

            //: MiraclePushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            MiraclePushManager.share.noBuild(clickEvent: kLet_greetData, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.stickingPoint()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            modelLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RoundReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
            //: view.show()
            view.colorAreaChoice()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                appearShow(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ClickThen: UpwardsViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func viewGift(giftModel _: BagHandyJSON, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func addOfSecond() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func contrastData(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = ClickViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.calendar(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func bodyType(actionType: ReloadExpressionConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            addFilterPhotos()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func subWith(giftModel _: BagHandyJSON, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func targetShowModel(giftModel: BagHandyJSON, giftNum: String, model: DetailReactiveCompatible) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            appearShow(showMsg: kLet_messageScreenTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(k_canValue.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(kUserTitle))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kResultData) + String(kAfterContentData.suffix(5)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(kDeleteData.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_colorValue.suffix(4)) + "temset" + String(k_layerText))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: SourceRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.requestView(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.phone(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.addOnNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.behindMake(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func statusBottom(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func numericalCount(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func underTo(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.underTo(tap)
    }

    //: func func__starRecordBtnDown() {}
    func byDown() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func ammunition(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ClickThen: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func addFilterPhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AuthorizationReactiveCompatible.funcCrush(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ToolThen.tied(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: MiraclePushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                MiraclePushManager.share.sectionVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.draft(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func coverBack() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AuthorizationReactiveCompatible.keep(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.draft(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func isothermalColor(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(kDeleteData.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ClickThen {
    //: func func__TipsNotification() {
    func queue() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(indexWith(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_appAddValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func indexWith(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: UserCellData  = userinfo["msg"] as! UserCellData
        let celldata: UserCellData = userinfo[(kViewData.replacingOccurrences(of: "observer", with: "ms"))] as! UserCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func modelLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: k_indexValue.reversed(), encoding: .utf8)!: tipStr, (String(kMatchData) + String(k_likeData)): (kAddModelName.lowercased())]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func will() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard RowReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard RowReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: RowReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.modelLocal(tipStr: RowReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func behindMake(extral: [String: Any?], model: DetailReactiveCompatible, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(k_postManagerTitle.suffix(5)) + String(kEqualName))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(k_equalData))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(kMatchData) + String(k_likeData))] = (k_guideCenterName.replacingOccurrences(of: "normal", with: "if"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(k_postManagerTitle.suffix(5)) + String(kEqualName))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: k_fromAddValue.map{cornerSearch(content: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(kMakePhotoImageContent)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: kLastTitle.map{viewType(user: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(k_styleData.suffix(6)) + kSharedContent.replacingOccurrences(of: "moment", with: "be")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((k_guideCenterName.replacingOccurrences(of: "normal", with: "if"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(k_guideCenterName.replacingOccurrences(of: "normal", with: "if"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(kMakePhotoImageContent))] = "\(giftdic[(String(k_controlAlwaysData.prefix(6)) + String(kMakeTargetTitle))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(kSumTitle))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kAspectLabGestureText) + String(kStateName))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kAspectLabGestureText) + String(kStateName))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(k_intimateValue))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        statisticalProcedure(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(k_bridgeLayerName) + String(k_errorName.prefix(4)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(k_bridgeLayerName) + String(k_errorName.prefix(4)))] as! NSNumber
            //: RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RowReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.combine(needReload: false, mf_coin: RowReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.clickMembers()
    }

    //: func initCachEffectData() {
    func shirtButton() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.taget(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func statisticalProcedure(giftDic: [String: Any], model: DetailReactiveCompatible) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.duringPrice(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AppViewThen.share.versionBag() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TalkingGiftTitleTheoryModelType]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.itemOff(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.endTo(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func observerDown(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AppViewThen.share.versionBag() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TalkingGiftTitleTheoryModelType]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.itemOff(model: giftModel as! TalkingBeginHandyJSON)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.betweenName(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ClickThen {
    /// 创建UI
    //: private func createUI() {
    private func topUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func groupConstraints() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_viewName)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(kLet_viewName)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - kLet_itemData)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func occurrence() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = ClickViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TUIMessageCellData).isKind(of: AddCellData.self) {
                    if (cellData as! TUIMessageCellData).isKind(of: AddCellData.self) {
                        //: let groupCellData = cellData as! AddCellData
                        let groupCellData = cellData as! AddCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(k_intimateValue))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: DelayConversationListener.shared.rx
        DelayConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(kModelLiveValue)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
