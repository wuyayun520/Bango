
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_pathValue:[UInt8] = [0x2a,0x2f,0x2a,0x35,0xe9,0x24,0x30,0x25,0x26,0x33,0xfb,0xea,0xe1,0x29,0x22,0x34,0xe1,0x2f,0x30,0x35,0xe1,0x23,0x26,0x26,0x2f,0xe1,0x2a,0x2e,0x31,0x2d,0x26,0x2e,0x26,0x2f,0x35,0x26,0x25]

fileprivate func viewTriggerIfColor(count num: UInt8) -> UInt8 {
    let value = Int(num) - 193
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let kBurnValue:String = "systemsystemsystem"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let kSizeShowFrameName:String = "Talkicorner let by view"
fileprivate let k_pathData:String = "app back guard tryatTe"
fileprivate let k_logValue:String = "let member outgCellID"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let k_addValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","I"]
fileprivate let k_toData:[Character] = ["m","a","g","e","M"]
fileprivate let kOfName:String = "sgCeself selection who original extension"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let kCellErrorHeightValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","V","i","d","e","o","M","s","g","C","e","l","l","I"]
fileprivate let k_labText:[Character] = ["D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let k_shareTitle:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","G","i","f","t","M","s","g","C"]
fileprivate let k_currentTitle:[Character] = ["e","l","l","I","D"]

/*: "UnwantedMsgCell" :*/
fileprivate let k_senseName:String = "Talkinmake let safe frame with"
fileprivate let kRandomAgeData:String = "translation content let backgCha"
fileprivate let kMinimumTitleName:[Character] = ["F","o","r","G","i","f","t","M","s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let k_itemTitle:String = "to make view var selfTalki"
fileprivate let kActualData:String = "mode"
fileprivate let k_insertName:String = "sMsgCtheme view equal image lab"

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let k_toolData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M"]
fileprivate let kAgeTitle:String = "voicech"
fileprivate let k_listName:String = "make info leadingellID"

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let kCheckData:String = "equal perTa"
fileprivate let kPopName:[Character] = ["l","k","i","n","g","C","h","a","t","A","u","d","i","o","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let k_modelValue:[UInt8] = [0x7e,0x4b,0x46,0x41,0x43,0x44,0x4d,0x69,0x42,0x4b,0x5e,0x79,0x53,0x59,0x5e,0x4f,0x47,0x64,0x45,0x5e,0x43,0x4c,0x60,0x5f,0x47,0x5a,0x67,0x59,0x4d,0x69,0x4f,0x46,0x46,0x63,0x6e]

private func contentAdd(next num: UInt8) -> UInt8 {
    return num ^ 42
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let kComponentTitle:[Character] = ["T","S","y","s","t","e","m"]
fileprivate let k_makeSucceedTitle:[Character] = ["M","e","s","s"]
fileprivate let k_modeScaleFalseTitle:[Character] = ["a","g","e","C","e","l","l","_","R","e","u","s","e","I","d"]

/*: "#128CFF" :*/
fileprivate let kUpName:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let k_fileValue:String = "image cancel viewFeedba"
fileprivate let k_indexData:String = "taskk"

/*: "#8370FF" :*/
fileprivate let k_viewGiftValue:[Character] = ["#"]
fileprivate let k_jumpValue:[Character] = ["8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let kMakeData:String = "Quickkit center"

/*: "Beauty Photos" :*/
fileprivate let kModelName:[Character] = ["B","e","a","u"]
fileprivate let k_colorName:String = "ty Photosname false"

/*: "url" :*/
fileprivate let kObjectData:String = "clearrl"

/*: "unlockGift" :*/
fileprivate let kRemoveValue:String = "unlockGfor model else style"
fileprivate let k_curText:String = "imodel"

/*: "%d" :*/
fileprivate let kAgeText:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let k_toName:[UInt8] = [0x9f,0xa9,0x78,0xab,0xa8,0xa4]

fileprivate func labMagnitude(then num: UInt8) -> UInt8 {
    let value = Int(num) + 202
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let k_equalTitleColorName:String = "giftIdvalue data screen"

/*: "rYMsgType" :*/
fileprivate let kListData:String = "rYMssucceed cell more view"
fileprivate let k_styleContentName:String = "else atgType"

/*: "GJ:CallCustom" :*/
fileprivate let kTitleData:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let k_whiteReasonData:String = "chpointn"
fileprivate let k_makeValue:String = "CTION"

/*: "extra" :*/
fileprivate let kIndicatorText:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let kRowValue:String = "layers"
fileprivate let kIntervalValue:String = "key class receive taskgInfo"

/*: "toUid" :*/
fileprivate let k_actualAspectElseValue:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let kArrayKindHeightTitle:String = "color"
fileprivate let k_managerFrameValue:[Character] = ["a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let kHomeName:String = "domain index labeljumpKey"

/*: "truePersonAuth" :*/
fileprivate let k_giftValue:[Character] = ["t","r","u","e","P","e","r","s","o","n","A","u","t"]
fileprivate let k_userData:String = "lab"

/*: "logType" :*/
fileprivate let k_imageData:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let kSucceedTitle:[UInt8] = [0xfe,0xe0,0xf4,0xc7,0xea,0xe3,0xf6]

private func fullMoon(label num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "txt" :*/
fileprivate let k_commentData:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let kActionName:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let k_errorModelName:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let k_picValue:[UInt8] = [0x2,0x13,0x0,0x13,0x1f,0x1]

private func dioramaPush(carrier num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "content" :*/
fileprivate let kCrushName:[UInt8] = [0xca,0xc6,0xc7,0xdd,0xcc,0xc7,0xdd]

/*: "id" :*/
fileprivate let kWeightData:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let kSizeTitle:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let kTargetData:[UInt8] = [0xf9,0xf1,0xe7,0xe7,0xf5,0xf3,0xf1]

private func valueWhite(context num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "TxtMsg" :*/
fileprivate let kStatusDirectionName:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let kWithName:[UInt8] = [0xac,0x95,0x93,0x99,0x9f,0xda,0x92,0x9f,0x96,0x96,0x95,0xda,0x9e,0x95,0x8d,0x94,0x96,0x95,0x9b,0x9e,0xda,0x9c,0x9b,0x93,0x96,0x9f,0x9e,0xd4,0xda,0xaa,0x88,0x95,0x99,0x9f,0x9e,0x8f,0x88,0x9f]

/*: "length" :*/
fileprivate let kHiddenName:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let k_viewText:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let kTitleContent:String = "right insert name selfaudioId"

/*: "audioLength" :*/
fileprivate let kImageData:[UInt8] = [0xe6,0xf2,0xe3,0xee,0xe8,0xcb,0xe2,0xe9,0xe0,0xf3,0xef]

private func atContentMeet(path num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "audioSandbox" :*/
fileprivate let k_backgroundData:String = "audtouch"

/*: "senderId" :*/
fileprivate let k_textData:[UInt8] = [0xf7,0xe1,0xea,0xe0,0xe1,0xf6,0xcd,0xe0]

private func searchedTable(wrap num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "AudioMsg" :*/
fileprivate let kSizeValue:[Character] = ["A"]
fileprivate let kUserCountValue:[Character] = ["u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let k_fieldData:String = "Can\'"
fileprivate let kErrorName:String = "d blvar send vertical"
fileprivate let k_inviteName:String = "ssdatege"

/*: "quoteMsgId" :*/
fileprivate let k_acceptText:[Character] = ["q","u","o","t","e","M","s","g","I"]
fileprivate let k_imageValue:String = "D"

/*: "quoteMsgInfo" :*/
fileprivate let kInfoData:[Character] = ["q","u","o","t","e","M","s","g","I"]
fileprivate let kCountimateValue:String = "nfstart"

/*: "contentType" :*/
fileprivate let kMainTransformValue:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let kTextData:String = "ImgMsgapp self lab available close"

/*: "img" :*/
fileprivate let kManagerCountName:String = "IMG"

/*: "imageUri" :*/
fileprivate let k_detailBottomData:[UInt8] = [0xe4,0xe8,0xdc,0xe2,0xe0,0xd0,0xed,0xe4]

fileprivate func inputLeadingMake(show num: UInt8) -> UInt8 {
    let value = Int(num) - 123
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let k_imageName:[UInt8] = [0x61,0x77,0x71,0x76,0x6d,0x6f,0x51,0x67,0x6c,0x66]

private func dataLet(gift num: UInt8) -> UInt8 {
    return num ^ 2
}

/*: "pic" :*/
fileprivate let k_makeShowTitle:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let kMessageTitle:String = "Videgreen tool self extension below"

/*: "video" :*/
fileprivate let kLastContent:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let kUserContent:[UInt8] = [0x60,0x6c,0x75,0x66,0x71,0x4a,0x6e,0x64]

/*: "videoUrl" :*/
fileprivate let kVideoData:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let k_fatalValue:[UInt8] = [0x79,0x68,0x6f,0x7c,0x69,0x74,0x72,0x73]

/*: "len" :*/
fileprivate let k_toText:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let k_scaleData:[Character] = ["D","o"]
fileprivate let k_sizeText:[Character] = ["c","u","m","e","n","t","s","/","U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let k_bottomUserTitle:[Character] = ["D","o","c","u","m","e","n","t","s","/"]
fileprivate let k_photoTitle:String = "post height color lab modelUser/"

/*: "msgIncome" :*/
fileprivate let kAutomaticValue:[Character] = ["m","s","g","I","n"]
fileprivate let k_targetName:String = "hiddenme"

/*: "mfBean" :*/
fileprivate let k_nameStatusData:[UInt8] = [0x4e,0x45,0x61,0x46,0x42,0x4d]

/*: "messageType" :*/
fileprivate let kTalkData:[Character] = ["m","e","s","s","a","g"]
fileprivate let kPlayerAddTitle:String = "eTypeback video of service"

/*: "WantGift" :*/
fileprivate let k_styleData:String = "Wantdata make attach super"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let k_textContent:[Character] = ["_","P","r","i","v","a","t"]
fileprivate let k_kindText:String = "eChaguard view view in"
fileprivate let k_leadingStatusCellContent:String = "view edgeTitle_"

/*: "msgId" :*/
fileprivate let k_succeedDateData:String = "msgIdmenu color as"

/*: "totalMfCoin" :*/
fileprivate let kEditData:String = "image tabtotalM"

/*: "attachSysMsg" :*/
fileprivate let kScreenVideoValue:String = "return nor make shareattachSys"
fileprivate let k_listText:[Character] = ["M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let k_nowContent:String = "motivationntmotivation"
fileprivate let kVideoTitle:String = "oshadowo"

/*: _ :*/
fileprivate let kTimeTitle:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let k_sectionTitle:[Character] = ["t","o","t","a","l"]
fileprivate let k_iconValue:String = "Intimatedescription head succeed info"

/*: "uid" :*/
fileprivate let k_pointKeyValue:[UInt8] = [0x51,0x4d,0x40]

private func playerBackground(collection num: UInt8) -> UInt8 {
    return num ^ 36
}

/*: "intimate" :*/
fileprivate let k_moleculeVoiceInContent:[UInt8] = [0x77,0x70,0x6a,0x77,0x73,0x7f,0x6a,0x7b]

private func equalManager(target num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let kMatchValue:[UInt8] = [0x6b,0x49,0x46,0xf,0x5c,0x8,0x5d,0x5b,0x4d,0x8,0x5c,0x40,0x4d,0x8,0x4e,0x5d,0x46,0x4b,0x5c,0x41,0x47,0x46,0x8,0x4c,0x5d,0x4d,0x8,0x5c,0x47,0x8,0x51,0x47,0x5d,0x5a,0x8,0x44,0x49,0x4b,0x43,0x8,0x47,0x4e,0x8,0x41,0x46,0x5c,0x41,0x45,0x49,0x4b,0x51,0x8,0x44,0x4d,0x5e,0x4d,0x44,0x6]

private func gestureShared(view num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "giftNum" :*/
fileprivate let kAppTitle:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let kImageText:[Character] = ["p","k","g","I","t","e","m","s","e"]
fileprivate let kTopArcData:String = "tIdmystery page bottom"

/*: "from" :*/
fileprivate let k_modeData:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let k_colorHomeContent:String = "waapp"
fileprivate let kMatchText:String = "make double else frame totGiftId"

/*: "WantGiftMsg" :*/
fileprivate let kDeleteAfterTitle:String = "data"
fileprivate let k_withContent:[Character] = ["a","n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let kImagePicTitle:String = "gtopft"

/*: "View info" :*/
fileprivate let k_showData:String = "Viewapp actual play pop title"
fileprivate let k_tillValue:String = "O"

/*: "Notes" :*/
fileprivate let k_managerTitle:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let k_blackTitle:String = "list quitReport"

/*: "View read receipts?" :*/
fileprivate let k_rangeName:String = "attribute kit transformView "
fileprivate let k_rawText:String = " recvalue make model"
fileprivate let k_viewMakeMediumData:String = "?"

/*: "He" :*/
fileprivate let kFrameText:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let kRangeName:String = "self selfShe"

/*: "%@ is using too low a version" :*/
fileprivate let k_dateName:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t","o","o"," ","l","o","w"," ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let k_countValue:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e"]
fileprivate let kItemText:String = "view show make viewast 5 "

/*: "Upload failed" :*/
fileprivate let k_framePushInName:String = "Uploamodel tool view"

/*: "播放状态: :*/
fileprivate let k_resultValue:String = "\u{64ad}\u{653e}\u{72b6}态:"

/*: "c2c_" :*/
fileprivate let kTransformName:String = "c2c_make case class self"

/*: "msg" :*/
fileprivate let k_leadingData:String = "msaction"

/*: "tips" :*/
fileprivate let k_labWithValue:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let k_keyTitle:[UInt8] = [0xf7,0xd,0x13,0xbe,0x1,0xff,0xc,0xbe,0x13,0x11,0x3,0xbe,0x12,0x6,0x3,0xbe,0x14,0x7,0x2,0x3,0xd,0xbe,0x1,0x6,0xff,0x12,0xbe,0x4,0x3,0xff,0x12,0x13,0x10,0x3,0xbe,0x12,0xd,0xbe,0x5,0x7,0x14,0x3,0xbe,0xc3,0xde,0xbe,0xff,0xbe,0x1,0xff,0xa,0xa]

fileprivate func liveFirst(center num: UInt8) -> UInt8 {
    let value = Int(num) - 158
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let kReplyValue:[Character] = ["c","e","l","l","D","a","t","a"]

/*: "System notification" :*/
fileprivate let kGestureLabelData:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a","t"]
fileprivate let kProgramText:[Character] = ["i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DoingdDoorBeginViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum BagFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class DoingdDoorBeginViewController: RecordReactiveCompatible {
	var monarchOff: Bool = false
	var existentTotal: Int = 47
	var withReadingTitle: String = "live"
	var pathArray: [AnyHashable] = []
	var fallForEnable: Bool = true
	var timeMagnitude: Int = 52
	var atTitle: String = "select"
	var acceptArray: [AnyHashable] = []
	var appearEqualEnable: Bool = false
	var withNumber: Int = 20
	var enableName: String = "model"
	var tableArray: [AnyHashable] = []

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = FrontRecordCacheModel()
    private var userWrap = FrontRecordCacheModel() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = BagFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: FrontRecordCacheModel = FrontRecordCacheModel()) {
    init(chatID: String, isFrom: BagFromEnum = .Normal, userWrap: FrontRecordCacheModel = FrontRecordCacheModel()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        DoingdDoorBeginViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: IndexViewThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        IndexViewThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == NameMacroDefine.getXiaoMiID() {
        if targetId == NameMacroDefine.have() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == NameMacroDefine.getCustomerServiceID() {
        } else if targetId == NameMacroDefine.finishShared() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_pathValue.map{viewTriggerIfColor(count: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ScreenDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    
            

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        barCell()
        //: createUI()
        sound()
        //: createUIConstraints()
        giftTableConstraints()
        //: bindInteraction()
        pothouse()
        //: addTapGestureRecognizer()
        hold()
        //: func__requestChatInfo()
        colorRe()
        //: func__getFillDraftText()
        cellInForeword()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        ContentPlayerDelegate.shared.element()
        //: func__addVoiceNotification()
        imageVideo()
        //: addReadPeceiptNotification()
        constraintAdd()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            sashLockService()
            //: func__TipsNotification()
            wish()
            //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue {
                //: func__setReplyTipsView()
                toSave()
            }
        }
    
            

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        taproom(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        taproom(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            clickApp()
        }
    
            

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (kBurnValue.replacingOccurrences(of: "system", with: "F5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(DerivationReactiveCompatible.self, forCellReuseIdentifier: (String(kSizeShowFrameName.prefix(5)) + "ngCh" + String(k_pathData.suffix(4)) + "xtMs" + String(k_logValue.suffix(7))))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(SupplementViewCell.self, forCellReuseIdentifier: (String(k_addValue) + String(k_toData) + String(kOfName.prefix(4)) + "llID"))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(UserViewCell.self, forCellReuseIdentifier: (String(kCellErrorHeightValue) + String(k_labText)))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(GiftMsgCell.self, forCellReuseIdentifier: (String(k_shareTitle) + String(k_currentTitle)))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(UnwantedMsgCell.self, forCellReuseIdentifier: (String(k_senseName.prefix(6)) + String(kRandomAgeData.suffix(4)) + "tAsk" + String(kMinimumTitleName)))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(InstanceMsgCell.self, forCellReuseIdentifier: (String(k_itemTitle.suffix(5)) + "ngChatTi" + kActualData.replacingOccurrences(of: "mode", with: "p") + String(k_insertName.prefix(5)) + "ellID"))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(VideoMessageCell.self, forCellReuseIdentifier: (String(k_toolData) + kAgeTitle.replacingOccurrences(of: "voice", with: "at") + "MsgC" + String(k_listName.suffix(5))))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(ContentMessageCell.self, forCellReuseIdentifier: (String(kCheckData.suffix(2)) + String(kPopName)))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(SelsynReactiveCompatible.self, forCellReuseIdentifier: String(bytes: k_modelValue.map{contentAdd(next: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(kComponentTitle) + String(k_makeSucceedTitle) + String(k_modeScaleFalseTitle)))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: BillMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = BillMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: RecordInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: ViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = RecordInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(kUpName)))!, .font: UIFont.ofAndSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_fileValue.suffix(6)) + k_indexData.replacingOccurrences(of: "task", with: "c")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(nicknameLine), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: LengthView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = LengthView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: EqualThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = EqualThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(k_viewGiftValue) + String(k_jumpValue))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(kMakeData.prefix(5)) + " Greeting").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.cellColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pathCamera), for: .touchUpInside)
        //: btn.isHidden = !RowReactiveCompatible.share.appUserConfigMode.showDirectGreet || RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || RowReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !RowReactiveCompatible.share.appUserConfigMode.showDirectGreet || RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue || RowReactiveCompatible.share.appStatus == SkinSubscriptType.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: VideoTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = VideoTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: AppReactiveCompatible = {
        //: let v = TalkingSelectTranslationView()
        let v = AppReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.giftActionView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: SourceThen = {
        //: let v = TalkingTranslationResultView()
        let v = SourceThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.funcToModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.ofAndSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(k_viewGiftValue) + String(k_jumpValue))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(kModelName) + String(k_colorName.prefix(9))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.cellColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(betweenHandleClick), for: .touchUpInside)
        //: btn.isHidden = RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || RowReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue || RowReactiveCompatible.share.appStatus == SkinSubscriptType.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: GamesGiftThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = GamesGiftThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: ColorDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = ColorDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: RefreshityView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = RefreshityView()
        //: v.isHidden = (RowReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (RowReactiveCompatible.share.appStatus == SkinSubscriptType.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ChangeReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ChangeReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.backProgress()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension DoingdDoorBeginViewController: AddThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func modification(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.beatBack(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension DoingdDoorBeginViewController: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func pathCamera() {
        //: IndexViewThen.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        IndexViewThen.toHandler(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.appearShow(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.imageAction(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func betweenHandleClick() {
        //: IndexViewThen.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        IndexViewThen.lineHandler(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = SourceRowViewDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().stopDown(urlStr: dict?[(kObjectData.replacingOccurrences(of: "clear", with: "u"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(kRemoveValue.prefix(7)) + k_curText.replacingOccurrences(of: "model", with: "ft"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ResumeSourceModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: k_toName.map{labMagnitude(then: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(k_equalTitleColorName.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.isometric(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: IndexCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: IndexCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(kListData.prefix(4)) + String(k_styleContentName.suffix(5)))].stringValue == (String(kTitleData)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(k_whiteReasonData.replacingOccurrences(of: "point", with: "a") + "geDire" + k_makeValue.lowercased())].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(kIndicatorText.lowercased())][(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))][(String(k_actualAspectElseValue))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = BeginCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = BeginCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: LengthReactiveCompatible.self) == true || cellData.isKind(of: ClickCellData.self) == true || cellData.isKind(of: BeginCellData.self) == true || cellData.isKind(of: ColorThen.self) == true {
        if cellData.isKind(of: LengthReactiveCompatible.self) == true || cellData.isKind(of: ClickCellData.self) == true || cellData.isKind(of: BeginCellData.self) == true || cellData.isKind(of: ColorThen.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: IndexViewThen.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        IndexViewThen.futurism(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: IndexViewThen.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        IndexViewThen.pastTransform(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = blockIn(data: data)

        //: if data.isKind(of: BeginCellData.self) {
        if data.isKind(of: BeginCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = DerivationReactiveCompatible(style: .default, reuseIdentifier: (String(kSizeShowFrameName.prefix(5)) + "ngCh" + String(k_pathData.suffix(4)) + "xtMs" + String(k_logValue.suffix(7))))
            //: let cellData = data as! BeginCellData
            let cellData = data as! BeginCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.localSize() {
                //: if cellData.msgModel.quoteMsgInfo.uid == RowReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == RowReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = RowReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = RowReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ClickCellData.self) {
        if data.isKind(of: ClickCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = SupplementViewCell(style: .default, reuseIdentifier: (String(k_addValue) + String(k_toData) + String(kOfName.prefix(4)) + "llID"))
            //: let cellData = data as! ClickCellData
            let cellData = data as! ClickCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ColorThen.self) {
        if data.isKind(of: ColorThen.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = UserViewCell(style: .default, reuseIdentifier: (String(kCellErrorHeightValue) + String(k_labText)))
            //: let cellData = data as! ColorThen
            let cellData = data as! ColorThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SourceCellData.self) {
        if data.isKind(of: SourceCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = GiftMsgCell(style: .default, reuseIdentifier: (String(k_shareTitle) + String(k_currentTitle)))
            //: let cellData = data as! SourceCellData
            let cellData = data as! SourceCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: TitleReactiveCompatible.self) {
        if data.isKind(of: TitleReactiveCompatible.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = UnwantedMsgCell(style: .default, reuseIdentifier: (String(k_senseName.prefix(6)) + String(kRandomAgeData.suffix(4)) + "tAsk" + String(kMinimumTitleName)))
            //: let cellData = data as! TitleReactiveCompatible
            let cellData = data as! TitleReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = BagHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.prepareTitle(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (kArrayKindHeightTitle.replacingOccurrences(of: "color", with: "w") + String(k_managerFrameValue)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: LengthReactiveCompatible.self) {
        if data.isKind(of: LengthReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = ContentMessageCell(style: .default, reuseIdentifier: (String(kCheckData.suffix(2)) + String(kPopName)))
            //: let cellData = data as! LengthReactiveCompatible
            let cellData = data as! LengthReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: IndexCellData.self) {
        if data.isKind(of: IndexCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = VideoMessageCell(style: .default, reuseIdentifier: (String(k_toolData) + kAgeTitle.replacingOccurrences(of: "voice", with: "at") + "MsgC" + String(k_listName.suffix(5))))
            //: let cellData = data as! IndexCellData
            let cellData = data as! IndexCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: UserCellData.self) {
        if data.isKind(of: UserCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = InstanceMsgCell(style: .default, reuseIdentifier: (String(k_itemTitle.suffix(5)) + "ngChatTi" + kActualData.replacingOccurrences(of: "mode", with: "p") + String(k_insertName.prefix(5)) + "ellID"))
            //: let cellData = data as! UserCellData
            let cellData = data as! UserCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: AddRowCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = SelsynReactiveCompatible(style: .default, reuseIdentifier: String(bytes: k_modelValue.map{contentAdd(next: $0)}, encoding: .utf8)!)
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! AddRowCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != NameMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != NameMacroDefine.finishShared() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: MiraclePushManager.share.func__pushToUserDetailVC(uid: String(uid))
            MiraclePushManager.share.toImage(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SupplementViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ClickCellData
            let imageCellData = cell.messageData as! ClickCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = CurrentReactiveCompatible(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.conceal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_detailTitle)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.eachViewButton(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.thumb(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                lineController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_maleData)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = UserTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = SurroundingsModelType()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [UserTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = RowFlowLayout(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                lineController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: UserViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! ColorThen
            let videoCellData = cell.messageData as! ColorThen
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = TalkingVideoVc(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                lineController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = TechnicalityViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.conceal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_detailTitle)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.eachViewButton(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.thumb(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                lineController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ContentMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ContentMessageCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! LengthReactiveCompatible, cellView: audioCell)
            rawData(cellData: audioCell.cellData as! LengthReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DerivationReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DerivationReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kIndicatorText.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(kHomeName.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(kHomeName.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(k_giftValue) + k_userData.replacingOccurrences(of: "lab", with: "h")) {
                    //: if RowReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if RowReactiveCompatible.share.loginUserMode.isTPAuth == GiftBasicType.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if RowReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if RowReactiveCompatible.share.loginUserMode.isTPAuth == GiftBasicType.unknown.rawValue {
                        //: MiraclePushManager.share.func__pushUserVerifyController(toast: nil)
                        MiraclePushManager.share.indexToast(toast: nil)
                        //: } else if RowReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if RowReactiveCompatible.share.loginUserMode.isTPAuth == GiftBasicType.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = FundamentalMeasureViewController()
                        //: MiraclePushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        MiraclePushManager.share.sectionVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: MiraclePushManager.share.func__pushUserVerifyController(toast: nil)
                        MiraclePushManager.share.indexToast(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AddModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: MiraclePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    MiraclePushManager.share.packet(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(kListData.prefix(4)) + String(k_styleContentName.suffix(5)))] as? String == (String(kTitleData)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))][(String(k_imageData))].intValue == 1 {
                    //: checkAndCallVoice()
                    bolus()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    self.dataInfo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: InstanceMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! InstanceMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kIndicatorText.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(kHomeName.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AddModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: MiraclePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    MiraclePushManager.share.packet(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: VideoMessageCell.self) {
            //: MiraclePushManager.share.func__pushToUserDetailVC(uid: targetId)
            MiraclePushManager.share.toImage(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ChatCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SupplementViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! SupplementViewCell
            //: cellData = imageCell.cellData as! ClickCellData
            cellData = imageCell.cellData as! ClickCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: UserViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! UserViewCell
            //: cellData = videoCell.cellData as! ColorThen
            cellData = videoCell.cellData as! ColorThen
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ContentMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ContentMessageCell
            //: cellData = audioCell.cellData as! LengthReactiveCompatible
            cellData = audioCell.cellData as! LengthReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DerivationReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DerivationReactiveCompatible
            //: cellData = textCell.cellData as! BeginCellData
            cellData = textCell.cellData as! BeginCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: ColorThen.self) {
                if reCellData!.isKind(of: ColorThen.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    visualCommunicationTitle(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    picturesqueness(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = IndexViewThen.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = IndexViewThen.eventTime(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: IndexViewThen.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        IndexViewThen.picSumerval(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func blockIn(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(RowReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(RowReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if RowReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if RowReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = RowReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = RowReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = RowReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = RowReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func applicationStatus() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_commentData))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(kActionName)): (String(k_errorModelName))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.picturesqueness(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func imageAction(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: kCrushName.map{$0^169}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: kCrushName.map{$0^169}, encoding: .utf8)!: content, (String(kSizeTitle)): greetId, String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_commentData))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: kTargetData.map{valueWhite(context: $0)}, encoding: .utf8)!: content, (String(kSizeTitle)): greetId, (String(kActionName)): (String(kStatusDirectionName))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.picturesqueness(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            VoiceDetailDirectorTaskManager.shared.taskCareCompleteLoadDownwardsAudioIncludeGreet(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.appearShow(showMsg: String(bytes: kWithName.map{$0^250}, encoding: .utf8)!)
                        //: IndexViewThen.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        IndexViewThen.searchVideo(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(kHiddenName))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_viewText)),
                               //: "audioId": greetId,
                               (String(kTitleContent.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: kImageData.map{atContentMeet(path: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox"): model.unZipFilePath,
                               //: "senderId": RowReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: k_textData.map{searchedTable(wrap: $0)}, encoding: .utf8)!: RowReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(kTitleContent.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: kImageData.map{atContentMeet(path: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(kActionName)): (String(kSizeValue) + String(kUserCountValue))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.picturesqueness(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func funcToModel(msgStr: String, quoteModel: StatusModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            appearShow(showMsg: (k_fieldData + "t sen" + String(kErrorName.prefix(4)) + "ank me" + k_inviteName.replacingOccurrences(of: "date", with: "a")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: kCrushName.map{$0^169}, encoding: .utf8)!: temStr, String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_commentData))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: kTargetData.map{valueWhite(context: $0)}, encoding: .utf8)!: temStr, (String(kActionName)): (String(kStatusDirectionName))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(k_acceptText) + k_imageValue.lowercased())] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kInfoData) + kCountimateValue.replacingOccurrences(of: "start", with: "o"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? StatusModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        picturesqueness(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func isometric(img: UIImage, sendModel: ResumeSourceModel) {
        //: let imagePath = ClickCellData.getChatImagePath(image: img)
        let imagePath = ClickCellData.reportSession(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: kMainTransformValue.reversed(), encoding: .utf8)!: (String(kTextData.prefix(6))), String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (kManagerCountName.lowercased()), String(bytes: k_detailBottomData.map{inputLeadingMake(show: $0)}, encoding: .utf8)!: imagePath, String(bytes: k_imageName.map{dataLet(gift: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(k_makeShowTitle)): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(kActionName)): (String(kTextData.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: k_toName.map{labMagnitude(then: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_equalTitleColorName.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        picturesqueness(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func visualCommunicationTitle(msgData: TUIMessageCellData?, sendModel: ResumeSourceModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: kMainTransformValue.reversed(), encoding: .utf8)!: (String(kMessageTitle.prefix(4)) + "oMsg"), String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(kLastContent)), String(bytes: k_imageName.map{dataLet(gift: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(kActionName)): (String(kMessageTitle.prefix(4)) + "oMsg"),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: kUserContent.map{$0^3}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: kVideoData.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: k_fatalValue.map{$0^29}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: k_toName.map{labMagnitude(then: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_equalTitleColorName.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.anySave(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.anySave(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        picturesqueness(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func ammunition(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_viewText)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: kImageData.map{atContentMeet(path: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox"): voicePath,
                       //: "senderId": RowReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: k_textData.map{searchedTable(wrap: $0)}, encoding: .utf8)!: RowReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox"): voicePath,
                         //: "type": "AudioMsg",
                         (String(kActionName)): (String(kSizeValue) + String(kUserCountValue)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: k_toText.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        picturesqueness(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func picturesqueness(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            appearShow(showMsg: kLet_messageScreenTitle)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(k_actualAspectElseValue))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: ColorThen.self) == false && msgData.isKind(of: TitleReactiveCompatible.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: ColorThen.self) == false, msgData.isKind(of: TitleReactiveCompatible.self) == false, params[(String(kActionName))] as? String != (String(k_errorModelName)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(k_makeShowTitle))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(k_makeShowTitle))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(k_makeShowTitle))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(kActionName))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(kSizeValue) + String(kUserCountValue)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (k_backgroundData.replacingOccurrences(of: "touch", with: "i") + "oSandbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(k_scaleData) + String(k_sizeText))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_frameData + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(k_bottomUserTitle) + String(k_photoTitle.suffix(5)) + "record/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_tabValue + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(kTitleContent.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(k_viewText))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: SourceRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.sexBy(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(k_errorModelName)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.conceal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_userTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(kSizeTitle))) {
                    //: IndexViewThen.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    IndexViewThen.searchVideo(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kAutomaticValue) + k_targetName.replacingOccurrences(of: "hidden", with: "co"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kAutomaticValue) + k_targetName.replacingOccurrences(of: "hidden", with: "co"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if WithNeighbourReactiveCompatible.clickEqual().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_recordTitle, object: nil, userInfo: [String(bytes: k_nameStatusData.map{$0^35}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.state(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(kTalkData) + String(kPlayerAddTitle.prefix(5)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(k_errorModelName)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!] = (String(k_styleData.prefix(4)) + "Gift")
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_maxValue.detailTing(eventID: kLet_bottomName)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(k_errorModelName)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!] = (String(k_commentData))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(RowReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(RowReactiveCompatible.share.loginUserMode.userID)" + (String(k_textContent) + String(k_kindText.prefix(4)) + "tTips" + String(k_leadingStatusCellContent.suffix(6))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue, type != (String(k_errorModelName)) {
                //: self.removeReplyTipsView()
                self.behindMakeView()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(k_succeedDateData.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: IndexViewThen.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            IndexViewThen.pastTransform(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(kEditData.suffix(6)) + "fCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(kEditData.suffix(6)) + "fCoin")] as! NSNumber
                //: RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(kScreenVideoValue.suffix(9)) + String(k_listText))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(kScreenVideoValue.suffix(9)) + String(k_listText))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.sumeractionAcrossStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((k_nowContent.replacingOccurrences(of: "motivation", with: "i") + "matePh" + kVideoTitle.replacingOccurrences(of: "shadow", with: "t"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(k_nowContent.replacingOccurrences(of: "motivation", with: "i") + "matePh" + kVideoTitle.replacingOccurrences(of: "shadow", with: "t"))] as! [String: Any]
                //: let model = TalkingAreaOutlookJsonModel(dic: intimatePhotoDict)
                let model = TalkingAreaOutlookJsonModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(kLastContent))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(kLastContent))] as! [String: Any]
                //: let model = LowlinessModel(dic: intimateVideoDict)
                let model = LowlinessModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        commentWith()

        //: updateSession()
        medicalInstitution()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func medicalInstitution() {
        //: MusicInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        MusicInfoManager.videoIn(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func conceal(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard RowReactiveCompatible.share.loginUserMode.status != 1 else {
            guard RowReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    appearShow(showMsg: errorStr)
                }
                //: return
                return
            }

            //: MiraclePushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            MiraclePushManager.share.noBuild(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.stickingPoint()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RoundReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
            //: view.show()
            view.colorAreaChoice()
            //: } else if errorCode == 60003, !RowReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !RowReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            BeanTimeThen.shared.beardEqual()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                appearShow(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func thumb(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.state(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func headwater() {
        //: if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.titleualMatterWith(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(RowReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(RowReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_tableRouteValue.array(forKey: kLet_statusName)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.doingTitle()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_tableRouteValue.set(intimateCacheData, forKey: kLet_statusName)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func anyClick(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(kIndicatorText.lowercased())].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(k_sectionTitle) + String(k_iconValue.prefix(8)))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.changeOf(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func state(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(k_sectionTitle) + String(k_iconValue.prefix(8)))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(k_sectionTitle) + String(k_iconValue.prefix(8)))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.changeOf(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func changeOf(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = MusicInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = MusicInfoManager.totalerval(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.titleualMatterWith(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.openCountimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.bivouac(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.bivouac(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: MusicInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        MusicInfoManager.keyList(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: k_pointKeyValue.map{playerBackground(collection: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: k_moleculeVoiceInContent.map{equalManager(target: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_formalTableData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension DoingdDoorBeginViewController: UpwardsViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func addOfSecond() {
        //: guard RowReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.need(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func contrastData(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func bodyType(actionType: ReloadExpressionConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    cypherFrom()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    enterSmall()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                cypherFrom()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if RowReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if RowReactiveCompatible.share.loginUserMode.loungePlus {
                if RowReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.demonstrateClick(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                enterSmall()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    shortStart()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    enterSmall()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                shortStart()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.skipWith(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func enterSmall() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.appearShow(showMsg: String(bytes: kMatchValue.map{gestureShared(view: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func targetShowModel(giftModel _: BagHandyJSON, giftNum _: String, model _: DetailReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func subWith(giftModel: BagHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        prepareTitle(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: TitleReactiveCompatible?, fromStr: String?) {
    func prepareTitle(giftModel: BagHandyJSON, giftNum: String, cellData: TitleReactiveCompatible?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            appearShow(showMsg: kLet_messageScreenTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(k_actualAspectElseValue))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(k_equalTitleColorName.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kAppTitle))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(kImageText) + String(kTopArcData.prefix(3)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(k_modeData))] = fromStr
        }
        // 送礼接口
        //: SourceRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.giftCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.conceal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_greetData)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.addOnNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.eachViewButton(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.medicalInstitution()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(k_sectionTitle) + String(k_iconValue.prefix(8)))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(k_sectionTitle) + String(k_iconValue.prefix(8)))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.changeOf(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_maxValue.detailTing(eventID: kLet_backTitle)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func viewGift(giftModel: BagHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_maxValue.detailTing(eventID: kLet_failValue)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            appearShow(showMsg: kLet_messageScreenTitle)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= RowReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            enterSmall()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(k_colorHomeContent.replacingOccurrences(of: "app", with: "n") + String(kMatchText.suffix(7)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(kActionName))] = (kDeleteAfterTitle.replacingOccurrences(of: "data", with: "W") + String(k_withContent))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_styleData.prefix(4)) + "Gift")]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5))): msgInfo, String(bytes: k_picValue.map{dioramaPush(carrier: $0)}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        picturesqueness(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func statusBottom(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_detailMarginTitle - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func numericalCount(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func underTo(_ tap: UITapGestureRecognizer) {
        //: super.underTo(tap)
        super.underTo(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                mutual(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func byDown() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if ContentPlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            ContentPlayerDelegate.shared.settleToPoolPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func eachViewButton(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!] = (kImagePicTitle.replacingOccurrences(of: "top", with: "i"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(kRowValue.replacingOccurrences(of: "layer", with: "m") + String(kIntervalValue.suffix(5)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kAutomaticValue) + k_targetName.replacingOccurrences(of: "hidden", with: "co"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kAutomaticValue) + k_targetName.replacingOccurrences(of: "hidden", with: "co"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(k_succeedDateData.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        dataWith(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(kEditData.suffix(6)) + "fCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(kEditData.suffix(6)) + "fCoin")] as! NSNumber
            //: RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RowReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RowReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.combine(needReload: false, mf_coin: RowReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func sashLockService() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.taget(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func dataWith(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.endTo(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func overload(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.betweenName(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func majority() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func bolus() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.addLock { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.medicalInstitution()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func dataInfo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.rowPoint { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.medicalInstitution()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func nicknameLine() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = MakeRecognizerDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        lineController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func section() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ViewItemReactiveCompatible(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.alongAm(cellTitleList: [(String(k_showData.prefix(4)) + " inf" + k_tillValue.lowercased()).localized, (String(k_managerTitle)).localized, (String(k_blackTitle.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .dropMultiView()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: MiraclePushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                MiraclePushManager.share.toImage(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                backProgress()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = MiddleNameAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.bridge(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func backProgress() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SelectNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.treetopUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.beforeCellWill(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func lowliness() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            mutual(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            lineController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func beyondInfo(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = RowKeyHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.wrapName(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func constraintAdd() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(duringView(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_partyValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(restoreList),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: kLet_nextValue,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func duringView(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard RowReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard RowReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func restoreList() {
        //: RowReactiveCompatible.share.loginUserMode.loungePlus = true
        RowReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func terminateMsg() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if IndexViewThen.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if IndexViewThen.equation(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.behindAllow(tipStr: (String(k_rangeName.suffix(5)) + "read" + String(k_rawText.prefix(4)) + "eipts" + k_viewMakeMediumData.capitalized).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension DoingdDoorBeginViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func cypherFrom() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AuthorizationReactiveCompatible.funcCrush(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = ToolThen.tied(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.shooter(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func electronicImage() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AuthorizationReactiveCompatible.keep(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.shooter(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func shooter(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ResumeSourceModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.isometric(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = RowFlowLayout(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ResumeSourceModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.isometric(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        lineController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func shortStart() {
        // 检测用户短视频版本是否可用
        //: guard IndexViewThen.checkShortVideo(userVersion: self.userWrap.version) else {
        guard IndexViewThen.infoBorder(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == AppTextProtocol.male.rawValue ? "He".localized : (String(kRangeName.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.appearShow(showMsg: (String(k_dateName)).roundName(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.picturePicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func picturePicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = ToolThen.tied(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            ToolThen.businessIndex(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.gestureUrl(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.appearShow(showMsg: (String(k_countValue) + String(kItemText.suffix(6)) + "seconds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func gestureUrl(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: DelayCurrentEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = TvThen(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ResumeSourceModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.visualCommunicationTitle(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        ViewBeginReactiveCompatible.capability()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        lineController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func anySave(msgData: TUIMessageCellData, sendModel: ResumeSourceModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        IndexSignatureProvider.shared.uploadThroughCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.appearShow(showMsg: (String(k_framePushInName.prefix(5)) + "d failed").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.visualCommunicationTitle(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    //: func func__addVoiceNotification() {
    func imageVideo() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(elect),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(statusBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(conversationInTitle(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(behindRow(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_formatContent,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(anyClick(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_matchData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func taproom(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            ContentPlayerDelegate.shared.element()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            ContentPlayerDelegate.shared.modelHandle()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func elect() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        taproom(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func statusBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        taproom(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func conversationInTitle(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            taproom(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            taproom(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: LengthReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func rawData(cellData: LengthReactiveCompatible, cellView: ContentMessageCell) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard SendSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.appearShow(showMsg: kLet_diskLineData)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: LengthReactiveCompatible.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: LengthReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! LengthReactiveCompatible
            let audioModel = indexModel as! LengthReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = MusicInfoManager.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = MusicInfoManager.cacheInCommunication(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        ContentPlayerDelegate.shared.settleToPoolPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if ContentPlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (k_resultValue) + "\(ContentPlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if ContentPlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                ContentPlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        ContentPlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        ContentPlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: LengthReactiveCompatible.self) {
                if subModel.isKind(of: LengthReactiveCompatible.self) {
                    //: let audioModel = subModel as! LengthReactiveCompatible
                    let audioModel = subModel as! LengthReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = MusicInfoManager.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = MusicInfoManager.cacheInCommunication(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: SmallnessMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    SmallnessMsgTable.sumervalCollection(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        ContentPlayerDelegate.shared.startArr(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func colorRe() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.inherit()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.imageAction(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: LengthReactiveCompatible.self) == true || indexModel?.isKind(of: ClickCellData.self) == true || indexModel?.isKind(of: BeginCellData.self) == true || indexModel?.isKind(of: ColorThen.self) == true {
                if indexModel?.isKind(of: LengthReactiveCompatible.self) == true || indexModel?.isKind(of: ClickCellData.self) == true || indexModel?.isKind(of: BeginCellData.self) == true || indexModel?.isKind(of: ColorThen.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.commentWith()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.recordEvent()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.terminateMsg()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func inherit() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.followOut(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.bulge(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.headwater()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == RowReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == RowReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == RowReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == RowReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.beforeCellWill(userWrap)
    }

    //: func func__getFillDraftText() {
    func cellInForeword() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kTransformName.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.addOfSecond()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func clickApp() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kTransformName.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    //: func func__TipsNotification() {
    func wish() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(onFile(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_appAddValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func onFile(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: UserCellData  = userinfo["msg"] as! UserCellData
        let celldata: UserCellData = userinfo[(k_leadingData.replacingOccurrences(of: "action", with: "g"))] as! UserCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func behindAllow(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: kCrushName.map{$0^169}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: kSucceedTitle.map{fullMoon(label: $0)}, encoding: .utf8)!: (String(k_labWithValue))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func recordEvent() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: IndexViewThen.chat_customerServiceCenter_insertTipMsg { tipsStr in
        IndexViewThen.viewHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.behindAllow(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func sumeractionAcrossStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.behindAllow(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func commentWith() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: IndexViewThen.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        IndexViewThen.snobbish(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.behindAllow(tipStr: String(bytes: k_keyTitle.map{liveFirst(center: $0)}, encoding: .utf8)!.roundName(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func behindRow(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(kReplyValue))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension DoingdDoorBeginViewController: SequesteredThen {
    //: func func__setReplyTipsView() {
    func toSave() {
        //: let key =  "\(RowReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(RowReactiveCompatible.share.loginUserMode.userID)" + (String(k_textContent) + String(k_kindText.prefix(4)) + "tTips" + String(k_leadingStatusCellContent.suffix(6))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.imageNor()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            modification(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            behindMakeView()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func fireReason(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        funcToModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        behindMakeView()
    }

    //: func removeReplyTipsView() {
    func behindMakeView() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            modification(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func barCell() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_barErrorTitle, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_nameValue - kLet_detailMarginTitle, width: kLet_errData, height: kLet_nameValue)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_errData,
                                     //: height: ScreenHeight)
                                     height: kLet_nameValue)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func mutual(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.belowResume() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if EventManager.bracketOut().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_videoValue, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ForenameViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_nameValue - kLet_detailMarginTitle,
                                     //: width: ScreenWidth,
                                     width: kLet_errData,
                                     //: height: ScreenHeight)
                                     height: kLet_nameValue)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if EventManager.bracketOut().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_videoValue, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension DoingdDoorBeginViewController {
    /// 创建UI
    //: private func createUI() {
    private func sound() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(kGestureLabelData) + String(kProgramText)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func giftTableConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_detailMarginTitle : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.quantityryAcross(width: contentView.width,
                                         //: height: contentView.height,
                                         height: contentView.height,
                                         //: corners: [.topLeft, .topRight],
                                         corners: [.topLeft, .topRight],
                                         //: cornerRadii: .init(width: 8, height: 8))
                                         cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_barPartyEndText : kLet_viewName)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_itemData)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_barPartyEndText : kLet_viewName)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_itemData)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_itemData)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func pothouse() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.lowliness()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.section()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.bodyType(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_maxValue.detailTing(eventID: kLet_detailData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
