
//: Declare String Begin

/*: "LIVE_NEED_OPEN_NOTIFICATION" :*/
fileprivate let k_routeName:String = "LIVE_Nstring class data return type"
fileprivate let kSectionToolText:String = "OPEN_lab model self regular"
fileprivate let kIndicatorValue:String = "FICATIObag"

/*: "LIVE_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let kStyleData:[UInt8] = [0xb2,0xb7,0xa8,0xbb,0xa1,0xad,0xaa,0xbf,0xaa,0xab,0xad,0xa1,0xbd,0xb6,0xbf,0xb0,0xb9,0xbb,0xa1,0xb0,0xb1,0xaa,0xb7,0xb8,0xb7,0xbd,0xbf,0xaa,0xb7,0xb1,0xb0]

/*: "live/startCheck" :*/
fileprivate let k_collectionName:String = "contenti"
fileprivate let kMatchManagerTrueData:String = "log add inve/sta"

/*: "live/start" :*/
fileprivate let k_giftLabelData:String = "lplayerve"

/*: "live/stop" :*/
fileprivate let k_endData:String = "LIV"
fileprivate let kCellConfirmValue:String = "e/stopin color var scale touch"

/*: "type" :*/
fileprivate let kMenuWithoutBlockValue:[UInt8] = [0x2b,0x26,0x2f,0x3a]

/*: "mf/user/dataCard" :*/
fileprivate let k_frameName:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let kBottomData:String = "dmodela"

/*: "uid" :*/
fileprivate let kPopValue:[UInt8] = [0x9d,0x81,0x8c]

private func makeModel(stage num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "streamerUid" :*/
fileprivate let k_managerValue:[UInt8] = [0x64,0x69,0x55,0x72,0x65,0x6d,0x61,0x65,0x72,0x74,0x73]

/*: "roomId" :*/
fileprivate let k_tapTitle:[Character] = ["r","o","o","m","I","d"]

/*: "Try again later, if you have any questions please contact customer service" :*/
fileprivate let k_viewTitle:[UInt8] = [0x91,0xb7,0xbc,0xe5,0xa4,0xa2,0xa4,0xac,0xab,0xe5,0xa9,0xa4,0xb1,0xa0,0xb7,0xe9,0xe5,0xac,0xa3,0xe5,0xbc,0xaa,0xb0,0xe5,0xad,0xa4,0xb3,0xa0,0xe5,0xa4,0xab,0xbc,0xe5,0xb4,0xb0,0xa0,0xb6,0xb1,0xac,0xaa,0xab,0xb6,0xe5,0xb5,0xa9,0xa0,0xa4,0xb6,0xa0,0xe5,0xa6,0xaa,0xab,0xb1,0xa4,0xa6,0xb1,0xe5,0xa6,0xb0,0xb6,0xb1,0xaa,0xa8,0xa0,0xb7,0xe5,0xb6,0xa0,0xb7,0xb3,0xac,0xa6,0xa0]

private func spectaclesAdd(input num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "redirectUrl" :*/
fileprivate let kUserTitle:String = "aspectediaspecte"

/*: "livePushUrl" :*/
fileprivate let kGiftValue:[Character] = ["l","i","v","e","P","u","s","h"]
fileprivate let k_titleCellName:String = "image minUrl"

/*: "FaceActionEvent_Interval_1_second" :*/
fileprivate let k_appearText:[UInt8] = [0xcb,0xe6,0xe8,0xea,0xc6,0xe8,0xf9,0xee,0xf4,0xf3,0xca,0xfb,0xea,0xf3,0xf9,0xe4,0xce,0xf3,0xf9,0xea,0xf7,0xfb,0xe6,0xf1,0xe4,0xb6,0xe4,0xf8,0xea,0xe8,0xf4,0xf3,0xe9]

fileprivate func inputColor(view num: UInt8) -> UInt8 {
    let value = Int(num) - 133
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "hasFace" :*/
fileprivate let kEarnValue:[Character] = ["h","a","s","F","a"]
fileprivate let kCrushName:String = "cmy"

/*: "Opening failed" :*/
fileprivate let kMoreValue:[Character] = ["O","p","e","n","i","n","g"," "]
fileprivate let kTalkData:String = "FAILED"

/*: "LIVE 状态码： :*/
fileprivate let kAreaName:String = "LIVfollow"

/*: ." :*/
fileprivate let kPathToData:String = "of"

/*: "Network busy!" :*/
fileprivate let kButtonValue:[Character] = ["N","e","t","w","o"]
fileprivate let kViewName:[Character] = ["r","k"]
fileprivate let k_statusName:[Character] = [" ","b","u","s","y","!"]

/*: "LIVE 异常下播，evtID: :*/
fileprivate let kToData:[Character] = ["L","I","V","E"," ","\u{5f02}","\u{5e38}","下","播","，","e"]
fileprivate let kAvailableName:[Character] = ["v","t","I","D",":"]

/*: "Live room exception" :*/
fileprivate let k_viewData:String = "Live rvar lab fatal"
fileprivate let k_infoTitle:String = "ccollectiontion"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithNeighbourReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2023/6/15.
//

//: import TXLiteAVSDK_Professional
import TXLiteAVSDK_Professional
//: import UIKit
import UIKit

// 是否需要恢复直播
//: public var NEED_RESTORE_LIVE = false
public var kLet_systemValue = false
// 唤起直播功能
//: public let LIVE_NEED_OPEN_NOTIFICATION = NSNotification.Name(rawValue: "LIVE_NEED_OPEN_NOTIFICATION")
public let kLet_agentContent = NSNotification.Name(rawValue: (String(k_routeName.prefix(6)) + "EED_" + String(kSectionToolText.prefix(5)) + "NOTI" + kIndicatorValue.replacingOccurrences(of: "bag", with: "N")))
// 直播状态变更通知
//: public let LIVE_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "LIVE_STATUS_CHANGE_NOTIFICATION")
public let kLet_successTimeTitle = NSNotification.Name(rawValue: String(bytes: kStyleData.map{$0^254}, encoding: .utf8)!)

/// 直播渲染配置
//: class TalkingLiveConfig: NSObject {
class NameReactiveCompatible: NSObject {
    //: var livePushUrl = ""                 // 推流地址
    var livePushUrl = "" // 推流地址
    //: var isSmallMode = false
    var isSmallMode = false // 是否小窗口
    //: var isMute = false
    var isMute = false // 是否静音
    //: var isRequest = false
    var isRequest = false // 正在请求中（防止多次调用）
    //: var frontCamera = true
    var frontCamera = true // 是否前置摄像头
}

//: class TalkingLiveManager: NSObject {
class WithNeighbourReactiveCompatible: NSObject {
    // 直播间消息
    //: var liveRoomModel = TalkingLiveRoomModel()
    var liveRoomModel = WorldHandyJSON()
    // 是否正在直播中（状态变更时发送通知）
    //: private(set) var isLive: Bool = false {
    private(set) var isLive: Bool = false {
        //: didSet {
        didSet {
            //: if RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue {
                //: NotificationCenter.default.post(name: LIVE_STATUS_CHANGE_NOTIFICATION, object: self)
                NotificationCenter.default.post(name: kLet_successTimeTitle, object: self)
            }
        }
    }

    // TXLive
    //: var config = TalkingLiveConfig()
    var config = NameReactiveCompatible() // 直播渲染配置类
    //: private weak var renderLiveView: UIView?
    private weak var renderLiveView: UIView? // 直播视图视频源
    //: private var localPreviewView: TalkingTRTCVideoView?
    private var localPreviewView: RowVideoView? // 本地预览view（使用商汤美颜）
    //: private static var _instance: TalkingLiveManager?
    private static var _instance: WithNeighbourReactiveCompatible? // singleton

    //: private override init() {}
    override private init() {}
    //: class func shared() -> TalkingLiveManager {
    class func clickEqual() -> WithNeighbourReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingLiveManager()
            _instance = WithNeighbourReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func skinValue() {
        //: if TalkingLiveManager._instance != nil {
        if WithNeighbourReactiveCompatible._instance != nil {
            //: TalkingLiveManager._instance = nil
            WithNeighbourReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 直播小窗口悬浮窗
    //: private lazy var liveMiniView: TalkingLiveMiniView = {
    private lazy var liveMiniView: BeginMiniView = {
        //: let mini = TalkingLiveMiniView.buildLiveMiniView()
        let mini = BeginMiniView.rearPush()
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 进入直播间
            //: self.live_updateTXLivePreview(self.liveRoomVC.renderView, smallWindow: false)
            self.withUp(self.liveRoomVC.renderView, smallWindow: false)
            //: guard let currentVC = self.currentViewController() else { return }
            guard let currentVC = self.belowResume() else { return }
            // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
            //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
            if currentVC.isKind(of: DoingdDoorBeginViewController.self) {
                //: if var vcArr = currentVC.navigationController?.viewControllers {
                if var vcArr = currentVC.navigationController?.viewControllers {
                    //: vcArr.removeLast()
                    vcArr.removeLast()
                    //: vcArr.append(self.liveRoomVC)
                    vcArr.append(self.liveRoomVC)
                    //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                    currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                }
                //: } else {
            } else {
                //: currentVC.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                currentVC.navigationController?.pushViewController(self.liveRoomVC, animated: true)
            }
        }
        //: return mini
        return mini
        //: }()
    }()

    // 直播控制器
    //: private lazy var liveRoomVC: TalkingLiveBeautifyViewController = {
    private lazy var liveRoomVC: RecordViewController = {
        //: let vc = TalkingLiveBeautifyViewController()
        let vc = RecordViewController()
        //: return vc
        return vc
        //: }()
    }()

    // 检测无人脸弹窗
    //: private lazy var noFaceAlert: TalkingLiveAlertView = {
    private lazy var noFaceAlert: BeanReactiveCompatible = //: return BeanReactiveCompatible(frame: .zero, type: .noFace)
        .init(frame: .zero, type: .noFace)
    //: }()

    // 直播推流对象
    //: private lazy var livePusher: TXLivePush = {
    private lazy var livePusher: TXLivePush = {
        //: let pushConfig = TXLivePushConfig()
        let pushConfig = TXLivePushConfig()
        //: pushConfig.frontCamera = self.config.frontCamera
        pushConfig.frontCamera = self.config.frontCamera
        //: pushConfig.customModeType |= CUSTOM_MODE_VIDEO_CAPTURE
        pushConfig.customModeType |= CUSTOM_MODE_VIDEO_CAPTURE
        // 标清
        //: let pusher = TXLivePush(config: pushConfig)
        let pusher = TXLivePush(config: pushConfig)
        //: pusher?.setVideoQuality(TX_Enum_Type_VideoQuality.VIDEO_QUALITY_STANDARD_DEFINITION,
        pusher?.setVideoQuality(TX_Enum_Type_VideoQuality.VIDEO_QUALITY_STANDARD_DEFINITION,
                                //: adjustBitrate: true,
                                adjustBitrate: true,
                                //: adjustResolution: false)
                                adjustResolution: false)
        //: return pusher!
        return pusher!
        //: }()
    }()
}

// MARK: - Request

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// 检测是否被禁用“开播功能”
    //: class func req_liveStartCheck(completion: @escaping FinishBlock) {
    class func sumimateGesture(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "live/startCheck"
        reqModel.requestPath = (k_collectionName.replacingOccurrences(of: "content", with: "l") + String(kMatchManagerTrueData.suffix(6)) + "rtCheck")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 开启直播
    //: class func req_liveStart(completion: @escaping FinishBlock) {
    class func makeCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "live/start"
        reqModel.requestPath = (k_giftLabelData.replacingOccurrences(of: "player", with: "i") + "/start")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 停止直播
    /// - Parameters:
    ///   - type: 停播类型：1=主动终止；2=没有人脸；3=通话中断
    ///   - completion: 回调
    //: class func req_liveStop(type: Int, completion: FinishBlock? = nil) {
    class func labCompletion(type: Int, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingMakePtolemaicSystemThen()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "live/stop"
        reqModel.requestPath = (k_endData.lowercased() + String(kCellConfirmValue.prefix(6)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: kMenuWithoutBlockValue.map{$0^95}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }

    /// - Parameters:
    ///  - uid: 卡片用户id
    ///   - streamerUid: 主播id
    //: class func req__userCardInfo(uid: String, streamerUid: String, roomId: String? = nil , completion: @escaping FinishBlock) {
    class func tarot(uid: String, streamerUid: String, roomId: String? = nil, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingMakePtolemaicSystemThen.init()
        let reqModel = TalkingMakePtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/dataCard"
        reqModel.requestPath = (String(k_frameName) + kBottomData.replacingOccurrences(of: "model", with: "at") + "Card")
        //: var params = ["uid": uid, "streamerUid": streamerUid]
        var params = [String(bytes: kPopValue.map{makeModel(stage: $0)}, encoding: .utf8)!: uid, String(bytes: k_managerValue.reversed(), encoding: .utf8)!: streamerUid]
        //: if let roomId = roomId {
        if let roomId = roomId {
            //: params["roomId"] = roomId
            params[(String(k_tapTitle))] = roomId
        }
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_gameName.onView(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}

// MARK: - 权限检测，通过后开启直播

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    // 检测直播状态，未开播时打开直播
    //: func checkLiveAuthAndTurnOn(completionHandler: ((_ isSucceed: Bool) -> Void)? = nil) {
    func consumerDetail(completionHandler: ((_ isSucceed: Bool) -> Void)? = nil) {
        //: guard self.isLive == false else { return }
        guard self.isLive == false else { return }
        //: guard self.config.isRequest == false else { return }
        guard self.config.isRequest == false else { return }
        //: self.config.isRequest = true
        self.config.isRequest = true

        // 1. 商汤美颜
        //: guard SenseTime_Use == true else {
        guard kLet_homeContent == true else {
            //: self.func__showStatusBarErrorMsg(showMsg: "Try again later, if you have any questions please contact customer service".localized)
            self.appearShow(showMsg: String(bytes: k_viewTitle.map{spectaclesAdd(input: $0)}, encoding: .utf8)!.localized)
            //: self.config.isRequest = false
            self.config.isRequest = false
            //: return
            return
        }

        // 2. 权限检测
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        AuthorizationReactiveCompatible.cellMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.appearShow(showMsg: kLet_senseValue)
                //: self.config.isRequest = false
                self.config.isRequest = false
                //: completionHandler?(false)
                completionHandler?(false)
                //: return
                return
            }
            // 3. 接口是否禁播检测
            //: TalkingLiveManager.req_liveStartCheck { succeed, result, errorModel in
            WithNeighbourReactiveCompatible.sumimateGesture { succeed, result, errorModel in
                //: guard succeed else {
                guard succeed else {
                    //: self.config.isRequest = false
                    self.config.isRequest = false
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? Dictionary<String, Any> {
                        if let result = result as? [String: Any] {
                            //: MiraclePushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            MiraclePushManager.share.pathTo(urlStr: result[(kUserTitle.replacingOccurrences(of: "aspect", with: "r") + "ctUrl")] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.appearShow(showMsg: errorModel!.errorMsg)
                    }
                    //: completionHandler?(false)
                    completionHandler?(false)
                    //: return
                    return
                }
                // 4. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard SendSocketDelegate.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.appearShow(showMsg: kLet_diskLineData)
                    //: self.config.isRequest = false
                    self.config.isRequest = false
                    //: completionHandler?(false)
                    completionHandler?(false)
                    //: return
                    return
                }

                // 校验成功，开启直播
                //: completionHandler?(true)
                completionHandler?(true)
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())

                // 进入直播间
                //: self.currentViewController()?.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                self.belowResume()?.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                //: self.live_createRoomAndStartPush(livePushUrl: json["livePushUrl"].stringValue)
                self.viewUrl(livePushUrl: json[(String(kGiftValue) + String(k_titleCellName.suffix(3)))].stringValue)
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// 展示直播违规弹窗
    //: static func live_showWarningView(content: String, reason: String, time: String) {
    static func tipEqual(content: String, reason: String, time: String) {
        //: let alert = TalkingLiveWarningView()
        let alert = IndexReactiveCompatible()
        //: alert.refreshUI(content: content, reason: reason, time: time)
        alert.occasion(content: content, reason: reason, time: time)
        //: alert.show()
        alert.cellTap()
    }

    /// 释放资源
    //: func live_releaseAllResource() {
    func nameObjectNeed() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isLive = false
            self.isLive = false
            //: self.config.isRequest = false
            self.config.isRequest = false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: NotificationCenter.default.removeObserver(self)
            NotificationCenter.default.removeObserver(self) // 移除通知
            //: TalkingSocketManager.shared.endLivePing()
            SendSocketDelegate.shared.eigenvalueOfAMatrix() // 结束心跳包
            //: self.liveMiniView.removeFromSuperview()
            self.liveMiniView.removeFromSuperview() // 移除小窗口
            //: self.liveRoomVC.popCurrentViewController()
            self.liveRoomVC.makeSub() // 将直播视图从栈中移除
            //: self.destoryTXLiveView()
            self.colorTable()
            // 释放单例
            //: TalkingLiveManager.destroy()
            WithNeighbourReactiveCompatible.skinValue()
            // 销毁弹幕
            //: TalkingDanmuManager.danmu_releaseAllResource()
            TalkingReactiveCompatible.resourceAllReleaseDanmu()
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AppTalkingApplication.shared as! AppTalkingApplication).animateBeing()
        }
    }

    /// 创建视频通话房间开始推流
    /// - Parameter livePushUrl: 推流地址
    //: func live_createRoomAndStartPush(livePushUrl: String) {
    func viewUrl(livePushUrl: String) {
        //: self.config.livePushUrl = livePushUrl
        self.config.livePushUrl = livePushUrl

        // 创建预览画面，开始直播（默认大屏）
        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
        //: self.live_updateTXLivePreview(self.liveRoomVC.renderView, smallWindow: false)
        self.withUp(self.liveRoomVC.renderView, smallWindow: false)
        //: self.live_startPush(livePushUrl: livePushUrl)
        self.columnFile(livePushUrl: livePushUrl)
    }

    /// 更新tx直播画面
    /// - Parameters:
    ///   - randerView: 渲染视图（默认为小窗口）
    ///   - smallWindow: 是否小窗口
    //: func live_updateTXLivePreview(_ randerView: UIView? = nil, smallWindow: Bool) {
    func withUp(_ randerView: UIView? = nil, smallWindow: Bool) {
        //: if randerView == nil {
        if randerView == nil {
            //: self.renderLiveView = self.liveMiniView.renderView
            self.renderLiveView = self.liveMiniView.renderView
            //: } else {
        } else {
            //: self.renderLiveView = randerView
            self.renderLiveView = randerView
        }

        //: self.liveMiniView.isHidden = !smallWindow
        self.liveMiniView.isHidden = !smallWindow
        //: self.config.isSmallMode = smallWindow
        self.config.isSmallMode = smallWindow
        // 清除小窗口未读消息标识
        //: if self.config.isSmallMode {
        if self.config.isSmallMode {
            //: updateUnredMessageCount(clear: true)
            wireSumClearCutUnredRestore(clear: true)
        }
        //: startTXLivePreview(smallWindow: smallWindow)
        giftAcross(smallWindow: smallWindow)
    }

    /// 前后摄像头切换
    /// - Parameter front: 是否前置
    //: func switchTRTCCamer(front: Bool) {
    func domicile(front: Bool) {
        //: guard self.config.frontCamera != front else { return }
        guard self.config.frontCamera != front else { return }
        //: self.config.frontCamera = front
        self.config.frontCamera = front

        //: guard SenseTime_Use == true else {
        guard kLet_homeContent == true else {
            //: livePusher.switchCamera()
            livePusher.switchCamera()
            //: return
            return
        }

        //: self.localPreviewView?.switchTRTCCamer(isFront: front)
        self.localPreviewView?.bagFront(isFront: front)
    }
}

// MARK: - 通知事件

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// 添加通知监听
    //: private func addNotifications() {
    private func wrapNotifications() {
        // 监听是否捕获到人脸
        //: if SenseTime_Use == true {
        if kLet_homeContent == true {
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(applicationNotification(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_1_second"),
                                                   name: NSNotification.Name(rawValue: String(bytes: k_appearText.map{inputColor(view: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillTerminate),
                                               selector: #selector(answerTerminate),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 程序销毁时调用停播接口
    //: @objc private func appWillTerminate() {
    @objc private func answerTerminate() {
        //: guard self.isLive == true else { return }
        guard self.isLive == true else { return }
        //: TalkingLiveManager.req_liveStop(type: 1)
        WithNeighbourReactiveCompatible.labCompletion(type: 1)
    }
}

// MARK: - 未读消息提醒标识

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// 更新未读消息数量
    /// - Parameter clear: 是否清空
    //: func updateUnredMessageCount(clear: Bool = false) {
    func wireSumClearCutUnredRestore(clear: Bool = false) {
        //: guard self.config.isSmallMode else { return }
        guard self.config.isSmallMode else { return }
        //: if clear {
        if clear {
            //: self.liveMiniView.msgCount = 0
            self.liveMiniView.msgCount = 0
            //: } else {
        } else {
            //: self.liveMiniView.msgCount += 1
            self.liveMiniView.msgCount += 1
        }
    }
}

// MARK: - 商汤人脸检测

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// 商汤美颜是否捕获到人脸通知（间隔1秒）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func applicationNotification(notification: NSNotification) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard self.isLive == true else { return }
            guard self.isLive == true else { return }
            //: let userInfo = notification.userInfo as! [String: AnyObject]
            let userInfo = notification.userInfo as! [String: AnyObject]
            //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
            let hasFace = (userInfo[(String(kEarnValue) + kCrushName.replacingOccurrences(of: "my", with: "e"))] as! NSNumber).description.boolValue
            //: if hasFace == true {
            if hasFace == true {
                //: self.liveMiniView.noFaceTime = 0
                self.liveMiniView.noFaceTime = 0
                //: self.liveRoomVC.topView.noFaceTime = 0
                self.liveRoomVC.topView.noFaceTime = 0
                //: } else {
            } else {
                //: self.liveMiniView.noFaceTime += 1
                self.liveMiniView.noFaceTime += 1
                //: self.liveRoomVC.topView.noFaceTime += 1
                self.liveRoomVC.topView.noFaceTime += 1
            }
            // 连续X秒无人脸出现直接下播
            //: if self.liveMiniView.noFaceTime == MAX_NOFACETIME {
            if self.liveMiniView.noFaceTime == kLet_senseData {
                //: self.interval_noFace()
                self.noFace()
            }
            // 开启超时检测（防止直播时长时间后台、锁屏）
            //: self.interval_cancelPreviousPerformRequest()
            self.noneTime()
            //: self.interval_startPerformRequest()
            self.latentPeriod()
        }
    }

    /// 开启超时检测（防止直播时长时间后台、锁屏）
    //: private func interval_startPerformRequest() {
    private func latentPeriod() {
        //: self.perform(#selector(interval_noFace), with: nil, afterDelay: 30)
        self.perform(#selector(noFace), with: nil, afterDelay: 30)
    }

    /// 取消超时检测
    //: private func interval_cancelPreviousPerformRequest() {
    private func noneTime() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self,
        NSObject.cancelPreviousPerformRequests(withTarget: self,
                                               //: selector: #selector(interval_noFace),
                                               selector: #selector(noFace),
                                               //: object: nil)
                                               object: nil)
    }

    /// 未检测到人脸超过30秒
    //: @objc private func interval_noFace() {
    @objc private func noFace() {
        //: guard self.isLive == true else { return }
        guard self.isLive == true else { return }
        //: TalkingLiveManager.req_liveStop(type: 2)
        WithNeighbourReactiveCompatible.labCompletion(type: 2)
        //: self.live_releaseAllResource()
        self.nameObjectNeed()
        //: self.noFaceAlert.show()
        self.noFaceAlert.countShow()
    }
}

// MARK: - TX直播

//: extension TalkingLiveManager {
extension WithNeighbourReactiveCompatible {
    /// tx本地预览视频画面
    /// - Parameter smallWindow: 是否自己的画面在小窗口
    //: private func startTXLivePreview(smallWindow: Bool) {
    private func giftAcross(smallWindow: Bool) {
        //: guard SenseTime_Use == true else {
        guard kLet_homeContent == true else {
            //: let beautyManger = livePusher.getBeautyManager()
            let beautyManger = livePusher.getBeautyManager()
            //: beautyManger?.setBeautyStyle(.nature)
            beautyManger?.setBeautyStyle(.nature)
            //: beautyManger?.setBeautyLevel(6)
            beautyManger?.setBeautyLevel(6)

            //: livePusher.startPreview(self.renderLiveView)
            livePusher.startPreview(self.renderLiveView)
            //: return
            return
        }

        //: if self.localPreviewView == nil {
        if self.localPreviewView == nil {
            //: self.localPreviewView = TalkingTRTCVideoView()
            self.localPreviewView = RowVideoView()
        }
        //: self.localPreviewView?.startPreView(isSmallWindow: smallWindow, livePusher: livePusher)
        self.localPreviewView?.thoughtImage(isSmallWindow: smallWindow, livePusher: livePusher)
        //: if self.localPreviewView?.superview != self.renderLiveView {
        if self.localPreviewView?.superview != self.renderLiveView {
            //: self.localPreviewView?.removeFromSuperview()
            self.localPreviewView?.removeFromSuperview()
            //: self.renderLiveView?.addSubview(self.localPreviewView!)
            self.renderLiveView?.addSubview(self.localPreviewView!)
            //: self.localPreviewView?.snp.makeConstraints({ make in
            self.localPreviewView?.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
                //: })
            }
        }
    }

    /// 开始直播推流
    /// - Parameter livePushUrl: 推流地址
    //: private func live_startPush(livePushUrl: String) {
    private func columnFile(livePushUrl: String) {
        //: if SenseTime_Use {
        if kLet_homeContent {
            //: } else {
        } else {
            //: let beautyManger = livePusher.getBeautyManager()
            let beautyManger = livePusher.getBeautyManager()
            //: beautyManger?.setBeautyStyle(.nature)
            beautyManger?.setBeautyStyle(.nature)
            //: beautyManger?.setBeautyLevel(6)
            beautyManger?.setBeautyLevel(6)
        }

        //: livePusher.delegate = self
        livePusher.delegate = self
        //: livePusher.setMute(self.config.isMute)
        livePusher.setMute(self.config.isMute)
        //: let result = livePusher.start(livePushUrl)
        let result = livePusher.start(livePushUrl)
        //: onEnterRoom(Int(result))
        rotunda(Int(result))
    }

    /// 销毁tx直播
    //: private func destoryTXLiveView() {
    private func colorTable() {
        //: if SenseTime_Use {
        if kLet_homeContent {
            //: self.localPreviewView?.stopPreView()
            self.localPreviewView?.serverCell()
            //: } else {
        } else {
            //: self.livePusher.stopPreview()
            self.livePusher.stopPreview()
        }

        //: self.localPreviewView?.removeFromSuperview()
        self.localPreviewView?.removeFromSuperview()
        //: self.localPreviewView = nil
        self.localPreviewView = nil

        //: self.livePusher.stop()
        self.livePusher.stop()
    }

    /// 进入直播间，调用接口
    //: func onEnterRoom(_ result: Int) {
    func rotunda(_ result: Int) {
        //: guard result >= 0 else {
        guard result >= 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "Opening failed".localized)
            self.appearShow(showMsg: (String(kMoreValue) + kTalkData.lowercased()).localized)
            //: self.live_releaseAllResource()
            self.nameObjectNeed()
            //: return
            return
        }

        // 加入房间成功，调用开播接口
        //: TalkingLiveManager.req_liveStart { succeed, result, errorModel in
        WithNeighbourReactiveCompatible.makeCompletion { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: self.live_releaseAllResource()
                self.nameObjectNeed()
                //: return
                return
            }
            //: if let model = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) {
            if let model = WorldHandyJSON.deserialize(from: result as? Dictionary) {
                //: self.liveRoomModel = model
                self.liveRoomModel = model
            }
            // 加入弹幕
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.liveRoomModel.chatGroupId, announcementMsg: self.liveRoomModel.notification)
            TalkingReactiveCompatible.makeTo(groupId: self.liveRoomModel.chatGroupId, announcementMsg: self.liveRoomModel.notification)
            // 开播成功
            //: self.isLive = true
            self.isLive = true
            //: self.liveMiniView.refreshMiniView()
            self.liveMiniView.appearDismissLocal()
            //: self.liveRoomVC.refreshLiveRoomView()
            self.liveRoomVC.buttonDestroy()
            //: TalkingSocketManager.shared.startLivePing()
            SendSocketDelegate.shared.indicatorPing()
            //: self.addNotifications()
            self.wrapNotifications()
            // 销毁上传busy状态定时器
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (AppTalkingApplication.shared as! AppTalkingApplication).blockTimer()
        }
    }
}

// MARK: - TXLivePushListener

//: extension TalkingLiveManager: TXLivePushListener {
extension WithNeighbourReactiveCompatible: TXLivePushListener {
    //: func onPushEvent(_ evtID: Int32, withParam param: [AnyHashable: Any]!) {
    func onPushEvent(_ evtID: Int32, withParam _: [AnyHashable: Any]!) {
        //: JumpLogTool.lineLoad(msg: "LIVE 状态码：\(evtID).")
        JumpLogTool.lineLoad(msg: (kAreaName.replacingOccurrences(of: "follow", with: "E") + " 状\u{6001}码\u{ff1a}") + "\(evtID).")
        //: if evtID == PUSH_WARNING_NET_BUSY.rawValue {
        if evtID == PUSH_WARNING_NET_BUSY.rawValue {
            //: self.func__showStatusBarErrorMsg(showMsg: "Network busy!".localized)
            self.appearShow(showMsg: (String(kButtonValue) + String(kViewName) + String(k_statusName)).localized)

            //: } else if evtID == PUSH_ERR_OPEN_CAMERA_FAIL.rawValue ||
        } else if evtID == PUSH_ERR_OPEN_CAMERA_FAIL.rawValue ||
            //: evtID == PUSH_ERR_OPEN_MIC_FAIL.rawValue ||
            evtID == PUSH_ERR_OPEN_MIC_FAIL.rawValue ||
            //: evtID == PUSH_ERR_VIDEO_ENCODE_FAIL.rawValue ||
            evtID == PUSH_ERR_VIDEO_ENCODE_FAIL.rawValue ||
            //: evtID == PUSH_ERR_AUDIO_ENCODE_FAIL.rawValue ||
            evtID == PUSH_ERR_AUDIO_ENCODE_FAIL.rawValue ||
            //: evtID == PUSH_ERR_UNSUPPORTED_RESOLUTION.rawValue ||
            evtID == PUSH_ERR_UNSUPPORTED_RESOLUTION.rawValue ||
            //: evtID == PUSH_ERR_UNSUPPORTED_SAMPLERATE.rawValue ||
            evtID == PUSH_ERR_UNSUPPORTED_SAMPLERATE.rawValue ||
            //: evtID == PUSH_ERR_NET_DISCONNECT.rawValue ||
            evtID == PUSH_ERR_NET_DISCONNECT.rawValue ||
            //: evtID == PUSH_ERR_AUDIO_SYSTEM_NOT_WORK.rawValue ||
            evtID == PUSH_ERR_AUDIO_SYSTEM_NOT_WORK.rawValue ||
            //: evtID == PUSH_ERR_INVALID_ADDRESS.rawValue ||
            evtID == PUSH_ERR_INVALID_ADDRESS.rawValue ||
            //: evtID == PUSH_ERR_CONNECT_SERVER_FAILED.rawValue ||
            evtID == PUSH_ERR_CONNECT_SERVER_FAILED.rawValue ||
            //: evtID == PUSH_ERR_NETWORK_UNAVAIABLE.rawValue ||
            evtID == PUSH_ERR_NETWORK_UNAVAIABLE.rawValue ||
            //: evtID == PUSH_ERR_SERVER_REFUSED.rawValue {
            evtID == PUSH_ERR_SERVER_REFUSED.rawValue
        {
            // 主播异常下播
            //: JumpLogTool.lineLoad(msg: "LIVE 异常下播，evtID:\(evtID).")
            JumpLogTool.lineLoad(msg: (String(kToData) + String(kAvailableName)) + "\(evtID).")
            //: self.func__showStatusBarErrorMsg(showMsg: "Live room exception".localized)
            self.appearShow(showMsg: (String(k_viewData.prefix(6)) + "oom ex" + k_infoTitle.replacingOccurrences(of: "collection", with: "ep")).localized)
            //: TalkingLiveManager.req_liveStop(type: 1)
            WithNeighbourReactiveCompatible.labCompletion(type: 1)
            //: live_releaseAllResource()
            nameObjectNeed()
            // 重新开播弹窗
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
                let alert = BeanReactiveCompatible(frame: .zero, type: .turnOnLive)
                //: alert.rightBlcok = {
                alert.rightBlcok = {
                    //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                    WithNeighbourReactiveCompatible.clickEqual().consumerDetail()
                }
                //: alert.show()
                alert.countShow()
            }
        }
    }

    //: func onNetStatus(_ param: [AnyHashable: Any]!) {}
    func onNetStatus(_: [AnyHashable: Any]!) {}

    //: func onScreenCaptureStarted() {}
    func onScreenCaptureStarted() {}

    //: func onScreenCapturePaused(_ reason: Int32) {}
    func onScreenCapturePaused(_: Int32) {}

    //: func onScreenCaptureResumed(_ reason: Int32) {}
    func onScreenCaptureResumed(_: Int32) {}

    //: func onScreenCaptureStoped(_ reason: Int32) {}
    func onScreenCaptureStoped(_: Int32) {}
}
