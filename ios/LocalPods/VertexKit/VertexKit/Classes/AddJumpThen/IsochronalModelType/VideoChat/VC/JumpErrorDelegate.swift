
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kShareValue:[UInt8] = [0x65,0x62,0x65,0x78,0x24,0x6f,0x63,0x68,0x69,0x7e,0x36,0x25,0x2c,0x64,0x6d,0x7f,0x2c,0x62,0x63,0x78,0x2c,0x6e,0x69,0x69,0x62,0x2c,0x65,0x61,0x7c,0x60,0x69,0x61,0x69,0x62,0x78,0x69,0x68]

/*: "uid" :*/
fileprivate let k_userValue:[UInt8] = [0x9,0x15,0x18]

/*: "headPic" :*/
fileprivate let k_selectedName:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let k_modelTitle:String = "size"
fileprivate let kPushContent:[Character] = ["c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let kRequestValue:[Character] = ["b","i","r","t","h"]
fileprivate let kModelText:[Character] = ["d","a","y"]

/*: "videoUrl" :*/
fileprivate let kActionMoveName:[Character] = ["v","i","d","e","o","U"]
fileprivate let k_topContent:String = "rshare"

/*: "location" :*/
fileprivate let k_buttonData:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let k_borderAppTitle:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let k_blockData:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let kGroupName:[Character] = ["a","l","l"]

/*: "data" :*/
fileprivate let kEqualTitle:[UInt8] = [0xdc,0xd9,0xcc,0xd9]

private func viewExtension(user num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "Call interruption" :*/
fileprivate let kExceptionData:String = "super make topCall "
fileprivate let k_liveData:String = "RRUPTION"

/*: "stopCall" :*/
fileprivate let kLinkName:String = "stmodel"
fileprivate let k_messageData:String = "err nor tablepCall"

/*: "logId" :*/
fileprivate let kMakeContent:[UInt8] = [0xe1,0xe4,0xdc,0xbe,0xd9]

fileprivate func currentImage(key num: UInt8) -> UInt8 {
    let value = Int(num) - 117
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let k_firstName:String = "scale"
fileprivate let k_modelName:[Character] = ["a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let k_videoContent:[Character] = ["N","o"," ","a"]
fileprivate let k_tableValue:[Character] = ["n","s","w","e","r"]

/*: "duration" :*/
fileprivate let k_withData:[Character] = ["d","u"]
fileprivate let k_arrayValue:[Character] = ["r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let k_addBottomContent:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n","g"," ","u"]
fileprivate let kUserData:String = "record"

/*: "cancelSex" :*/
fileprivate let kFatalValue:String = "video"
fileprivate let kItemValue:String = "model model makeancelSex"

/*: "He has hung up" :*/
fileprivate let k_makeName:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u"]
fileprivate let k_sizeValue:String = "lab"

/*: "endType" :*/
fileprivate let kShowTitle:String = "block"
fileprivate let k_pathName:[Character] = ["n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let k_indexName:String = "stopSein model self"
fileprivate let kRefreshValue:String = "result"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kLiveTitle:[UInt8] = [0xad,0x8a,0x97,0x91,0x82,0x82,0x8d,0x87,0x8d,0x81,0x8a,0x90,0xc4,0x83,0x8b,0x88,0x80,0xc4,0x87,0x8b,0x8d,0x8a,0x97,0xc8,0xc4,0x87,0x85,0x88,0x88,0xc4,0x8c,0x85,0x97,0xc4,0x81,0x8a,0x80,0x81,0x80]

private func pathMessage(make num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "The other hung up the phone" :*/
fileprivate let kViewValue:[Character] = ["T","h","e"," ","o","t","h"]
fileprivate let kMyValue:[Character] = ["e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let k_actualText:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let k_edgeScaleProText:String = "jumpUrlmodel text cell"

/*: "Cancel" :*/
fileprivate let kHeadName:String = "c"
fileprivate let kLocationTitle:String = "ancesize"

/*: "Get gold coins" :*/
fileprivate let k_mediumName:[Character] = ["G","e","t"," ","g","o","l","d"]
fileprivate let kTotalTitle:String = "index hidden center current data coins"

/*: "addBean" :*/
fileprivate let kMakeText:String = "normal windowaddBe"
fileprivate let kByName:[Character] = ["a","n"]

/*: "refuseSex" :*/
fileprivate let kValueName:String = "rcancelfu"

/*: "onAcceptCall" :*/
fileprivate let k_displayValue:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let k_sharedIconValue:[Character] = ["a","c","c","e","p","t","C","a","l"]
fileprivate let kCellValue:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let kTitleValue:String = "guard makeCalls"
fileprivate let k_viewTitle:String = "rate app canc"
fileprivate let kVideoTitle:[Character] = ["e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let k_momentValue:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d"]
fileprivate let kCenterViewContent:String = "econtent"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  JumpErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_showFailName = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class JumpErrorDelegate: RecordReactiveCompatible {
	var timeOff: Bool = true
	var valueQuantity: Int = 72
	var tapDestroyDictionary: [AnyHashable: String] = [:]

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: MakeChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SphericPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ServiceMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SendSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SendSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SendSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kShareValue.map{$0^12}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! IndexControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.judge()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            toolStatus()
        }
    
		
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.informationIconInitEffect()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        buttonDown()
        //: setupSubViewsConstraint()
        restrictionTotalerchangeViewsEqual()
        //: initNotificationObserver()
        dismissService()
        //: req_callGetUserInfo()
        setOutDoing()
        //: timeoutRules()
        queryedImage()
        //: resumeTalk()
        itemTalk()
        //: setUpBGMPlayer()
        control()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ScreenDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        SendSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.saveCell()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: TitleThen = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TitleThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: WorldViewCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = WorldViewCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func setOutDoing() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RefreshThen.sharedTag(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(k_selectedName))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(k_modelTitle.replacingOccurrences(of: "size", with: "ni") + String(kPushContent))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kRequestValue) + String(kModelText))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(kActionMoveName) + k_topContent.replacingOccurrences(of: "share", with: "l"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(k_buttonData))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.reading()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func toolStatus() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AuthorizationReactiveCompatible.twinkle(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.appearShow(showMsg: kLet_senseValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (String(k_blockData) + String(kGroupName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.popAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.cancelConstraint()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SphericPlayListener.titleType(type: .Response)
            //: self.startBGM()
            self.judge()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func saveCell() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.cancelConstraint()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SendSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SendSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SendSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func popAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.cancelConstraint()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func sunporchSucceed() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.loadTag(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func itemTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            sunporchSucceed()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func dismissService() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(capacityDoing(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(doText(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_giftContent,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_showFailName.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func capacityDoing(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            popAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func doText(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SendSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SendSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SendSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SendSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.thanRequestSickbay(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension JumpErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.appearShow(showMsg: (String(kExceptionData.suffix(5)) + "inte" + k_liveData.lowercased()).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (kLinkName.replacingOccurrences(of: "model", with: "o") + String(k_messageData.suffix(5))), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (k_firstName.replacingOccurrences(of: "scale", with: "c") + String(k_modelName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (String(k_blockData) + String(kGroupName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.rowData()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SendSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            popAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.anyGift(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func control() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DelayTextConvertible.Call : DelayTextConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SphericPlayListener.titleType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func judge() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.priceUp()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func cancelConstraint() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.barDoingCurrent()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func queryedImage() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_connectValue : self.chatModel?.serviceTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            begin()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.begin()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func begin() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !SendSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.appearShow(showMsg: (String(k_videoContent) + String(k_tableValue)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (k_firstName.replacingOccurrences(of: "scale", with: "c") + String(k_modelName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (String(k_blockData) + String(kGroupName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SendSocketDelegate.shared.castToDataFormattingSub(info: socketDict)
        //: popCurrentViewController()
        popAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func userData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(k_withData) + String(k_arrayValue))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.clipStartReloadTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.logKey()
            //: createTalkRoom()
            sunporchSucceed()
            //: self.stopBGM()
            self.cancelConstraint()
        }
    }
}

// MARK: - UserObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension JumpErrorDelegate: UserObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func vacuumBagData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_addBottomContent) + kUserData.replacingOccurrences(of: "record", with: "p")).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(kFatalValue.replacingOccurrences(of: "video", with: "c") + String(kItemValue.suffix(8)))].stringValue == AppTextProtocol.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_makeName) + k_sizeValue.replacingOccurrences(of: "lab", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        popAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func aggregationButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.cancelConstraint()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.logKey()
        //: self.createTalkRoom()
        self.sunporchSucceed()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func outsideEqual(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(kShowTitle.replacingOccurrences(of: "block", with: "e") + String(k_pathName))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(k_addBottomContent) + kUserData.replacingOccurrences(of: "record", with: "p")).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_indexName.prefix(6)) + kRefreshValue.replacingOccurrences(of: "result", with: "x"))].stringValue == AppTextProtocol.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_makeName) + k_sizeValue.replacingOccurrences(of: "lab", with: "p")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kLiveTitle.map{pathMessage(make: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kViewValue) + String(kMyValue)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.fileImage()
        //: self.popCurrentViewController()
        self.popAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_errorData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func statUp(msg: String, data: [String: Any]) {
        //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(k_actualText.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_edgeScaleProText.prefix(7)))] as? String else { return }
            //: let httpsUrl = RowReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RowReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = GiftAddConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: MiraclePushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            MiraclePushManager.share.pathTo(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            CurrentThen.magnitudeimateCan(title: nil,
                                               //: message: msg,
                                               message: msg,
                                               //: leftBtnTitle: "Cancel".localized,
                                               leftBtnTitle: (kHeadName.uppercased() + kLocationTitle.replacingOccurrences(of: "size", with: "l")).localized,
                                               //: rightBtnTitle: "Get gold coins".localized) {
                                               rightBtnTitle: (String(k_mediumName) + String(kTotalTitle.suffix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                CurrentThen.meeting()
                //: } rightBlock: {
            } rightBlock: {
                //: MiraclePushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                MiraclePushManager.share.noBuild(clickEvent: kLet_environmentValue, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func modelMessage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(kMakeText.suffix(5)) + String(kByName))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.nationalService(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.playerAdd(point: addBean)
        }
    }
}

// MARK: - RecordObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension JumpErrorDelegate: RecordObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func levelBar(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_addBottomContent) + kUserData.replacingOccurrences(of: "record", with: "p")).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(kValueName.replacingOccurrences(of: "cancel", with: "e") + "seSex")].stringValue == AppTextProtocol.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_makeName) + k_sizeValue.replacingOccurrences(of: "lab", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        popAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func tableData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.cancelConstraint()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.logKey()
        //: self.createTalkRoom()
        self.sunporchSucceed()
    }
}

// MARK: - DirectorObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension JumpErrorDelegate: DirectorObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func anonymous(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_blockData) + String(kGroupName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.appearShow(showMsg: (String(k_videoContent) + String(k_tableValue)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.rowData()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SendSocketDelegate.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.popAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(k_displayValue)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else { return }
            //: MiraclePushManager.share.func__jumpToWebRecharge(sufficient: false)
            MiraclePushManager.share.noBuild(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(k_displayValue)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.appearShow(showMsg: msg)
            //: self.popCurrentViewController()
            self.popAnimated()
        }
    }
}

// MARK: - ClickViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension JumpErrorDelegate: ClickViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func sumeractionFromRefer() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (String(k_sharedIconValue) + String(kCellValue)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func part() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.appearShow(showMsg: (String(kTitleValue.suffix(5)) + " are" + String(k_viewTitle.suffix(5)) + String(kVideoTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (k_firstName.replacingOccurrences(of: "scale", with: "c") + String(k_modelName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.appearShow(showMsg: (String(kTitleValue.suffix(5)) + " are" + String(k_viewTitle.suffix(5)) + String(kVideoTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (String(k_blockData) + String(kGroupName)), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: k_userValue.map{$0^124}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.appearShow(showMsg: (String(k_momentValue) + kCenterViewContent.replacingOccurrences(of: "content", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_borderAppTitle.reversed(), encoding: .utf8)!: (kLinkName.replacingOccurrences(of: "model", with: "o") + String(k_messageData.suffix(5))), String(bytes: kEqualTitle.map{viewExtension(user: $0)}, encoding: .utf8)!: [String(bytes: kMakeContent.map{currentImage(key: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.fileImage()
        //: self.popCurrentViewController()
        self.popAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func nearList(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.inputSignal(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func pointHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.set(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func miniskirt() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        SendSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ServiceMiniView.failure()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.belowResume()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.clearGet()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.writeAbout(talkingTime)
        //: popCurrentViewController()
        popAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func representativeTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.writeAbout(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func viewText(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.valueTo(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension JumpErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func buttonDown() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func restrictionTotalerchangeViewsEqual() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
