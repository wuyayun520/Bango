
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_selectedText:[UInt8] = [0x6c,0x6b,0x6c,0x71,0x2d,0x66,0x6a,0x61,0x60,0x77,0x3f,0x2c,0x25,0x6d,0x64,0x76,0x25,0x6b,0x6a,0x71,0x25,0x67,0x60,0x60,0x6b,0x25,0x6c,0x68,0x75,0x69,0x60,0x68,0x60,0x6b,0x71,0x60,0x61]

private func imageInfo(label num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "uid" :*/
fileprivate let k_firstName:[UInt8] = [0x1d,0x1,0xc]

private func lastMake(better num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "headPic" :*/
fileprivate let kPathData:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let k_fromName:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let kViewData:String = "text"
fileprivate let kShowTitle:String = "IRTHDAY"

/*: "videoUrl" :*/
fileprivate let k_cellText:String = "VIDE"

/*: "cover" :*/
fileprivate let kInfoName:String = "cgifter"

/*: "location" :*/
fileprivate let kTingName:String = "LOCATION"

/*: "cmd" :*/
fileprivate let k_shareViewData:[UInt8] = [0x9a,0x94,0x9d]

private func telephoneConversation(mode num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "refuseCall" :*/
fileprivate let k_topTitle:String = "direction"
fileprivate let kByContent:String = "efupush"

/*: "data" :*/
fileprivate let kLengthLabelName:[UInt8] = [0x83,0x80,0x93,0x80]

fileprivate func modelValue(gift num: UInt8) -> UInt8 {
    let value = Int(num) + 225
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let k_frameName:[Character] = ["C","a","l","l"," ","i","n","t","e","r"]
fileprivate let kPlayTitle:[Character] = ["r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let k_indicatorName:String = "icon"
fileprivate let kPopValue:[Character] = ["t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let k_indexValue:[UInt8] = [0x5b,0x58,0x50,0x7e,0x53]

/*: "cancelCall" :*/
fileprivate let k_labelActualData:String = "canceresult"

/*: "No answer" :*/
fileprivate let k_observerTitle:String = "view model info allNo answ"
fileprivate let k_presentName:String = "ER"

/*: "duration" :*/
fileprivate let k_pointName:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let k_layerData:String = "She equal equal section"
fileprivate let k_writeShowPlayerValue:String = "hung upfeature title return add"

/*: "cancelSex" :*/
fileprivate let k_modelMakeData:String = "currentnce"

/*: "He has hung up" :*/
fileprivate let k_imageData:String = "He hself let fill var false"
fileprivate let kAppName:String = "ung normal image if manager"
fileprivate let kLabText:String = "mediump"

/*: "endType" :*/
fileprivate let kSendQuantityimateData:String = "layer false imageendTy"
fileprivate let k_contentDiskData:[Character] = ["p","e"]

/*: "stopSex" :*/
fileprivate let k_rawResultData:[Character] = ["s","t","o","p","S","e"]
fileprivate let k_privacyName:[Character] = ["x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kPingContent:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x73,0x6e,0x69,0x6f,0x63,0x20,0x64,0x6c,0x6f,0x67,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let kObserverTitle:String = "height content image typeThe o"
fileprivate let k_startData:String = "hubackgroundg"
fileprivate let kCancelValue:String = "size event self manager viewhe pho"
fileprivate let kVideoText:[Character] = ["n","e"]

/*: "type" :*/
fileprivate let kBottomTitle:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let kFileData:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let k_frameValue:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let k_tableName:String = "status agentGet g"
fileprivate let kClearText:String = "momentins"

/*: "addBean" :*/
fileprivate let k_tapTitle:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let k_styleToName:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kCookieActionValue:String = "table skinonAccep"
fileprivate let k_textModelData:[Character] = ["t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let kVerticalValue:String = "infocce"

/*: "Calls are cancelled" :*/
fileprivate let kQuoteData:String = "Calls back make touch item any"
fileprivate let k_destroyTitle:String = "aneffectelle"
fileprivate let k_informationText:[Character] = ["d"]

/*: "The call has ended" :*/
fileprivate let k_applicationData:String = "The ctype video label self"
fileprivate let k_equalAtValue:[Character] = ["a","l","l"," ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LastViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_userData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class LastViewController: RecordReactiveCompatible {
	var tableDoing: Bool = true
	var bindMagnitude: Double = -91.6
	var requestArray: [AnyHashable] = []
	var elementEnable: Bool = false
	var sizeSum: Double = -44.2
	var opArray: [AnyHashable] = []
	var giftAcceptDictionary: [AnyHashable: String] = [:]

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: MakeChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SphericPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: EqualRefreshPositionMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: FrontReactiveCompatible? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SendSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SendSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SendSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_selectedText.map{imageInfo(label: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! IndexControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.tableBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            authData()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            fatherToTreatNurseLogId(logId: self.chatModel?.logId)
        }
    
		
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.instanceObject()
    
		
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        delouseSubviews()
        //: setupSubViewsConstraint()
        enterImage()
        //: initNotificationObserver()
        signal()
        //: req_callGetUserInfo()
        giftKey()
        //: timeoutRules()
        visibleMessage()
        //: setUpBGMPlayer()
        motivation()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.sizePreview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RowReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ScreenDataReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_errData, height: kLet_nameValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        SendSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.constraintCheck()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ColorAppReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = ColorAppReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.observerTask(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: EnrichThen = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = EnrichThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: SelectedBagReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = SelectedBagReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_errData - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if TextLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_topData + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.perspectiveSession()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RefreshThen = {
        //: let m = TalkingVideoChatManager()
        let m = RefreshThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func giftKey() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RefreshThen.sharedTag(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kPathData))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(k_fromName))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(kViewData.replacingOccurrences(of: "text", with: "b") + kShowTitle.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(k_cellText.lowercased() + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(kInfoName.replacingOccurrences(of: "gift", with: "ov"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(kTingName.lowercased())].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.paradigmInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.countimate()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func countimate() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = EventCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = EventCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.viewData(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.clickShared(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func authData() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        AuthorizationReactiveCompatible.cellMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.appearShow(showMsg: kLet_senseValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_topTitle.replacingOccurrences(of: "direction", with: "r") + kByContent.replacingOccurrences(of: "push", with: "se") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.enablegoingStart()
                //: return
                return
            }

            //: self.startPreview()
            self.sizePreview()
            //: self.stopBGM()
            self.fileTo()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SphericPlayListener.titleType(type: .Response)
            //: self.startBGM()
            self.tableBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func sizePreview() {
        //: self.videoManager.startPreview()
        self.videoManager.toClick()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func constraintCheck() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.fileTo()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TalkingDetailDanmuManager.resourceDoing()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SendSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SendSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SendSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func enablegoingStart(animated: Bool = true) {
        //: self.stopBGM()
        self.fileTo()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func perspectiveSession() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.sign()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimate()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func soliloquise() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.upPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimate()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.bioLabAnimation()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func bioLabAnimation() {
        //: guard RowReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard RowReactiveCompatible.share.loginUserMode.sex == AppTextProtocol.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = FrontReactiveCompatible()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.nextMini()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func signal() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(timeNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(self.notification(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_domainName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(quitNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_giftContent,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_userData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func timeNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            enablegoingStart()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func quitNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SendSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SendSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SendSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SendSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.priceCurrent()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func notification(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimate()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension LastViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.appearShow(showMsg: (String(k_frameName) + String(kPlayTitle)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_indicatorName.replacingOccurrences(of: "icon", with: "s") + String(kPopValue)), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_labelActualData.replacingOccurrences(of: "result", with: "l") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_topTitle.replacingOccurrences(of: "direction", with: "r") + kByContent.replacingOccurrences(of: "push", with: "se") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.rowData()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SendSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            enablegoingStart()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func motivation() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DelayTextConvertible.Call : DelayTextConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SphericPlayListener.titleType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func tableBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.priceUp()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func fileTo() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.barDoingCurrent()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func visibleMessage() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_connectValue : self.chatModel?.serviceTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            serration()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.serration()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func serration() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !SendSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.appearShow(showMsg: (String(k_observerTitle.suffix(7)) + k_presentName.lowercased()).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_labelActualData.replacingOccurrences(of: "result", with: "l") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_topTitle.replacingOccurrences(of: "direction", with: "r") + kByContent.replacingOccurrences(of: "push", with: "se") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SendSocketDelegate.shared.castToDataFormattingSub(info: socketDict)
        //: popCurrentViewController()
        enablegoingStart()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func imageEqual(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(k_pointName))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.nearAction(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.sinceErrorNext()
            //: createTalkRoom()
            soliloquise()
            //: self.stopBGM()
            self.fileTo()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func fatherToTreatNurseLogId(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.sign()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.observerTask(show: false)
        //: self.stopBGM()
        self.fileTo()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.sinceErrorNext()
        //: self.createTalkRoom()
        self.soliloquise()
    }
}

// MARK: - UserObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension LastViewController: UserObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func vacuumBagData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_layerData.prefix(4)) + "has " + String(k_writeShowPlayerValue.prefix(7))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_modelMakeData.replacingOccurrences(of: "current", with: "ca") + "lSex")].stringValue == AppTextProtocol.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_imageData.prefix(4)) + "as h" + String(kAppName.prefix(4)) + kLabText.replacingOccurrences(of: "medium", with: "u")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        enablegoingStart()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func aggregationButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        fatherToTreatNurseLogId(logId: json[String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func outsideEqual(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kSendQuantityimateData.suffix(5)) + String(k_contentDiskData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(k_layerData.prefix(4)) + "has " + String(k_writeShowPlayerValue.prefix(7))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_rawResultData) + String(k_privacyName))].stringValue == AppTextProtocol.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_imageData.prefix(4)) + "as h" + String(kAppName.prefix(4)) + kLabText.replacingOccurrences(of: "medium", with: "u")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kPingContent.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kObserverTitle.suffix(5)) + "ther " + k_startData.replacingOccurrences(of: "background", with: "n") + " up t" + String(kCancelValue.suffix(6)) + String(kVideoText)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.fromDismiss()
        //: popCurrentViewController()
        enablegoingStart()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func statUp(msg: String, data: [String: Any]) {
        //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(kBottomTitle.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(kFileData))] as? String else { return }
            //: let httpsUrl = RowReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RowReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = GiftAddConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: MiraclePushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            MiraclePushManager.share.pathTo(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            CurrentThen.magnitudeimateCan(title: nil,
                                               //: message: msg,
                                               message: msg,
                                               //: leftBtnTitle: "Cancel".localized,
                                               leftBtnTitle: (String(k_frameValue)).localized,
                                               //: rightBtnTitle: "Get gold coins".localized) {
                                               rightBtnTitle: (String(k_tableName.suffix(5)) + "old " + kClearText.replacingOccurrences(of: "moment", with: "co")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                CurrentThen.meeting()
                //: } rightBlock: {
            } rightBlock: {
                //: MiraclePushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                MiraclePushManager.share.noBuild(clickEvent: kLet_environmentValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func anyFirst(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.deviseData(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func modelMessage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(k_tapTitle))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.handle(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.addPoint(point: addBean)
        }
    }
}

// MARK: - RecordObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension LastViewController: RecordObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func levelBar(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_layerData.prefix(4)) + "has " + String(k_writeShowPlayerValue.prefix(7))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(k_styleToName))].stringValue == AppTextProtocol.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_imageData.prefix(4)) + "as h" + String(kAppName.prefix(4)) + kLabText.replacingOccurrences(of: "medium", with: "u")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.appearShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.rowData()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            SendSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        enablegoingStart()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func tableData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        fatherToTreatNurseLogId(logId: json[String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!].intValue)
    }
}

// MARK: - DirectorObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension LastViewController: DirectorObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func anonymous(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (k_topTitle.replacingOccurrences(of: "direction", with: "r") + kByContent.replacingOccurrences(of: "push", with: "se") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.appearShow(showMsg: (String(k_observerTitle.suffix(7)) + k_presentName.lowercased()).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.rowData()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                SendSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            enablegoingStart()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kCookieActionValue.suffix(7)) + String(k_textModelData)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard RowReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RowReactiveCompatible.share.appStatus == SkinSubscriptType.normal.rawValue else { return }
            //: MiraclePushManager.share.func__jumpToWebRecharge(sufficient: false)
            MiraclePushManager.share.noBuild(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kCookieActionValue.suffix(7)) + String(k_textModelData)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.appearShow(showMsg: msg)
            //: popCurrentViewController()
            enablegoingStart()
        }
    }
}

// MARK: - TimeViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension LastViewController: TimeViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func outsideIndexName() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (kVerticalValue.replacingOccurrences(of: "info", with: "a") + "ptCall"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func listAcross() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.appearShow(showMsg: (String(kQuoteData.prefix(6)) + "are c" + k_destroyTitle.replacingOccurrences(of: "effect", with: "c") + String(k_informationText)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_labelActualData.replacingOccurrences(of: "result", with: "l") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.appearShow(showMsg: (String(kQuoteData.prefix(6)) + "are c" + k_destroyTitle.replacingOccurrences(of: "effect", with: "c") + String(k_informationText)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_topTitle.replacingOccurrences(of: "direction", with: "r") + kByContent.replacingOccurrences(of: "push", with: "se") + "Call"), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_firstName.map{lastMake(better: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.appearShow(showMsg: (String(k_applicationData.prefix(5)) + String(k_equalAtValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            SendSocketDelegate.shared.castToDataFormattingSub(info: [String(bytes: k_shareViewData.map{telephoneConversation(mode: $0)}, encoding: .utf8)!: (k_indicatorName.replacingOccurrences(of: "icon", with: "s") + String(kPopValue)), String(bytes: kLengthLabelName.map{modelValue(gift: $0)}, encoding: .utf8)!: [String(bytes: k_indexValue.map{$0^55}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        enablegoingStart()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func jam(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.objectWith(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func forbidden(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.observerTask(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.cellStart(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.countimate()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func cellWindow() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        SendSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = EqualRefreshPositionMiniView.sightColorAppear()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.showMode()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.colorFrom()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.belowResume()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        enablegoingStart()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func imageSucceed() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension LastViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func delouseSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.horseIn(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func enterImage() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
